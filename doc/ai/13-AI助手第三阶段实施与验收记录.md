# RuoYi AI 助手第三阶段实施与验收记录

> 日期：2026-09-18  
> 状态：实施中（A-H 主体已落地并持续通过 CI；I 最终全栈验收进行中）  
> 方案：`11-AI助手第三阶段推进目标与验收方案.md`  
> 实施计划：`12-AI助手第三阶段实施计划.md`

## 1. 本文件用途

本文件只记录第三阶段**实际发生的事实和验收证据**，不重复写理想方案。

要求：

- 每条记录必须能回指 R3-01 ~ R3-18。
- 计划里写了但实际没有做的，不得写成完成。
- CI 绿色不能自动替代用户场景验收。
- 如果实施与方案不同，要明确写“实际偏差、原因、是否仍满足原始诉求”。
- 如果某需求只完成技术部分但用户体验未通过，状态保持“已实现，未验证”。

## 2. 开始实施时填写的基线

### 2.1 仓库

- 后端：`ShanySky/RuoYi-Vue:chatgpt/ai-agent-assistant`
- 前端：`ShanySky/RuoYi-Vue3:chatgpt/ai-agent-assistant`
- 后端第三阶段代码实施前基线 SHA：`7016e11556b1c4ac1dfade4a529d7126b5d2201b`
- 前端第三阶段代码实施前基线 SHA：`93a41207f7c76836ca7c53cb88ca4b729563dee9`
- 后端 master：`a6ea55ecf74ae6cdb063f2b413d66a33f29bf828`
- 前端 master：`838965c5a18d2c61b73ec30c6e288057aaa08b63`
- 当前盘点时两开发分支均 `behind=0`；后端/前端仅在独立开发分支持续前移，master 未修改。

### 2.2 技术版本

- Java：17
- Spring Boot：4.1.0
- Spring AI：2.0.1
- Node.js：CI 使用 Node.js 20
- Vue：3.5.26
- Vite：6.4.1
- Element Plus：2.13.1
- MySQL/Redis 验收版本：MySQL 8.0 / Redis 7.4（GitHub Actions 服务容器）

### 2.3 第三阶段开始前回归

- Backend CI：run `35322805306` 已通过（Maven build、MySQL/Redis、Spring Boot 真启动、Provider/Agent API）。
- Frontend CI：B1 最新 run `35323214700` 通过；B3 中文化 runs `35322877317` / `35322885363` 通过。
- Fullstack E2E：run `35323880260` 已成功；真实启动 MySQL/Redis/Spring Boot/Vite，并通过 Chromium 浏览器第二阶段闭环回归。
- 第二阶段 Public Preview / 当前页面基线：待填写

## 3. 需求验收总表

| ID | 用户诉求 | 实现状态 | 自动验证 | 用户场景验收 | 最终结论 |
|---|---|---|---|---|---|
| R3-01 | 聊天字体可调整 | 账号级偏好已实现 | Fullstack 字体/Dock/reload 已通过 | 重登与最终视觉回验待 I | 待最终验收 |
| R3-02 | 停止当前执行 | 已验证 | Fullstack + DB 断言通过 | 已通过 | 通过 |
| R3-03 | AI 内双 ESC 安全中断 | 已验证 | 最新 Fullstack 场景 16 | AI 内/业务页面焦点边界通过 | 通过 |
| R3-04 | 运行中继续补充 / Steering | 已验证 | 最新 Fullstack 场景 17/18 + DB 断言 | 旧 Run/旧 WRITE 不再继续，最新指令接管 | 通过 |
| R3-05 | GPT Prompt Cache | Runtime/cache key/mock usage 已验证 | Backend CI 已通过 | 真实 GPT cache read 待 I | 待最终验收 |
| R3-06 | Reasoning 中文与真实档位 | 中文 UI/协议枚举已实现 | Frontend + Fullstack 模型/档位切换通过 | 真实 GPT 请求参数待 I | 待最终验收 |
| R3-07 | 自动上下文压缩并续跑 | Runtime/Checkpoint/自动续跑已验证 | Backend + H27 Fullstack 通过 | 真实模型语义质量待 I | 待最终验收 |
| R3-08 | 每模型上下文高级设置 | 已验证 | Frontend + Fullstack 场景 5a/H27 | 已通过 | 通过 |
| R3-09 | 模型选择器与聊天 UI 精修 | 已实现 | Production build + 浏览器功能回归通过 | 最终视觉人工验收待 I | 待最终验收 |
| R3-10 | 完整语义化页面能力 | 24 个主/子页面路由进入统一能力目录，动态 schema 与专项动作已补强 | Backend/Frontend/Fullstack 最新 HEAD 全绿 | 代表性全动作/权限回验进入 I | 待最终验收 |
| R3-11 | Prompt 数据库化与管理员可配置 | 已实现 | Fullstack H26/H27/H29 + DB 断言 | 修改/恢复/运行时生效已通过 | 通过 |
| R3-12 | 其他业务页面全面接入 AI | 24 个主/子页面路由已纳入覆盖矩阵；特殊页面安全边界明确 | 最新三套 CI 全绿 | 最终覆盖矩阵与代表性操作回验待 I | 待最终验收 |
| R3-13 | 跨页面导航与连续业务操作 | 已实现 | Fullstack 场景 19 | 同一 Conversation/Run 跨页创建角色并授权已通过 | 通过，组合边界待 I |
| R3-14 | Conversation/Run/UI 状态刷新恢复 | 已实现 | Fullstack 场景 20/21/24 | F5/pending WRITE/多 Tab 已通过，page instance 组合边界待 I | 待最终验收 |
| R3-15 | 用户历史会话与长期恢复 | 已实现 | Fullstack 场景 20/22/23 | 历史、重命名、退出重登/恢复已通过 | 通过 |
| R3-16 | 用户偏好、历史入口、自动恢复与撤销 | 已实现 | Fullstack 场景 22/23/25 | 恢复/撤销/自动恢复与模型回退已通过；最终视觉体验待 I | 待最终验收 |
| R3-17 | 系统级与用户级 AI 设置分层 | 已实现 | Fullstack H28/H30/H31 + 场景 25 | 页面开关、策略、模型失效回退与管理入口已通过 | 通过，边界总回归待 I |
| R3-18 | 管理员只读会话审计 | 已验证 | Fullstack `35341391522` attempt 2 | 跨用户只读可看、普通 API/Turn 禁止接管 | 通过 |

## 4. 实施日志

> 后续按时间顺序追加，不覆盖历史事实。

### 2026-09-18：第三阶段建档

- 已确认第三阶段目标与实施计划。
- 用户明确要求：方案、计划、实施、验收必须围绕原始诉求层层递进，不得用纯技术指标替代用户场景验收。
- 正式代码尚未进入第三阶段实施。

### 2026-09-18：第三阶段需求扩展与文档校正

- R3-11 ~ R3-18 已纳入统一需求追踪链。
- 新增范围包括：Prompt 数据库化、其他页面全面接入、跨页面操作、刷新恢复、历史会话、用户偏好、AI 管理分层和管理员只读会话审计。
- 明确 Prompt Cache 默认由 Runtime 利用，不增加普通管理员“关闭缓存 / Cache Key 策略”配置。
- 明确“哪些页面接入 AI”本阶段提供管理员级全局配置；复杂风险策略编辑器和全局 Tool/Capability 策略 UI 延后，但固定安全机制本阶段必须可靠。
- 明确管理员审计其他用户会话只读，不能接管或继续他人 Conversation。
- 本次只更新第三阶段文档，不代表相关产品代码已经实施。


### 2026-09-18：续跑进度盘点

- 后端当前开发分支 HEAD：`1a827f9c28fac2b282f4d14a75e5366d67e9606e`。
- 前端当前开发分支 HEAD：`e3ade48b85092612c69b81b61106a19acad933e4`。
- 后端相对 master ahead 134 / behind 0；前端 ahead 61 / behind 0；master 未修改。
- 已发现上一轮实际已经提交一批第三阶段代码，但计划/验收文档没有同步标记进度，因此本次先做真实代码盘点再继续。
- 后端已提交：phase3 schema、Run/Checkpoint/Prompt/UserPreference、Run-aware Agent Loop、Stop/Steering 基础、Checkpoint compaction、Prompt Cache key、Conversation/Run 恢复等基础实现。
- 前端已提交：全局 AI Store、历史/偏好 API 基础、reasoning 中文化和模型选择器 Chevron 精修。
- 前端最新 CI（HEAD `e3ade48b85`）成功。
- 后端最新 Backend CI 与 Fullstack E2E（HEAD `1a827f9c28`）均失败在 `Build backend`，因此后端第三阶段代码当前不能视为已验证。
- 当前 AI Assistant 主组件仍禁用运行中的输入与模型选择，发送按钮仍是 loading 发送态；Stop、双 ESC、Steering UI 尚未接完。
- 后续要求：每完成阶段或小阶段，立即同步更新 `12` 的状态和本文件证据，再进入下一项。

### 2026-09-18：A1 基线恢复与 B1/B3 小阶段推进

- 修复 `AiRunService.start` 新增 `clientRunKey` 后调用方签名未同步的问题，commit `a0038200f8f11d1990a220a377577b9c6e89fd19`。
- 为 Backend CI 增加 Maven build 日志 artifact，commit `2cf46b0766f867d279fb17e1cb071082a48bee95`，便于后续失败直接定位。
- 修复 Prompt Cache 稳定前缀重排导致 Tool Result 续跑顺序错误的问题：Tool Result 轮次严格保持 USER → ASSISTANT(tool) → TOOL 历史顺序；新用户轮次才把动态页面上下文置于最新用户消息前，commit `7570f3f465f69888d9fa1217d868141beefe046c`。
- Backend CI run `35322805306` 完整成功，证明当前后端可编译、可启动并通过现有 Agent API 闭环。
- Reasoning 中文化补齐 Quick Settings / 完整配置页，前端 commits `dfcd94f52c6db70b47099f15188badb5d191de17`、`dd819221646076711bb3d454d4a06bae170317f9`；对应 Frontend CI 成功。
- 浏览器 E2E 失败截图确认 UI 已显示中文“无 / 最低 / 低 / 中 / 高 / 极高 / 最高”，失败原因为测试仍查找 `High / Low`；测试已在 frontend commit `89803b52dafd7aefdc018924a14d94b46a53ec9e` 同步为中文。
- B1 字体偏好已落入账号级 AI Preference：Quick Settings 提供“小 / 标准 / 大 / 特大”，Assistant 小窗/Dock 共用 CSS 变量；frontend commits `16c23e84817baddcad8d0304b3a91fbaa740ee77`、`e1819e7d9cbafc13d53460d4939a8c1760b3bbc4`；Frontend CI runs `35323184459` / `35323214700` 成功。
- B1 尚缺专项浏览器场景验证（切 Dock、reload、重登），不能仅凭回归绿标记最终“已验证”。
- B3 已通过新的 Fullstack E2E 回归，但真实 GPT 档位仍留到后续真实 Provider 验证。
- A1 基线复核已完成：Backend CI `35323880243`、Frontend CI `35323214700`、Fullstack E2E `35323880260` 均成功。

### 2026-09-18：B/C 运行控制专项闭环

- Frontend CI 持续绿色；Stop/Steering/双 ESC 相关实现位于前端 `eba4676904`、`523b39288c`、`538f410b01`，后续补充内部 Popover Escape 修复 `afcd305d47`、`44ad6fef19`。
- 后端 Run 取消/Steering 基础已通过 `clientRunKey` + `ai_run` 持久化；commit `62bc0aea00602d3786ffae0eacacd5e77384413a` 在每次模型调用前再次检查 Run 是否仍活动，防止 superseded/cancelled Run 继续调用模型。
- 取消路径曾因合成 `InterruptedException` 污染 Servlet 线程 interrupt 标记，导致 Druid 获取连接中断；已在 `20a21995264371fc8e45b0e13f59b76cf06b813c` / `8b9d371422d1b41e3a20d3e0f25ac48bcac74cd3` 修复。
- Fullstack E2E run `35325844042` 成功，浏览器覆盖字体、Stop、双 ESC 内部菜单、Steering；DB 额外验证 `USER_STOP` / `DOUBLE_ESC` 的 CANCELLED、`STEERING` 的 SUPERSEDED，且旧 Run 的 `SLOW_STOP_DONE / SLOW_ESC_DONE / STEER_OLD_DONE` 均未落入 Assistant 消息。
- R3-03 已补业务页面普通输入焦点防误停断言；AI 内部浮层第一 ESC 消费、700ms 双 ESC 停止主链继续保持通过。业务 Dialog 更细粒度焦点仍会在最终 I 浏览器回归中再覆盖一次。
- R3-04 已补“旧 Run 正等待 WRITE 确认时用户 Steering”真实浏览器场景：旧确认卡被取消，旧 nickname `SHOULD_NOT_SAVE` 未持久化，最新 Steering 返回 `WRITE_CANCELLED_BY_STEERING`。

### 2026-09-18：D/E Context、Compaction 与 Cache 基础闭环

- `ai_model` 已持久化 64K 工作预算、自动压缩开关和 75% 阈值；前端新增复用的模型“高级设置” Dialog，并同时接入完整 AI 配置与 Quick Settings。
- Frontend CI `35326421533` 成功；Fullstack E2E `35325844042` attempt 2 验证 64K/75% 默认值、72K/70% 修改持久化及恢复默认。
- Context 估算已覆盖 System、Checkpoint、近期消息、Tool arguments、动态 Page Context 与 Tool schema；无 Provider token usage 时使用字符估算并留固定 overhead。
- Run usage 由覆盖改为累计，Compaction 模型调用也计入同一 Run。
- Backend CI `35326094428` 成功：低预算场景稳定触发 Checkpoint，压缩后同一指令自动继续；DB 断言 Checkpoint 与 cache-read usage 成功。
- Backend CI `35328061798` 成功：Compaction Prompt 已采用固定结构；mock 记录 `COMPACTION_PROMPT_STRUCTURED=1`、`COMPACTION_SOURCE_HAS_A=1`、`CHECKPOINT_REINJECTED=1`、`LATEST_USER_RAW=1`；LONG_CONTEXT_A/B 两条原始 USER 消息仍保留在数据库。
- Prompt Cache 官方能力已按当前 OpenAI/Spring AI 2.0.1 文档重新复核；实现改用 Spring AI `Usage.getCacheReadInputTokens()/getCacheWriteInputTokens()` 原生 API。
- mock Provider 证据只用于证明协议/Runtime/usage 链路，真实 GPT cache-read 与成本下降仍明确保留到 I 阶段。

### 2026-09-18：G 恢复链与 H 管理模块续跑

- 已重新核对真实仓库而非沿用旧会话进度：F6 跨页代表性闭环此前已通过，当前主推进点实际为 G/H。
- G 历史恢复 E2E 暴露“会话标题允许重复、测试按标题取第一个”的歧义；前端 b83178a4 为历史项暴露稳定 Conversation ID，9b79333a 改为按 ID 精确恢复。随后综合 run 已跨过旧失败位置。
- G4 增加用户默认模型失效回退：6e84d730 在 Store 中回退系统默认，93cfc47d 增加可见提示，42b95dba 增加管理员禁用用户默认模型后的真实 E2E。上述 Frontend CI 均成功。
- G5 最新综合 run 35338379748 已通过 1-21 项，进入历史恢复场景后确认目标历史 Conversation 恢复正确；“撤销恢复”断言失败是 Playwright 未等待异步服务器恢复完成，产品逻辑会恢复原 Conversation。frontend 57894c34 已改为等待目标 Conversation 标签后再断言，并已重跑该 Fullstack job。该 run 在修复前仍记为失败证据，不伪装成通过。
- H 后端基础 commit 68dc8afc：页面接入配置、会话策略/定时清理、Prompt 管理增强、独立只读会话审计与一级 AI 管理菜单；Backend CI 成功。
- H Runtime commits 1ca135d5 / d1b265c0：Page Capability 开关真正进入 Agent Loop 服务端批准边界；导航目标同样受页面开关约束；System / Compaction Prompt 从数据库读取并将受控模板变量渲染进真实模型调用。对应 Backend CI 均成功。
- H 前端 commit 0c0bdad0：AI 服务、模型管理、提示词管理、页面能力、会话策略、会话审计页面；Frontend Production CI 35338001284 成功。
- H 权限/日志/兼容 commits 2d3241ee、e0ecfc03、56391479：模型系统配置进入 RuoYi 操作日志；Prompt 权限与 Provider 配置权限分离；保留隐藏 /system/aiConfig 第二阶段兼容路由；一级“AI 管理”使用独立子菜单与权限。
- H 综合验收已加入 frontend cafef71b 与 backend workflow f740df41：将真实验证 System Prompt 请求 marker、Compaction Prompt 自动 Checkpoint、页面开关服务端拒绝 Tool、Provider-only 用户不能读审计、恶意 Prompt 不能绕过 Tool/权限、会话策略物理清理、AI 管理路由和操作日志。cafef71b Frontend CI 35338341921 已成功；Fullstack 正在随 G5 竞态修复重跑。
- 当前不宣布 G/H 完成：必须等重跑实际越过 G5 并跑完 H26-H31，随后才回填最终验收勾选与 run ID。

## 5. R3-01 聊天字体

### 实际实现

- 用户偏好后端已有 `chatFontSize`，允许 `small / standard / large / xlarge`。
- Quick Settings 已增加“小 / 标准 / 大 / 特大”选择。
- AI Assistant 通过统一 CSS 变量 `--ai-chat-font-size` 同时作用于消息正文、输入框、Tool 状态和工作状态，小窗/Dock 共用。
- 偏好通过账号级 `/ai/preferences` 保存，不再只依赖浏览器 localStorage。
- 前端实现 commits：`16c23e84817baddcad8d0304b3a91fbaa740ee77`、`e1819e7d9cbafc13d53460d4939a8c1760b3bbc4`。

### 自动验证

- Frontend CI run `35323184459`：成功。
- Frontend CI run `35323214700`：成功。
- 尚需在 Fullstack E2E 增加 computed style / Dock / reload 场景后才能标记“已验证”。

### 用户场景验收

待验证：实际切换四档字体、小窗 ↔ Dock、刷新、重新登录后的恢复，以及标题/按钮不被正文设置误放大。

### 偏差/问题

无已知方案偏差；当前属于“已实现，待场景验收”。

## 6. R3-02 / R3-03 Stop 与双 ESC

### 实际实现

- 运行中主按钮切换为“停止”，前端 Abort 当前 HTTP 请求，同时调用后端 Run cancel。
- Run 已有 `CANCELLED` 状态、取消原因、pending Tool 取消和 in-flight Future cancel。
- 迟到模型响应在前端 generation 与后端 Run 状态两层都被丢弃，不再继续 Tool/WRITE。
- 双 ESC 使用约 700ms 二次确认，第一次显示轻量提示；AI 内模型 Popover 先关闭且不计入停止手势。
- 双 ESC 与 Stop 使用同一后端 cancel 语义，取消原因分别记录 `USER_STOP` / `DOUBLE_ESC`。

### Run 取消语义

Fullstack E2E + DB 已验证：Stop/双 ESC 后对应 Run 为 CANCELLED；旧模型迟到内容没有写入 `ai_message`。

### 双 ESC 焦点范围

- AI 输入区双 ESC：已验证。
- AI 模型 Popover 打开时第一次 ESC 只关 Popover：已验证。
- 普通业务输入焦点下双 ESC 不会停止 AI，已通过 `35327679056`；业务 Dialog 焦点在 I 阶段继续做回归性覆盖。

### 自动验证

- Frontend CI：相关 commits 均成功。
- Fullstack E2E run `35325844042`：成功。
- DB 持久化断言：`USER_STOP`、`DOUBLE_ESC` CANCELLED，旧 Assistant 响应计数为 0。

### 用户场景验收

R3-02 Stop 与 R3-03 双 ESC 主场景已通过自动浏览器闭环；普通业务输入焦点防误停已补齐，最终 I 继续覆盖业务 Dialog 焦点。

### 已知边界

Stop 不伪装回滚已经提交完成的业务 WRITE；该原则保留到 WRITE/Steering 边界场景继续验证。

## 7. R3-04 Steering

### 实际实现

- AI 运行中输入框保持可编辑。
- 有补充文字时显示“发送补充”；发送后新消息立即进入同一 Conversation。
- 新 Run 创建时原子 supersede 旧活动 Run，取消旧 pending Tool / in-flight 模型调用。
- 前端 generation 防止旧响应继续渲染或执行页面 Tool。

### Supersede 行为

Fullstack E2E 已验证 `STEER_OLD → STEER_NEW`：最终只出现 `STEER_NEW_OK`，旧 `STEER_OLD_DONE` 不渲染且不入库；旧 Run 在数据库为 `SUPERSEDED / STEERING`。

### Tool / WRITE 安全

旧 Run 尚未执行的 Tool 会随 Run supersede 失效；“等待 WRITE 确认时 Steering”仍需真实用户管理页面闭环验证，当前不能提前写通过。

### 自动验证

- Fullstack E2E run `35325844042`：成功。
- DB 断言：SUPERSEDED/STEERING 存在，旧 Assistant 迟到响应为 0。

### 用户场景验收

基本运行中补充/纠偏已通过；pending WRITE 纠偏场景待补。

## 8. R3-05 Prompt Cache

### 当前 Provider / 模型

自动化阶段使用 deterministic OpenAI-compatible mock 验证协议和持久化链路；真实 GPT Provider 留到 I 阶段，不用 mock 结果冒充真实缓存收益。

### 官方资料复核

2026-09-18 已重新核对：

- OpenAI 当前 API 提供可选 `prompt_cache_key`，GPT-5.6+ 还提供 prompt cache options/diagnostics 能力。
- Spring AI 2.0.1 `OpenAiChatOptions` 原生支持 `promptCacheKey` / `extraBody`。
- Spring AI 2.0.1 `Usage` 原生提供 `getCacheReadInputTokens()` / `getCacheWriteInputTokens()`；OpenAI 主要提供 cache read tokens。

### 实际请求结构调整

- System Prompt 从动态 route/page context 中拆出，保持长期稳定。
- Checkpoint 放在稳定系统规则之后。
- 动态 route / page instance / page context / 当前可用工具后置到最新用户消息前。
- Tool Result resume 保持严格 USER → ASSISTANT(tool) → TOOL 顺序，不为了缓存破坏协议。
- conversation 请求使用 `ruoyi:conv:<conversationId>:system:<promptVersion>` cache key；compaction 使用独立 conversation compaction key。
- Run Usage 改为跨主调用/压缩调用累计。

### Cache Usage 证据

Backend CI run `35326094428`：

- mock response 提供 cached token usage。
- `ai_run.input_tokens > 0`、`cache_read_tokens > 0`、`total_tokens > 0` 断言通过。
- mock 请求日志确认 conversation 与 compaction 请求均实际携带 `prompt_cache_key`。
- 该证据证明 Spring AI → Provider → Usage → DB 链路成立。

真实 GPT 的首轮/第二轮/后续 cache read 与成本对比：**待 I 阶段真实 Provider 验证**。

### OpenAI-compatible 兼容性降级证据

- Runtime 默认仍发送稳定的 `prompt_cache_key`，不会因为“可能不兼容”主动放弃 GPT 缓存能力。
- 仅当异常链明确指出 `prompt_cache_key` / prompt cache key 属于 unsupported / unknown / unrecognized / not allowed / invalid parameter 等参数错误时，主 Agent 调用与 Compaction 调用各允许**单次**不带 cache key 重试。
- 普通网络错误、模型错误或其他 4xx/5xx 不进入该降级路径，避免无条件双请求。
- Backend CI `35345817904` 使用严格 mock：第一次携带 `prompt_cache_key` 返回 400 `Unsupported parameter: prompt_cache_key`，Runtime 第二次无 key 请求成功返回 `CACHE_FALLBACK_OK`；证明 OpenAI-compatible Provider 不会因为缓存参数直接不可用。
- 审计 Runs/Usage 已增加只读缓存诊断提示：能区分“已观察到 cache read”“Provider 未返回可用 usage”“有 input 但未观察到 cache read（稳定前缀不足/前缀变化/Provider 不返回明细等常见原因）”，不暴露 Prompt 明文。

### 用户目标验收

Runtime 已具备利用缓存、兼容严格 Provider 并记录/解释命中的基础；“真实 GPT 长会话是否实际减少重复输入成本”仍未最终验收，必须等待真实 Provider 凭据后由 Real GPT workflow 取得 cache read 硬证据。

## 9. R3-06 Reasoning

### 实际支持档位

- 后端通用能力探测会分别尝试 `none / minimal / low / medium / high / xhigh / max`；不支持的值不会写入模型支持列表。
- GPT-5.6 目标档位仍按方案以实际探测结果收敛到 `none / low / medium / high / xhigh / max`；`minimal` 只在其他 Provider 真正支持时作为独立档位存在。

### 中文映射

- `none → 无`
- `minimal → 最低`
- `low → 低`
- `medium → 中`
- `high → 高`
- `xhigh → 极高`
- `max → 最高`

模型选择器、Quick Settings、完整 AI 配置页已统一使用中文显示；API/DB 继续保存 Provider 原始英文值。

### 自动验证

- Frontend CI runs `35322877317` / `35322885363`：成功。
- Browser E2E 测试已从旧 `High / Low` 文案修正为 `高 / 低`，等待新 Fullstack E2E 回归。

### 用户场景验收

待新的 Fullstack E2E 与后续真实 GPT Provider 验证后确认。

## 10. R3-07 / R3-08 Context 与自动压缩

### 模型设置

- 默认工作窗口：64K。
- 默认压缩阈值：75%。
- 自动压缩：开启。
- 数据库字段：`context_window_tokens / auto_compaction / compaction_threshold_percent`。
- API：`PUT /ai/config/models/{modelId}/runtime-settings`。
- 完整 AI 配置与 Quick Settings 共用 `AiModelRuntimeSettingsDialog`。
- Fullstack rerun `35325844042` attempt 2 已验证默认 64K/75%，修改 72K/70% 后 API 持久化，再恢复默认。

### Checkpoint Prompt

当前 DB seed 已使用专用 Compaction Prompt，要求保留目标、决策、已完成/当前/下一步、未解决事项、Agent/页面/模型状态、Tool 结果、不可逆 WRITE、pending 状态、最新 Steering、关键 ID/配置、事实/暂定区分和压缩后需复核项，并禁止重复已完成工作。后续 H 阶段会提供管理员编辑/恢复默认 UI。

2026-09-18 本轮把该 Prompt 升级为固定 8 组栏目，并让压缩 payload 携带历史 Run status/cancel reason；未配对旧 Tool Call 明确标注“已取消或未完成，不得视为已执行成功”。CI `35328061798` 已证明新结构实际发送到 Compaction Provider，而不是只存在于 SQL 文件。

### 压缩测试会话

Backend CI run `35326094428` 临时把模型工作预算调为 8K、阈值 50%，连续发送两个约 11.8K 字符的用户回合以快速稳定触发 compaction；测试结束恢复 64K/75%。

### 压缩前状态

- 原始消息完整保存在 `ai_message`。
- 第二轮触发前第一轮用户/Assistant 历史已存在。
- 最新用户消息通过 cutoff 规则排除在被压缩范围之外。
- 动态 Page Context / Tool schema 已纳入预算估算。

### 压缩后验证

- 生成 `ai_checkpoint` ACTIVE 记录：通过。
- Checkpoint 后自动继续当前 Run、用户无需重发：通过。
- 第二轮最终仍返回正常 `AI_OK`：通过。
- 原始 DB 消息未删除：通过。
- 压缩模型调用 Usage 计入同一 Run：通过。
- 已完成 WRITE 不重复：Backend CI `35353739061` 使用真实 Agent Loop 先完成一次 `page_system_user_edit_submit` Tool Result，再以低预算触发 compaction；Checkpoint 明确保留 `WRITE_ALREADY_DONE` 和“不要再次执行”状态，压缩后最终请求返回 `NO_REPEAT_OK`，mock 日志出现 `COMPACTION_PRESERVED_COMPLETED_WRITE=1` / `COMPLETED_WRITE_NOT_REPEATED=1`，并且该 Conversation 的 pending Tool 计数仍为 1。
- 真实模型是否完整记得主题/进度/下一步/最新 Steering：仍待 I 阶段真实 Provider 凭据；mock/确定性证据不能替代真实模型语义验收。

## 11. R3-09 UI 精修

### 模型选择器

- 正规 Chevron、模型名截断、Hover/Selected 和 reasoning 二级入口均已落地；Fullstack 浏览器场景会真实展开模型选择器。
- `phase3-ai-model-picker.png` 已在 Fullstack `35353359451` 产出，且 workflow 强制要求非空。

### Stop / Steering / Compaction 状态视觉

- Stop 复用主发送按钮区域；Steering 使用“旧规划将在安全边界停止”轻量状态；Compaction 使用“正在整理较早的会话上下文…”工作态，不新增模态确认。
- Fullstack 场景 14a/14b/14c、15~18 已对状态切换与安全行为完成浏览器自动验收。

### RuoYi 主题一致性

- 机器侧打开 `phase3-ai-floating.png` / `phase3-ai-dock.png` / `phase3-ai-model-picker.png` 检查：未发现全屏遮罩、面板裁切、Dock 未让位、聊天/输入区溢出等明显布局回归。
- “视觉风格是否主观上与 RuoYi/Element Plus 足够一致”仍属于最终人工预览项，不能用自动截图替代用户确认。

### 浏览器截图/人工验收

- Fullstack `35353359451` 浏览器步骤已成功并生成三张目标 Chromium 全页截图，随后 workflow 的文件存在性检查也成功。
- 该 run 最终红色来自浏览器步骤之后的旧 MySQL 软删除断言，不影响上述截图和浏览器场景本身的证据效力。
- 最终当前 HEAD 公网预览与用户主观视觉确认仍待 I 阶段收口。

## 12. R3-10 完整语义化页面能力

### Page Capability 协议

2026-09-18 已形成 `ruoyi-semantic-page-v1` 基础协议：

- 前端 `toolRegistry` 为当前页面注册 `pageId / pageName / route / pageInstanceId / pageVersion`，页面切换或重新注册会产生新实例。
- `createAiCrudPageCapabilities` 统一描述查询字段、表单字段、页面动作、当前列表/选择/表单上下文，并生成语义化 Tool。
- `app_navigate` 只通过 Vue Router 导航；导航后等待新页面 Capability Instance ready。
- AI 每轮请求携带 route/pageInstanceId/pageVersion；Tool 真正执行前前端再次按请求快照校验，后端 Pending Tool Result 也校验页面实例，旧页面 Tool 在切页后失效。
- 前端权限只决定“是否向模型暴露”；后端 `AiFrontendToolPolicy` 重新决定权威 description/schema/risk/permission，业务 API 继续执行 RuoYi 原生权限和数据范围。
- WRITE / DANGEROUS_WRITE 仍必须经过 AI 面板内确认；不直接写数据库。

当前协议仍需继续补强：字段字典/校验规则的统一 schema 表达、通用动作的服务端权威 schema 覆盖范围、稳定 Tool 序列化，以及跨页面真实业务闭环。

### 页面能力覆盖矩阵

当前前端 `pageCapabilityCatalog` 共有 24 个主/子页面路由。下表记录真实代码接入边界；“受控导航”不是伪装成 CRUD 全覆盖，而是明确说明该类特殊页面暂不开放任意 DOM/脚本控制。

| 页面/路由 | 当前接入状态 | 主要能力/边界 | 服务端安全 |
|---|---|---|---|
| 用户管理 `/system/user` | 已接入 | 查询、表格/选择、新增/编辑、状态、删除、密码、授权、导入导出等语义能力 | ToolPolicy + 原 API 权限/数据范围 |
| 分配角色 `/system/user-auth/role/*` | 已接入并实测 | 查看/选择/提交用户角色 | 提交为 DANGEROUS_WRITE |
| 角色管理 `/system/role` | 已接入并实测 | 查询、增改、状态、删除、导出、数据权限入口 | 角色写入/数据范围为高影响动作 |
| 角色分配用户 `/system/role-auth/user/*` | 已接入 | 已授权/候选用户查询、批量授权/取消授权 | 授权变更为 DANGEROUS_WRITE |
| 菜单管理 `/system/menu` | 已接入 | 树查询、增改删、显示排序 | 权限校验；排序为受控 WRITE |
| 部门管理 `/system/dept` | 已接入 | 树查询、增改删、显示排序 | 原业务约束 + 受控 WRITE |
| 岗位管理 `/system/post` | 已接入 | 查询、增改、批量删除、导出 | 删除/写入确认 |
| 字典类型 `/system/dict` | 已接入 | 查询、增改删、数据项入口、导出、刷新缓存 | 删除/刷新按真实风险 |
| 字典数据 `/system/dict-data/index/*` | 已接入 | 数据项查询、表单与真实字段 schema | live schema + 原 API 权限 |
| 通知公告 `/system/notice` | 已接入 | 查询、增改删、查看、已读用户 | 富文本仅受控字段；无任意 DOM |
| 个人中心 `/user/profile` | 已接入 | 本人资料查看/编辑及页签操作 | 保持“本人”语义，不复用管理员越权能力 |
| 在线用户 `/monitor/online` | 已接入 | 查询、强退 | 强退为 DANGEROUS_WRITE |
| 定时任务 `/monitor/job` | 已接入 | 查询、增改、详情、启停、立即执行、日志、导出 | 启停/立即执行为高风险 |
| 调度日志 `/monitor/job-log/index/*` | 已接入 | 查询、详情/日志上下文 | 原权限控制 |
| 登录日志 `/monitor/logininfor` | 已接入 | 查询、删/清空、解锁、导出 | 清空为 DANGEROUS_WRITE |
| 操作日志 `/monitor/operlog` | 已接入 | 查询、详情、删/清空、导出 | 清空为 DANGEROUS_WRITE |
| 缓存监控 `/monitor/cache` | 已接入 | Redis 基本信息/命令/内存等只读指标 | 不擅自增加源码不存在的写能力 |
| 缓存列表 `/monitor/cacheList` | 已接入 | 缓存名称、键枚举、值查看、按名称/键/全部清理 | 清理动作固定 DANGEROUS_WRITE |
| 服务监控 `/monitor/server` | 已接入 | CPU/内存/JVM/磁盘只读指标 | 只读 |
| Druid `/monitor/druid` | 受控导航 | 外部/嵌入监控入口，不转换为任意 DOM Tool | 仅导航/页面上下文 |
| 代码生成 `/tool/gen` | 已接入 | 查询、预览、同步 DB、生成、进入编辑 | 同步/生成固定 DANGEROUS_WRITE |
| 修改生成配置 `/tool/gen-edit/index/*` | 已接入 | 基础信息、字段配置、排序、页签、提交 | 提交为 DANGEROUS_WRITE |
| 表单构建 `/tool/build` | 受控导航 | 图形化设计器保持特殊页边界 | 不采用坐标/任意 DOM 自动化 |
| 系统接口 `/tool/swagger` | 受控导航 | API 文档查看入口 | 不把任意 API 自动暴露成 Tool |

### 2026-09-18：用户管理完整能力最终闭环

- 前端 live schema 已补齐 add-only / edit-only、动态部门/岗位/角色/字典 options，以及用户名、密码、手机号、邮箱等真实表单校验约束；相关 Frontend CI 均成功。
- Fullstack `35355450361` 场景 31b 成功完成可回滚业务生命周期：新增两名临时用户 → 停用/启用 → 重置密码并使用新密码真实登录 → 打开导入 → 触发导出下载 → 批量删除。
- 同一 run 场景 31c 成功覆盖：全部安全可编辑字段（nickName / deptId / phonenumber / email / sex / status / postIds / roleIds / remark）真实保存并由业务 API 回读；全部查询条件组合真实执行；表格行实际勾选后 `selectedIds` 正确；用户详情 Drawer 可通过语义 Tool 打开。
- userName/password 只在新增 schema 暴露，已有账号编辑 schema 明确不暴露；昵称仍可编辑，延续第二阶段账号语义边界。
- 临时用户删除按 RuoYi 原生软删除语义处理：活动列表不可再查询，DB 保留删除态记录；Fullstack 末尾状态断言成功。
- 结论：F4 “完整用户管理能力”不再仅以 schema/注册数量作为证据，已通过真实业务闭环验收。

### 管理员页面接入配置

已实现 `/ai/admin/pages` 管理与服务端 Agent Loop 强制校验。Fullstack H28 已验证：管理员关闭 `/system/user` 后，即使客户端仍提交该页 Tool 定义，服务端也拒绝批准；恢复开关后重新可用。

### 跨页面导航实现

- `app_navigate` 仅通过 Vue Router，不使用坐标、DOM selector 或浏览器自动化控制生产页面。
- Page Capability 每次注册携带 `route / pageInstanceId / pageVersion`；导航后等待目标页面产生新的 instance，再把新 Page Context 作为 Tool Result 回传 Agent。
- 前端 Tool 真正执行前验证请求快照仍匹配当前页面；后端接收 Pending Tool Result 时再次验证 route/pageInstanceId/pageVersion。
- 当前 Router 只包含当前用户可访问的动态路由；`app_navigate` 对不存在/不可达路由直接拒绝。
- 用户角色分配隐藏路由 `/system/user-auth/role/:userId` 已注册专用 Page Capability，授权提交由服务端固定为 DANGEROUS_WRITE。

### 代表性跨页业务闭环

2026-09-18 Fullstack E2E `35333710107` 已通过以下真实业务链：

```text
同一 Conversation / 同一 Agent Run
→ 用户管理查询 ry
→ app_navigate 到角色管理
→ 查询 E2E 临时角色
→ 打开新增角色并填写
→ DANGEROUS_WRITE 确认后真实创建角色
→ 再次查询取得真实 roleId
→ app_navigate 到 /system/user-auth/role/2
→ 读取 ry 当前角色
→ 选择原角色 + 新角色
→ DANGEROUS_WRITE 确认后真实授权
→ 后端 /system/user/authRole/2 验证新角色 flag=true
→ 自动恢复 ry 原角色集合并删除临时角色
```

首次 run `35333203750` 在第 9 个 Tool 前被原有 8 次单轮限制安全截断；这不是业务写入错误，而是第三阶段跨页链长度超过第二阶段单页保护值。前后端随后同时把有界上限提高到 16 次：后端 SHA `8967a182ad7cb7f7292c91d8db0b0732379ef44e`，前端 SHA `4927086ee9f6dc47231ac2506babf50d0feabc11`。修复后 Backend CI `35333710063`、Frontend CI `35333712668`、Fullstack E2E `35333710107` 全部成功。

至少验证一个真实场景：

```text
同一用户业务目标
→ 页面 A 查询
→ 页面 B 检查/创建
→ 页面 C 关联/保存
→ Conversation 不变
→ 旧页面 Tool 不再可执行
```

### 未覆盖页面/能力及原因

- F6 代表性跨页主闭环已通过。
- 跨页过程中同时发生 Steering 或自动 Compaction 的组合场景尚未做专门浏览器 E2E，留 I 阶段交叉回归；现有 Run/Checkpoint/Steering 都是 Conversation 级后端状态，不依赖 route。
- F5 中部分特殊页面和专项动作仍继续实施，不能因 F6 已通过而把 R3-12 提前标记完成。

## 15. R3-14 应用级 Conversation / Run / UI 状态与刷新恢复

### 后端 Source of Truth

- `ai_conversation / ai_message / ai_run / ai_checkpoint / ai_pending_tool_call` 继续作为权威持久状态。
- `GET /ai/chat/conversations`、`GET /ai/chat/conversations/last`、`GET /ai/chat/conversations/{id}` 提供当前用户自己的历史、最近会话和详情恢复。
- 详情返回 Conversation、Messages、active Run、pending Tools、Checkpoint；所有普通接口按 owner userId 校验。
- F5 对仍处于 RUNNING/WAITING_TOOL 的旧 Run 使用 `CLIENT_RELOAD` 取消；退出登录使用 `POST /ai/chat/runs/cancel-all` 统一取消当前用户所有活动 Run，pending Tool/WRITE 同步取消。

### 前端 Global AI Store

前端 `src/store/modules/ai.js` 已成为 Conversation UI 状态镜像：conversationId、messages、activeRun、pendingTools、checkpoint、model/reasoning、Draft、history、账号偏好都在 Store 管理；`AiAssistant` 不再把核心会话状态锁在组件局部 ref。

### sessionStorage / localStorage 边界

- sessionStorage：当前 Tab `activeConversationId` 与未发送 Draft。
- localStorage：Dock 宽度、上次 floating/dock 模式，以及发送快捷键兼容缓存。
- 账号级模型/reasoning/字体/快捷键/双 ESC/历史入口/自动恢复/默认打开方式的主数据在 `ai_user_preference`。

### F5 / Ctrl+R 场景

- 空闲会话刷新：Fullstack E2E `35339406751` 场景 20 已通过，同一 Conversation、历史消息和未发送 Draft 均恢复。
- RUNNING / WAITING_TOOL 刷新：场景 21 已通过；旧 Run 以 `CLIENT_RELOAD` 安全取消，pending WRITE 未落库，Conversation 保持可恢复。
- pending Tool / pending WRITE 刷新：已验证不会自动继续危险操作。
- 刷新前旧 Page Capability Instance 失效：代码已按 pageInstanceId/pageVersion 校验，专项浏览器断言仍待补。
- 新实例注册后继续：跨页注册已在 F6 通过，F5 刷新组合断言仍待补。

### 多标签页

- 不同 Tab activeConversationId 隔离：`35339406751` 场景 24 已通过，两个 Tab 的 sessionStorage 保持独立 Conversation。
- 同一 Conversation 多 Tab 只允许一个活动 Run：服务端互斥已实现，I 阶段补并发专项回归。

### 用户场景验收

`35339406751` 已覆盖真实 F5、pending WRITE 刷新和多 Tab 恢复；剩余 page instance / 并发组合边界继续进入 I 回归。

## 16. R3-15 / R3-16 历史会话、用户偏好与恢复/撤销

### 我的会话

已实现并已由综合 Fullstack 场景 20/22/23 验证：

- 列表。
- 时间分组。
- 搜索。
- 切换。
- 新建。
- 重命名。
- 归档/删除策略。

### 退出登录 / 重新登录

Fullstack E2E `35339406751` 场景 23 已通过：退出登录时活动 Run 以 `USER_LOGOUT` 取消，当前 Tab Conversation/Draft 清理；重新登录后历史仍保留，开启自动恢复时恢复最近会话，旧 Run 的迟到响应不会继续写回。

重点验证：

- 历史 Conversation 仍存在。
- 上次 RUNNING Run 已安全中断。
- pending Tool / WRITE 不会自动继续危险操作。
- 用户可以主动恢复旧会话继续工作。

### 用户 AI 偏好

当前实际账号级字段已覆盖：

- 我的默认模型。
- 我的默认 reasoning。
- 聊天字体。
- 发送快捷键。
- 双 ESC。
- 历史入口。
- 自动恢复上次会话。
- AI 助手默认打开方式（默认小窗 / 默认 Dock / 恢复上次使用模式）。

设备级 Dock 宽度等本机优先设置单独记录。

### 恢复上次会话 / 撤销恢复

- 手动恢复：`35339406751` 场景 22 已通过。
- 自动恢复（默认关闭）：场景 23 的重新登录流程已通过。
- 恢复后短时撤销：10 秒撤销窗口已通过场景 22。
- 恢复前 Draft / Conversation / 模型状态还原：已通过。
- 产生新 Agent 行为后撤销立即失效：代码已实现，综合流程无回退。
- 撤销只切换 UI/Conversation，不执行业务 WRITE 回滚；文案理解留最终人工预览。

### 新手 / 高级用户体验

- 默认隐藏历史入口：`35339406751` 场景 22 已通过。
- 用户开启后历史按钮出现：场景 22 已通过。
- 历史入口不常驻占据业务界面：待最终人工预览验收。
- 默认打开方式已纳入账号偏好与重登恢复。
- 用户默认模型被管理员禁用后：场景 25 已验证回退系统默认模型并显示提示。

## 17. R3-17 系统级 AI 管理与用户级设置分层

### AI 管理一级模块

已建立一级 `AI 管理`，包含：

- AI 服务。
- 模型管理。
- 提示词管理。
- 页面能力。
- 会话策略。
- 会话审计。
- Token / cache usage 当前在只读审计详情的 Run/Usage 中展示，不额外建设复杂运营平台。

第二阶段 `/system/aiConfig` 保留隐藏兼容路由用于回归，不再作为可见主入口。

### 系统级配置

已实现 Provider、系统模型/系统默认模型、模型上下文与压缩参数、System/Compaction Prompt、页面接入开关和会话保留策略。会话策略复用 RuoYi `sys_config`：默认保留 90 天、允许归档、默认不允许用户删除；定时任务每天 03:20 清理超过保留期且无活动 Run 的 Conversation，同时提供管理员手动清理入口。

至少记录并验证：

- Provider / 系统可用模型 / 系统默认模型。
- System Prompt / Compaction Prompt。
- 页面接入开关。
- 会话保留天数。
- 是否允许用户归档/删除自己的会话。
- 历史会话清理/保留策略的实际执行方式。
- 归档、用户主动删除、系统自动清理三者的真实语义和边界。
- 清理策略是否会误影响仍需恢复的有效 Conversation。

### 用户级配置

用户级设置绑定账号：默认模型、默认 reasoning、聊天字体、发送快捷键、双 ESC、历史入口、自动恢复、默认打开方式；Dock 宽度等屏幕相关设置保持本机优先。系统级管理入口与个人设置已分层。

### 系统默认模型 / 用户默认模型

验证：

- 用户默认有效时优先：待验证。
- 用户默认被管理员禁用后回退系统默认：待验证。
- 用户无法选择系统未开放模型：待验证。
- 用户 reasoning 不能超出模型/系统允许范围：待验证。

### Prompt Cache 管理边界

- Runtime 继续自动利用稳定前缀/cache key，不提供管理员关闭缓存开关。
- 管理 UI 不提供 Cache Key 策略编辑器。
- input/cache read/cache write/total usage 已持久化到 Run，并在只读审计详情中展示。

### Agent 安全配置边界

- 页面接入开关：Fullstack `35339406751` 场景 28 已证明关闭 `/system/user` 后服务端不会批准该页业务 Tool。
- WRITE / DANGEROUS_WRITE、RuoYi 权限、Tool allowlist、page instance/version 校验继续由服务端代码执行，不依赖 Prompt。
- 未引入动作策略 DSL 或任意 Tool 编辑器。

## 18. R3-18 管理员只读 AI 会话审计

### 权限设计

已增加独立 `ai:conversation:audit` 权限；Provider/模型管理使用独立配置权限，普通 Conversation API 仍按 owner userId 校验。`35339406751` H29 前半段已验证：测试用户拥有 Provider 查看权限时 `/ai/config/provider` 可访问，但无审计权限访问 `/ai/admin/audit` 返回 403。

必须区分：

- 用户查看自己的会话。
- Provider / 模型管理权限。
- 全员会话审计权限。

### 审计列表与详情

已支持按用户、Conversation ID、时间、模型、状态、标题筛选；列表标识 Tool / WRITE / Stop / Steering / Compaction；详情只读展示 Message、Run、Tool/Pending、Checkpoint 与 token/cache usage。前端审计页没有输入、继续、重试、接管或 Tool/WRITE 执行入口。

至少支持记录/验证：

- 用户。
- Conversation ID。
- 时间。
- 模型。
- 状态。
- 标题。
- Tool / WRITE / Stop / Steering / Compaction 标记。
- Message / Tool Call/Result / WRITE 确认 / Checkpoint / Run 状态。

### 只读约束

- 无输入框：已通过前端审计页结构与 H31 页面回归确认。
- 无继续/重试：已通过前端审计页结构与 H31 页面回归确认。
- 无接管他人 Run：已由 `35341391522` attempt 2 验证。
- 无 Tool / WRITE 执行入口：已通过审计页只读实现与后端独立 API 边界确认。
- 后端 API 同样拒绝管理员在他人 Conversation 上创建/继续 Run：`35341391522` attempt 2 已验证普通 Conversation 详情与 Turn 接管均被 owner 校验拒绝。

### 审计访问日志

审计列表与详情均使用 RuoYi `@Log` 记录访问；`35341391522` workflow 已跑到末尾并通过 AI 管理相关 `sys_oper_log` 持久化断言。

### 权限场景验收

- 普通用户自己的 Conversation API 按 owner 校验；`35341391522` attempt 2 已用 ry 的真实 Conversation 验证 admin 通过普通详情接口也不能越过 owner 边界。
- 有 Provider 查看权限但无 `ai:conversation:audit`：`35339406751` 已验证审计 API 返回 403。
- 有审计权限管理员只读查看其他用户 Conversation：`35341391522` attempt 2 已验证 admin 可通过 `/ai/admin/audit/{id}` 读取 ry Conversation。
- 审计管理员不能通过普通 Conversation/Turn API 接管他人 Conversation：`35341391522` attempt 2 已验证普通详情与 Turn 接管均被 owner 校验拒绝。
- 恶意 Prompt 不能绕过 Tool allowlist：`35341391522` 场景 29 已通过，服务端拒绝未注册高权限 Tool。

### 2026-09-18：续跑断点校正、F 能力补强与 I 阶段切入

- 用户再次明确要求：先确认上一轮真实断点，并在每个阶段/小阶段完成后即时更新实施计划；本次首先重新读取 GitHub 当前分支、`11/12/13` 和最新 Actions，而不是沿用旧文档状态。
- 当前后端 HEAD：`d0dcc3989115e413c3d8294dcbc70b60d140fc6c`；相对 master `ahead=227 / behind=0`。
- 当前前端 HEAD：`60b807e1e4b6355b15b6630d842e074f36e0c0d7`；相对 master `ahead=159 / behind=0`。
- 最新 Backend CI `35343032635`、Frontend CI `35343080362`、Fullstack E2E `35343032753` 全部成功。
- 中断前实际已经继续推进 F：补入 live page schema 的动态 options/validation、角色授权用户、字典数据、调度日志、个人中心、代码生成/编辑、缓存列表、公告已读用户、部门排序、菜单排序等能力；后端同时补对应权限/风险策略。
- 当前 `pageCapabilityCatalog` 已列出 24 个主/子页面路由。Druid、表单构建、Swagger 等不适合普通 CRUD 的页面保留为安全导航/受控语义边界，没有为覆盖率开放任意 DOM、坐标或 JavaScript 执行。
- 最新 Fullstack `35343032753` 在真实 MySQL 8.0 + Redis 7.4 + Spring Boot + Vite + Chromium 环境下通过场景 1-31，并执行末尾数据库状态/审计/加密断言。
- 因此真实断点已校正为：**A-H 主体完成并持续回归绿色；I 最终验收正在收口。** 当前硬缺口是“真实 GPT/cache read/真实压缩语义”与“当前 HEAD Public Preview + 人工视觉验收”，以及少量 I 组合边界。
- `12` 已在 commit `029f45feea05811cdac14d4ce531b8eef7a86998` 同步上述事实和 I1/I2 当前完成状态。

### 2026-09-18：I3 组合边界补测、失败定位与重跑

- 前端 commit `bf4b715273038d9bc23fe69e24a72a5b91d0cfb2` 新增两项最终边界：
  - 场景 21：捕获 `/ai/chat/turn` 中的 `pageInstanceId`，F5 后要求新实例 ID 与刷新前不同，并确认旧 pending WRITE/active Run 已清理。
  - 场景 24a：两个 Tab 指向同一 Conversation，先发慢 Run，再由另一 Tab 发新指令，要求旧 Run = `SUPERSEDED/STEERING`、迟到回复不落库、新回复获胜。
- Frontend CI `35344047445` 对该提交 Production build 成功。
- Fullstack `35344116170` 第一次执行在场景 21 超时。日志显示等待的是 `AI_OK`，而该探针位于用户管理页，mock 按现有设计进入完整用户页 Tool Loop；这是**测试探针选择错误**，不是 Page Instance 产品缺陷。
- 前端 commit `e6973f945f8b48528ccc6e469493f08e31b073ca` 将刷新后探针改为 mock 中优先匹配且不触发 Tool 的 `STEER_NEW`；Frontend CI `35344458776` 已成功。
- Backend commit `1bea318d5f1d346612094196cba4420f143cb8f7` 刷新 Fullstack 并保留实际 acceptance revisions；run `35344486182` 正在重跑。
- 本条严格按“失败 → 读日志 → 定位 → 修复测试 → 再验证”记录；最终通过/失败结论待 `35344486182` 完成后回填。

## 19. CI 与真实运行记录

### 当前分支与基线

- 后端 HEAD：`1bea318d5f1d346612094196cba4420f143cb8f7`（随后文档回填 commit 会继续前移），在该点相对 master `ahead=234 / behind=0`。
- 前端 HEAD：`e6973f945f8b48528ccc6e469493f08e31b073ca`，相对 master `ahead=161 / behind=0`。
- 两个开发分支均 `behind=0`；master 没有被第三阶段日常开发直接修改。

### Backend CI

- 最新 run：`35343032635`，成功。
- Job `backend` 中数据库初始化、Maven build、mock Provider、Spring Boot 真启动、Provider/Agent API 验证全部成功。
- 当前后端依赖事实：Java 17、Spring Boot 4.1.0、Spring AI 2.0.1。

### Frontend CI

- 最新 run：`35343080362`，成功。
- Job `frontend` 的 `Install dependencies` 与 `Production build` 均成功。
- 当前前端依赖事实：Vue 3.5.26、Vite 6.4.1、Element Plus 2.13.1。

### Fullstack E2E

- 最新 run：`35343032753`，成功；job `e2e` 全部步骤成功。
- 真实环境：MySQL `8.0` service、Redis `7.4` service、Java 17、Node 20、Spring Boot jar、Vue3/Vite、Playwright Chromium。
- 成功步骤包括：Initialize database → Build backend → Start deterministic OpenAI-compatible mock → Start RuoYi backend → Install frontend and browser → Start Vue3 frontend → Run browser closed-loop acceptance → Verify persistence/audit/Agent state。
- 浏览器场景 1-13 重验第二阶段主链；14-31 覆盖第三阶段现有自动化主场景。
- 末尾 MySQL 断言确认：Stop/DOUBLE_ESC/Steering 状态、迟到响应丢弃、字体/历史/自动恢复偏好、会话策略、页面开关、Prompt 恢复默认、Checkpoint/Prompt version、审计操作日志与 Provider Token 加密均符合预期。

### MySQL / Redis / Spring Boot / Vite

- MySQL：最新 Fullstack 日志显示 `mysql:8.0` 服务健康并实际监听 3306，SQL 初始化和末尾多项业务断言成功。
- Redis：最新 Fullstack 日志显示 `redis:7.4` 服务健康；该 run 实际完成登录、验证码关闭后的认证、AI/业务全栈流程。
- Spring Boot：`Start RuoYi backend` 步骤成功，真实 jar 运行并被后续 API/浏览器请求使用。
- Vite：`Start Vue3 frontend` 步骤成功，浏览器从该实例完成 31 个场景。
- Frontend Production build：独立 Frontend CI `35343080362` 成功。

### 真实 GPT

已新增 `.github/workflows/ai-agent-real-gpt.yml`，只允许通过 GitHub Actions Secret/Variables 注入真实 Provider，不在仓库保存真实 Token。工作流会：

- 经 RuoYi 自身 Provider/Model API 配置并发现指定 `gpt-*` 模型。
- 在长会话连续请求中要求 `ai_run.cache_read_tokens > 0`，作为 R3-05 的真实 cache read 硬证据。
- 临时降低测试上下文预算触发真实模型 Compaction，要求 Checkpoint 保留语义标记且压缩后续跑仍能恢复该标记，随后恢复默认 64K/75%。
- 将非敏感证据写入 artifact；不输出 Token。

执行结果：

- 修正版 run：`35344277094`，workflow 本身成功。
- 该 run 明确输出：`R3_REAL_GPT_BASE_URL`、`R3_REAL_GPT_MODEL`、`R3_REAL_GPT_TOKEN` 当前均未配置。
- 因此真实 GPT 分支按设计跳过，没有发出真实 Provider 请求，也没有 cache read/真实语义证据。
- 结论：R3-05 与 R3-07 的“真实 GPT/真实模型”最终证据当前属于**外部凭据阻塞**；mock 证据不能替代。

## 20. Public Preview 与人工验收

### 当前 Public Preview

- Run：`35347579485` attempt 2，成功。
- URL：`https://refugees-pill-cycles-cleanup.trycloudflare.com`
- 后端 SHA：`6e700dd57c82625d73d2fce8f98d16e1e76946b9`
- 前端 SHA：`c77a00e0b77a167211993bc4d90568c9b177af33`
- attempt 1 的 Quick Tunnel 已成功注册，但随机 trycloudflare 域名在 Runner 的验证窗口内尚未完成 DNS 传播；按“定位 → 重跑”执行后 attempt 2 公网首页与 API 验证成功。这是临时 Quick Tunnel 传播问题，不是 RuoYi 前后端故障。
- attempt 2 的 `Keep preview alive` 正常执行，workflow 约保活 30 分钟。
- artifact 除 URL/SHA/日志外新增 `phase3-manual-acceptance.md`，按 R3-01 ~ R3-18 编排人工验收路线。
- Preview 使用明确标识的 mock Provider，仅用于 UI/受控 Tool/页面交互人工验收；不作为真实 GPT 证据。
- 当前前端已经继续前移到 `c32664cf...`，因此本次预览证明“公网预览机制 + 人工路线”已可用；最终自动验收全绿后还需再刷新一次当前 HEAD 预览，不能把较旧 SHA 冒充最终版本。

### 机器侧公网证据

run `35347579485` attempt 2 已成功完成：

1. MySQL 8.0 / Redis 7.4 启动。
2. 后端 build + Spring Boot 启动。
3. Vue3/Vite 启动。
4. Cloudflare Quick Tunnel 创建。
5. 从公网 URL 请求首页成功。
6. 从公网 URL 请求 `/dev-api/captchaImage` 成功，artifact 中响应为 `{"msg":"操作成功","code":200,"captchaEnabled":false}`。
7. artifact 同步记录 `public-url.txt`、`preview-revisions.txt` 与 `phase3-manual-acceptance.md`。

### 人工验收路线

按 R3-01 ~ R3-18 最终回验时，重点人工确认：

- R3-01/R3-09：字体三档、模型选择器 Chevron/间距/截断、Stop/Steering/压缩状态的视觉一致性。
- R3-03：AI 内双 ESC 与业务页面/弹框焦点不会误停。
- R3-06：GPT-5.6 目标档位显示为“无/低/中/高/极高/最高”，不把 `minimal` 冒充 `none`。
- R3-10/R3-12：用户、角色、菜单、部门、字典、监控、代码生成等页面的受控语义能力与危险动作确认。
- R3-13：从一个业务目标出发跨页面连续操作。
- R3-14~R3-16：刷新、历史、恢复/撤销、重新登录、多 Tab。
- R3-17/R3-18：AI 管理分层与会话审计只读边界。

## 21. 实施偏差与决策变更

任何偏离 `11` / `12` 的实际实现都记录在这里：

| 日期 | 对应需求 | 原方案 | 实际变化 | 原因 | 是否仍满足用户诉求 |
|---|---|---|---|---|---|
| - | - | - | - | - | - |

## 22. 第三阶段最终结论

当前：**I 阶段最终全栈验收进行中。**

最终只有在 R3-01 ~ R3-18 全部能够回指到用户真实场景，并且第二阶段功能无回退后，才允许把本节改为“第三阶段完成”。
