# RuoYi AI 助手 3.5 阶段 P1 收口实施与验收记录

> 文档类型：Phase 3.5 P1 实施与验收记录  
> 日期：2026-09-20  
> 状态：✅ Phase 3.5 P1 已正式关闭  
> 上层约束：`14-AI助手三阶段架构复盘与后续优化建议.md`、`21-AI助手3.5阶段P1收口自主推进落地方案.md`、`22-AI助手3.5阶段P1收口实施计划.md`  
> 最终运行时验收配对：Backend `a815c7f3d9eddb693746e25bfefd477562e15460` + Frontend `17f8c4bd85dec7c97d517196f7581b304bc9dc12`  
> 说明：本记录是在最终运行时代码、workflow、PR 合入与分支清理均完成后追加的 docs-only 状态记录，不改变上述已经通过 paired Fullstack E2E 的运行时语义。

## 1. 最终结论

Phase 3.5 P1 已按照“诉求 → 方案 → 计划 → 实施 → 审核 → 真实验收 → 主线合入 → 分支清理”的完整流水线收口。

本轮最终证明：

1. 当前全部 24 个 Page Capability（原 P0 样板 `/system/user` + 23 个 legacy 页面）均通过正式 Page / Action Contract + Runtime Bindings 注册。
2. `legacyPageCapabilities` 已删除，catalog 由正式 Contract 派生；无 Contract 的 flat-options compatibility 已删除并由 no-legacy invariant 阻止回归。
3. C2 只针对真实热点有限拆分：键盘策略和会话历史/继续/Undo 协调从 `AiAssistant` 抽离，没有复制 AI Store、Run 或 Tool 状态。
4. A1 / A2 / A4 已按 14 / 21 号边界完成：双 ESC / Composer 快捷键统一策略、Runtime Settings 推荐优先/自定义/恢复推荐、“继续上次会话”与自动继续语义。
5. P0 的 Capability Protocol v1、stale runtime、Tool Policy、WRITE Confirm、Run Lifecycle、Compaction、persistence / audit 语义没有退化。
6. 没有实现 P2 或第四阶段能力。
7. Frontend / Backend 均已通过 PR 进入 `chatgpt/ai-agent-assistant`，最终 AI 主线配对重新完成真实 Fullstack E2E。
8. P1 工作分支、Frontend 临时 archive、兼容 smoke 和 cleanup 分支均已完成使命并删除；`master` 未用于本轮 AI 日常开发。

因此，本轮完成标准不是“22 号计划执行完”，而是用户最初提出的 P1 收口诉求已经有代码、测试、真实运行、主线状态和 Git 生命周期证据共同支持。

## 2. 起始基线与 22 号计划

开始时重新检查 GitHub 真实状态：

| 仓库 | AI 主线 | P1 工作分支 | 起始关系 |
|---|---|---|---|
| Backend `ShanySky/RuoYi-Vue` | `7fdb00860ede59425da245c18e0bbf9408c4d1dd` | `ea8053df7220ab269131b94559c1c6b8c4fe4337` | P1 ahead 1，仅包含 21 号方案 |
| Frontend `ShanySky/RuoYi-Vue3` | `cde5d8e9cf4733495f98a72da07fec4bba8493a2` | `cde5d8e9cf4733495f98a72da07fec4bba8493a2` | 完全一致 |

当时确认：

- `pageCapabilityCatalog.js` 仍有 23 条 `legacyPageCapabilities`。
- 正式 Contract 只有 `/system/user` 样板。
- `createAiCrudPageCapabilities` 仍同时支持 Contract 与 flat-options。
- 23 个 legacy 页面已经存在真实 handler / permission / API，P1 不应重写业务能力，而应迁移声明边界。
- Fullstack workflow 当时不会把 P1 Backend 自动配对 P1 Frontend，必须修正 paired checkout。

随后形成 `22-AI助手3.5阶段P1收口实施计划.md`，提交 `6e4c12fd979e67e20276bcd5b25bf0ff47807007`。计划形成后执行了“21 号方案 ↔ 22 号计划”一致性审核；23 页迁移、动态路由、特殊页面、C2、A1/A2/A4、no-legacy、P0 回归、P2 排除、真实 paired SHA、历史整理和清理均得到覆盖，没有改变已确认架构边界，因此直接进入实施。

## 3. Wave 1：System / User Page Capability 迁移

P1 将以下 10 个 legacy 页面迁入正式 Contract + Runtime Bindings：

- `/system/user-auth/role/*`
- `/system/role`
- `/system/role-auth/user/*`
- `/system/menu`
- `/system/dept`
- `/system/post`
- `/system/dict`
- `/system/dict-data/index/*`
- `/system/notice`
- `/user/profile`

保留 P0 已完成的 `/system/user` 样板并作为统一实现基线。

主要落点：

- 新增 `src/ai/pages/systemPageCapabilities.js`。
- 稳定声明进入 Contract：pageId、pageName、canonical route、toolPrefix、query/form 字段、permission、Action suffix/schema。
- 页面内只保留 Runtime Bindings：handler、响应式 options / validation rules、rows、selection、context。
- Profile 继续明确排除 `passwordFields` 和 `avatarBinaryUpload`。

Wave 1 没有更改 Tool 名、业务 API、权限事实或真实副作用 core。

## 4. Wave 2：Monitor / Tool 异构页面迁移

以下 13 个 legacy 页面完成迁移：

- `/monitor/online`
- `/monitor/job`
- `/monitor/job-log/index/*`
- `/monitor/logininfor`
- `/monitor/operlog`
- `/monitor/cache`
- `/monitor/cacheList`
- `/monitor/server`
- `/monitor/druid`
- `/tool/gen`
- `/tool/gen-edit/index/*`
- `/tool/build`
- `/tool/swagger`

主要落点：

- 新增 `src/ai/pages/monitorPageCapabilities.js`。
- 新增 `src/ai/pages/toolPageCapabilities.js`。
- read-only、日志、缓存、代码生成编辑器均继续使用同一 Contract / Adapter 边界，以显式 Action 表达异构能力。
- Druid、Form Builder、Swagger 迁为正式 0-Action Contract，继续保持 navigation-only：
  - 不暴露 iframe DOM。
  - 不开放坐标点击。
  - 不开放任意 JavaScript。
  - 不把 Swagger API 自动转换为可执行 Tool。

没有为特殊页面创建第二套 Adapter 或 Registry。

## 5. 动态路由与 stale runtime

5 个 legacy 动态页面在迁移前后保持原 pageId：

| 页面 | pageId | canonical route |
|---|---|---|
| 用户分配角色 | `system-user-auth-role` | `/system/user-auth/role/*` |
| 角色分配用户 | `system.role.authUser` | `/system/role-auth/user/*` |
| 字典数据 | `system.dict.data` | `/system/dict-data/index/*` |
| 调度日志 | `monitor.job.log` | `/monitor/job-log/index/*` |
| 修改生成配置 | `tool.gen.edit` | `/tool/gen-edit/index/*` |

页面注册继续使用实际 `route.path` 形成 runtime identity；P0 的 full semantic identity、stale runtime 和 Action navigation source-runtime preservation 没有被放宽或重写。

`/user/profile/:activeTab?` 的 tab 仍是页面内部状态，未提升成新的 Page Contract identity。

## 6. Wave 3：C2 与 A1 / A2 / A4

### 6.1 C2 / A1：键盘策略

新增 `src/ai/keyboardPolicy.js`，收敛：

- Enter / Ctrl+Enter / Shift+Enter / IME 发送判断。
- 双 ESC 700ms arm/stop。
- AI focus guard。
- model picker / Element Plus overlay guard。
- Esc 关闭内部 popup 不计第一次 Stop gesture。

`AiAssistant` 继续拥有 `sendMessage`、`stopCurrentRun` 和 UI refs，只把策略通过 callback 交给 keyboard controller，没有建设 global hotkey framework。

### 6.2 C2 / A4：历史与继续会话

新增 `src/components/AiAssistant/useConversationHistory.js`，收敛：

- history query/loading/grouping。
- “继续上次会话”与从历史继续。
- 10 秒 Undo Restore snapshot。
- rename / archive。
- composable 生命周期清理。

Pinia AI Store 仍是 Conversation / Message / Run 状态权威，没有复制 store。

用户侧文案完成：

- “恢复上次会话” → “继续上次会话”。
- “登录后自动恢复上次会话” → “登录后自动继续上次会话”。

Undo Restore 的“撤销恢复”继续保留为动作语义。

`AiAssistant/index.vue` 从 1281 行降至 1116 行；拆出的职责正是高频热点，而不是为了形式机械拆组件。

### 6.3 A2：Runtime Settings 推荐优先

新增 `src/ai/modelRuntimeSettings.js`，并调整 `AiModelRuntimeSettingsDialog`：

- “工作上下文窗口”改为“Agent 工作上下文预算”。
- 主状态提供“自动（推荐）”和“自定义”。
- 推荐值与 Backend canonical defaults 对齐：64K / auto compaction / 75%。
- 自定义模式才展开预算、自动压缩和阈值。
- 提供“恢复推荐设置”。
- reasoning 设置继续独立保留。

没有新增数据库字段、Model Profile、Provider runtime 或新 API。

## 7. Wave 4：no-legacy 全局收口

最终完成：

1. 删除 `legacyPageCapabilities`。
2. `aiPageCapabilityCatalog` 完全由 24 个正式 Contract 派生。
3. `createAiCrudPageCapabilities` 在没有正式 `contract` 时直接拒绝，不再接受 flat-options compatibility。
4. Contract test 建立 no-legacy invariant：
   - 24 个 pageId / route / toolPrefix 唯一。
   - Action suffix 在各 Contract 内唯一。
   - 所有页面使用 `contract + bindings`。
   - 页面不再用字符串 pageId 走 legacy `useAiPageTools` 注册。
   - flat-options 调用必须失败。
5. 三个 navigation-only 页面保持 0 executable Action。

因此 P1 结束后没有“新旧两套长期并存、以后慢慢迁移”的状态。

## 8. 代码与架构复核

实施完成后针对用户最初目标重新做了代码/架构复核，而不是只看 CI。

### 8.1 Contract / Tool / Permission 语义

对迁移前 P0 主线与迁移后的 Contract 做集合差分：

- System/User legacy 页面：86 个“Tool 名 + permission”生成/Action 组合，missing 0、extra 0。
- Monitor legacy 页面：41 个组合，missing 0、extra 0。
- Tool/Codegen legacy 页面：13 个组合，missing 0、extra 0。

23 个迁移页面的 Contract Action 均检查到对应 Runtime Binding，missing 0。

因此迁移没有通过改名或漏绑悄悄改变既有 Tool / permission 事实。

### 8.2 Registry / 写路径

最终 `src/ai` 仍只有原有 `toolRegistry.js` 一套 Registry。

本轮未修改：

- `src/ai/capabilityProtocol.js`
- `src/ai/toolRegistry.js`

Backend P1 也没有修改产品 Runtime、Service、Tool Policy、SQL 或写路径，只修改 canonical 文档和 CI paired-ref 治理。

因此没有出现第二套 Adapter / Registry / 业务写路径。

### 8.3 C2 与状态所有权

keyboard policy 仅拥有 ESC arm timer 等键盘策略状态；conversation history composable 仅拥有 history/popover/Undo UI 协调状态。

Conversation / Message / Run / live run / Tool 状态继续归原 AI Store 和后端生命周期管理，不存在重复状态源。

### 8.4 P2 / 第四阶段检查

未实现：

- B7 TokenEstimator。
- B8 RunExecutionManager / 多实例。
- Streaming。
- Server Tools。
- MCP / Skill 产品能力。
- Multi-Agent。
- RAG。
- Harness。
- 动态 Tool Policy DSL。
- Flyway / Liquibase。
- 第四阶段自治能力。

## 9. 审核发现与修复闭环

实际审核/验收中发现一个需要修复的问题：

### 9.1 A2 UI 改造后的 E2E 断言过时

第一次 P1 paired Fullstack：

- run `35482260583`
- attempt 1
- Backend：`bdc51655cb37202aaf099e8c8a82abe0802c5431`
- Frontend 当时 checkout：`96c33fa9a25d87c416817db05bd6176068379066`
- 结果：failure

失败发生在 Browser E2E 的 Runtime Settings 验证。A2 已将默认 UI 改为“自动（推荐）”，自定义 input 在推荐态隐藏，但旧测试仍直接读取 input，导致 Playwright timeout。

处理：

1. 没有回退 A2 产品设计。
2. 更新 E2E：先验证推荐 summary，再切换“自定义”，验证 64K/75%、保存 72K/70%，最后通过“恢复推荐设置”回到 64K/75%。
3. 增加“继续上次会话”和“登录后自动继续上次会话”UI 断言。
4. 重新运行 Frontend CI 和 paired Fullstack。

后续 attempt 2 与历史整理后的 attempt 3 均成功。

这形成了完整“发现 → 修复 → 针对性验证 → 再审核”闭环。

## 10. 工作分支验证证据

### 10.1 Frontend CI

历史整理前最终实现：

- Frontend HEAD：`7ac39d75754d29bbca200883f43285014f082f54`
- run `35482300417`
- conclusion：success

覆盖：

- `node tests/ai-capability-contract.test.mjs`
- `npm install`
- `npm run build:prod`

### 10.2 Backend CI

- Backend P1 HEAD（运行时验收前）：`9eb030b46c15c43b8683a5182dbfcda52d8afb5f`
- run `35482254616`
- conclusion：success

### 10.3 Paired Fullstack

历史整理前：

- run `35482260583` attempt 2
- Backend：`bdc51655cb37202aaf099e8c8a82abe0802c5431`
- Frontend：`7ac39d75754d29bbca200883f43285014f082f54`
- conclusion：success

历史整理后：

- run `35482260583` attempt 3
- Backend：`bdc51655cb37202aaf099e8c8a82abe0802c5431`
- Frontend：`3eb8322dd67f6ea1ece97dfbf983f0802f4b3d91`
- conclusion：success
- `AI_CAPABILITY_CONTRACT_TEST_OK`
- `AI_AGENT_MODEL_SELECTION_E2E_OK`
- Browser / visual evidence / persistence / audit assertions 全部成功

## 11. Frontend 历史整理

Frontend 实施期形成 41 个过程提交。

按 Git workflow：

1. 重新确认工作分支 HEAD 无未知变化。
2. 创建临时 archive：
   - `archive/ai-agent-phase35-p1-closure-20260920`
   - old HEAD：`7ac39d75754d29bbca200883f43285014f082f54`
3. 将 37 个实际 diff 文件分为 3 组长期语义：
   - `feat(ai): unify phase35 page capability contracts`
   - `refactor(ai): close phase35 assistant interaction hotspots`
   - `test(ai): validate phase35 p1 closure`
4. 整理前 final tree：`f01ceec88f28f51fb67168f892c08aee990d9d47`
5. 整理后 final tree：`f01ceec88f28f51fb67168f892c08aee990d9d47`
6. tree 完全一致后才受控更新工作分支。
7. 整理后的 Frontend HEAD：`3eb8322dd67f6ea1ece97dfbf983f0802f4b3d91`
8. Frontend CI run `35484057963`：success。
9. paired Fullstack attempt 3：success。

Backend 工作分支只有方案、计划、Backend CI trigger、paired-ref 四个长期有意义提交，没有为了形式额外改写历史。

## 12. PR 与安全合入顺序

### 12.1 Frontend 先合入

Frontend PR：

- `ShanySky/RuoYi-Vue3#4`
- merge method：rebase
- Frontend AI 主线落点：`17f8c4bd85dec7c97d517196f7581b304bc9dc12`
- final tree 仍为：`f01ceec88f28f51fb67168f892c08aee990d9d47`
- Frontend mainline CI run `35484323671`：success

### 12.2 新 Frontend + 旧 Backend 兼容 smoke

从当时 Backend AI 主线 `7fdb00860ede59425da245c18e0bbf9408c4d1dd` 创建一次性 workflow-only 分支：

- `chatgpt/phase35-p1-frontend-backcompat-smoke`
- temporary HEAD：`b7a0e6eaf545689f2dc32d38d9d6e8f53b09865f`

兼容 run：

- `35484339546`
- conclusion：success
- Backend 产品基线：旧 AI 主线 `7fdb00860ede59425da245c18e0bbf9408c4d1dd`
- Frontend：`chatgpt/ai-agent-assistant @ 17f8c4bd85dec7c97d517196f7581b304bc9dc12`
- `AI_CAPABILITY_CONTRACT_TEST_OK`
- `AI_AGENT_MODEL_SELECTION_E2E_OK`
- Browser / visual / persistence / audit 成功

因此 Frontend 先合入没有制造“必须同步发布 Backend 才能工作”的硬兼容断点。

### 12.3 Backend 后合入

Backend PR：

- `ShanySky/RuoYi-Vue#6`
- merge method：rebase
- Backend AI 主线运行时落点：`a815c7f3d9eddb693746e25bfefd477562e15460`

Backend PR 没有 Backend 产品 Runtime 改动，只包含 21/22 canonical 文档和 P1 CI / paired checkout 治理。

## 13. AI 主线最终真实验收

最终结论不依赖工作分支曾经成功，而重新针对两个 AI 主线真实 HEAD 验收。

### 13.1 Frontend mainline CI

- run `35484323671`
- Frontend SHA：`17f8c4bd85dec7c97d517196f7581b304bc9dc12`
- conclusion：success

### 13.2 Backend mainline CI

- run `35485276701`
- Backend SHA：`a815c7f3d9eddb693746e25bfefd477562e15460`
- conclusion：success

成功证据包括：

- `AI_SCHEMA_ACCEPTANCE_OK`
- 21 个 focused AI tests，0 failures / 0 errors
- Maven `BUILD SUCCESS`
- `B1_B2_B3_HARDENING_ACCEPTANCE_OK`
- `AI_PHASE35_P0_INVARIANTS_OK`

这证明 P0 关键安全和运行语义没有因 P1 退化。

### 13.3 Paired mainline Fullstack E2E

- run `35485276690`
- conclusion：success
- `BACKEND_REF=chatgpt/ai-agent-assistant`
- `BACKEND_SHA=a815c7f3d9eddb693746e25bfefd477562e15460`
- `FRONTEND_REF=chatgpt/ai-agent-assistant`
- `FRONTEND_SHA=17f8c4bd85dec7c97d517196f7581b304bc9dc12`
- `AI_CAPABILITY_CONTRACT_TEST_OK`
- `AI_AGENT_MODEL_SELECTION_E2E_OK`

真实执行链路包含：

`MySQL 8 → Redis 7.4 → fresh AI schema → Spring Boot → Vue/Vite → /dev-api → Browser → Agent / Tool Loop → Page Capability → Tool Result → WRITE Confirm / Permission → Navigation / stale runtime → persistence / audit`

Browser suite 实际覆盖本轮要求的 UI 回归，包括：

- 双 ESC Stop。
- AI focus guard。
- model picker / Element Plus overlay guard。
- Enter / Ctrl+Enter / Shift+Enter Composer 快捷键。
- Runtime Settings 推荐、自定义、恢复推荐。
- “继续上次会话”。
- “登录后自动继续上次会话”。
- 历史会话继续 + Undo Restore。
- AiAssistant / QuickSettings 相关主流程。
- 代表 System / Monitor / Generator Page Capability。
- 可回滚的用户管理完整业务生命周期。
- WRITE / permission / stale runtime / navigation 等 P0 安全边界。

## 14. 分支清理

由于 Connector 没有 delete_ref 能力，按 `.agents/rules/git-workflow.md` 使用只存在于临时清理分支的 GitHub Actions fallback。

### Backend

cleanup run：

- `35485476756`
- conclusion：success

删除：

- `chatgpt/ai-agent-phase35-p1-closure`
- `chatgpt/phase35-p1-frontend-backcompat-smoke`
- `chatgpt/cleanup-ai-agent-phase35-p1`

清理后重新查询均不存在，Backend AI 主线仍为 `a815c7f3d9eddb693746e25bfefd477562e15460`。

### Frontend

cleanup run：

- `35485478859`
- conclusion：success

删除：

- `chatgpt/ai-agent-phase35-p1-closure`
- `archive/ai-agent-phase35-p1-closure-20260920`
- `chatgpt/cleanup-ai-agent-phase35-p1`

清理后重新查询均不存在，Frontend AI 主线仍为 `17f8c4bd85dec7c97d517196f7581b304bc9dc12`。

archive 仅用于保护历史改写过程；tree equality、整理后 CI、paired E2E、PR 合入和主线验收全部完成后，按用户要求删除，不长期保留。

## 15. 计划偏差与说明

1. 实施期间 P1 工作分支出现了新的同范围提交。
   - 发现后暂停继续写代码，重新检查两个仓库 HEAD、主线关系和 diff。
   - 确认新增内容均属于同一 P1 流水线，而非未知无关改动后，以最新真实代码作为实施输入继续审核与验收，没有覆盖重做。
2. 第一次 paired Fullstack 因 A2 UI 改造后的 E2E 断言过时失败。
   - 已按第 9 节完成修复、复测、再审核。
3. 本轮未运行 Real GPT Provider acceptance。
   - P1 没有修改 Provider、Spring AI transport、reasoning/cache 或后端 Agent Runtime 语义。
   - deterministic Provider + Browser closed-loop 已覆盖本轮 Page Capability、UI 和 P0 回归边界。
4. 本轮未创建公网预览。
   - 自动 Playwright Browser E2E 已覆盖本轮要求的真实 UI / Agent / Tool / WRITE / Navigation 行为，没有额外人工视觉验收需求。
5. Backend 产品代码没有因 P1 改动。
   - P1 的运行时代码主体位于 Frontend；Backend 只需要 canonical 文档、CI trigger 与真实 paired checkout 治理。

这些偏差都没有改变 14 / 21 已确认的架构边界。

## 16. 明确延后事项

P1 关闭后仍明确延后：

- B7 TokenEstimator 精确化。
- B8 RunExecutionManager / 多实例。
- Streaming。
- Server Tools。
- MCP / Skill 产品能力。
- Multi-Agent。
- RAG。
- Harness。
- 动态 Tool Policy DSL。
- Flyway / Liquibase。
- 第四阶段自治能力。

P0 已关闭，不因本轮重新规划或重做。

## 17. 对用户原始完成标准的最终回扣

| 原始完成标准 | 最终证据 | 结论 |
|---|---|---|
| 全部现有 Page Capability 不存在长期新旧双形态 | 24 Contract catalog；flat compatibility 删除；no-legacy invariant | ✅ |
| C2 高频职责有限合理收敛 | keyboard policy + conversation history composable；AiAssistant 1281 → 1116 行 | ✅ |
| A1 / A2 / A4 完成 | UI + deterministic tests + Browser E2E | ✅ |
| P0 安全和运行语义无退化 | Backend mainline CI + P0 invariants + paired Browser E2E | ✅ |
| 没有实现 P2 / 第四阶段 | 代码 diff / 架构审核 / 延后清单 | ✅ |
| 文档、代码、测试、workflow、主线一致 | 00 / 14 状态同步；21 / 22 / 23 链路；主线 CI/E2E | ✅ |
| 工作分支完成使命并清理 | 两仓 cleanup runs + 分支重新查询 | ✅ |
| 诉求 → 方案 → 计划 → 落地 → 验收环环相扣 | 21 → 22 → Waves → 审核修复 → mainline acceptance → 23 | ✅ |

## 18. 阶段结论

Phase 3.5 P1 可以正式关闭。

P1 结束后的 AI 助手底座已经从“P0 有统一机制但现有页面仍长期双形态”收敛为：

- 单一 Page / Action Contract + Runtime Bindings 形态。
- 单一 Registry / Tool 调用路径。
- 明确 dynamic route / stale runtime 边界。
- 保持服务端 Permission / Risk / WRITE Confirm 权威。
- 前端高频交互职责得到有限拆分。
- Runtime Settings 和会话恢复语义更符合普通用户使用方式。
- no-legacy invariant、Frontend CI、Backend P0 invariants 与 paired Fullstack E2E 共同守住回归边界。

这为后续继续推进业务助手、虚拟员工以及更远期能力留下了单一、稳定、可治理、可验证的底座，同时没有提前引入 P2 或第四阶段复杂度。
