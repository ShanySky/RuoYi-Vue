# RuoYi AI 助手三阶段架构复盘与后续优化建议

> 文档类型：三阶段完成后的架构复盘 / 后续讨论基础
> 
> 日期：2026-09-19
> 
> 状态：架构复盘决策已收口。A1～A5、B1～B10、C、D、E、F 均已形成明确结论；B1 / B2 / B3 已完成实施并进入 AI 主线，其余已确认事项按本文优先级进入后续 Phase 3.5 落地方案与实施计划。
>
> 实施进度：2026-09-19，B1 / B2 / B3 已完成真实验收并 fast-forward 合入 `chatgpt/ai-agent-assistant`；历史实施分支 `chatgpt/ai-agent-hardening-b1-b3` 已删除。
> 
> 关联：`../00-全局设计/00-AI助手总体设计.md`

## 1. 复盘结论

经过三个阶段，RuoYi AI 助手的主要架构方向是合理的，不需要推倒重来。

目前最重要的几根主梁已经建立：

- 独立 `ruoyi-ai` 模块。
- Provider / Model。
- Conversation / Message。
- Agent Tool Loop。
- Run / Stop / Steering。
- Checkpoint / Compaction。
- Prompt Cache。
- Page Capability。
- 跨页面连续操作。
- 前端全局 AI Store。
- 会话恢复、历史和偏好。
- AI 管理和只读审计。
- 继续复用 RuoYi 原业务 API、权限和数据范围。

真正的风险不是“前三阶段已经把底座做错”，而是当前几个核心扩展点开始快速膨胀。如果继续直接叠加第四、第五阶段大功能，后续维护成本可能明显加速。

因此项目在继续增加大功能前进入 Phase 3.5 Architecture Hardening；其中首批 B1 / B2 / B3 已完成并进入主线，后续事项继续按本文逐项讨论。


### 1.1 对当前底座的四维判断

为了避免把“功能多”误判成“架构已经做重”，当前三阶段整体应分四个维度判断：

- **架构方向：稳。** Conversation / Run / Tool Call / Page Capability / 权限边界 / Checkpoint / 全局 AI Store 这些主干方向基本正确，没有出现需要推倒重建的根本性错误。
- **模块边界：开始出现预警。** Agent Loop、Tool Policy、AiAssistant、QuickSettings 和业务页面内的 AI Adapter 已经承担越来越多职责；现在还能维护，但继续直接叠大功能会明显增加耦合。
- **数据模型：总体合理，但存在两个明显缺口。** Run / Checkpoint / Conversation / Message / Pending Tool 的分离是对的；Prompt 需要真正的版本历史，Run 生命周期需要更显式的状态转换约束。
- **扩展性：方向已留口，但 Runtime / Protocol 边界仍偏薄。** 现有架构已经可以继续扩展，但在新的 Provider、Runtime、Harness、更多页面能力继续进入之前，最好先把 AgentRuntime、Capability Protocol 等边界正式化。

因此当前最合适的动作不是删除第三阶段已有功能，也不是提前重做，而是做一次有限范围的架构收敛。


## 2. 后续讨论地图与决策状态

> 本节只维护讨论范围、当前状态和最终决策摘要。详细理由保留在本文后续对应章节中。
>
> 状态：`⬜ 未讨论`　`🟡 讨论中`　`✅ 已定`　`⏸ 延后`　`❌ 不做`

### A. 第三阶段哪些功能做重了

| 编号 | 议题 | 状态 | 最终决策摘要 |
|---|---|---|---|
| A1 | 双 ESC 中断：交互细节是否做得过深 | ✅ | 保留双 ESC；现有交互不再继续扩张，但在主要架构加固完成后，以低优先级收敛为统一 AI 快捷键机制，避免后续继续在组件内复制堆叠特殊快捷键逻辑 |
| A2 | 每模型 Context / Compaction 高级设置：是否暴露过多 Runtime 参数 | ✅ | 面向普通 RuoYi 管理员：保留每模型 Runtime 数据结构，但 UI 以推荐/自动为主；“工作上下文窗口”改为“Agent 工作上下文预算”，压缩阈值降级到自定义高级项，增加恢复推荐设置；不重构数据库，不建设复杂 Model Profile 平台 |
| A3 | Page Capability：如何让页面天然 AI-ready，而不是后期逐页适配 | ✅ | 页面/Action 以统一契约天然可发现；标准契约内自动发现，特殊能力显式扩展，不做黑盒猜测；业务事实只定义一次，AI 只补充 AI 专属语义；AI 页面管理定位为能力治理/策略中心，而不是页面登记中心 |
| A4 | 会话历史 / 用户偏好 / 恢复撤销：是否开始向完整聊天产品过度扩张 | ✅ | 现有设计总体合理，不做结构性调整；历史入口显示开关、自动恢复、撤销恢复均保留；仅将用户侧“恢复上次会话”统一改为“继续上次会话”，作为 Phase 3.5 后段/收尾的低优先级文案调整 |
| A5 | AI 管理 / 会话审计：企业级能力是否过重，边界应该停在哪里 | ✅ | 当前设计不过重，现有能力保留；现阶段以系统配置、能力治理、审计和运行诊断为主，但不在架构上封死运营分析、复杂策略、审批、成本管理等未来方向，后续按真实产品需求扩展 |

本组主要回答：**现有功能有没有需要收缩目标、停止继续扩张的。**

### B. 哪些底层反而做轻了

| 编号 | 议题 | 状态 | 最终决策摘要 |
|---|---|---|---|
| B1 | 代理运行时接口（Agent Runtime SPI）：当前直接绑定 Spring AI 是否需要现在解耦 | ✅ | 现在建立一层很薄的代理运行时接口，隔离 Spring AI 的消息、提示词、工具回调和模型响应等实现细节；会话、运行、停止、纠偏、检查点、能力治理和权限仍由 RuoYi AI 框架负责；当前只实现 Spring AI 代理运行时，不建设多运行时平台 |
| B2 | 运行状态机（Run State Machine）：状态已有，但是否需要正式状态机 / 生命周期层（Lifecycle） | ✅ | 建立统一运行生命周期服务，集中定义允许的状态转换和前置条件；所有停止、纠偏、工具返回、压缩、完成和失败统一走该入口；不引入复杂工作流引擎 |
| B3 | 提示词版本历史（Prompt Version History）：只有版本号、没有历史正文是否够用 | ✅ | 保存每个提示词版本的完整正文，但正文按版本只保存一次；会话/运行只保存版本记录引用，不重复保存完整提示词；暂不建设复杂审批、发布和回滚工作流 |
| B4 | 页面能力适配层（Page Capability Adapter）：AI 代码是否应该从业务 `.vue` 中迁出去 | ✅ | 建立统一 Page / Action 契约和单一事实来源机制；先完成底座并用一个代表页面验证，再在 Phase 3.5 后段以集中任务迁移所有现有已接入 Page Capability 的页面，避免长期并存两种实现形态；不等同于强制所有 RuoYi 页面一次达到 Level 3 |
| B5 | Tool Policy 拆分：`AiFrontendToolPolicy` 是否应该拆 Registry / Provider | ✅ | 现在做轻量职责拆分，形成 Registry + Generic CRUD + 少量领域 Provider；继续使用服务端代码规则，不引入数据库动态规则引擎、DSL 或插件平台 |
| B6 | Capability Protocol v1 正式化：现在只是有协议名，契约是否太松 | ✅ | Phase 3.5 正式冻结最小 v1 契约，明确版本、页面实例、能力定义、上下文及调用结果/错误等核心字段；不提前建设复杂协议协商或多版本平台 |
| B7 | TokenEstimator / Checkpoint：上层压缩设计很细，底层 Token 估算是否太粗 | ⏸ | 当前字符启发式估算已支撑真实 Provider 与 Compaction 验收，暂不为精确性单独重构；保留现状，待出现真实误差问题或可靠 tokenizer / Provider 计数能力时再引入正式 TokenEstimator |
| B8 | RunExecutionManager：当前单 JVM Future 管理是否应该先抽象边界 | ⏸ | 当前单 JVM Executor / Future 满足现阶段产品诉求，不提前为多实例或后台长任务建设执行平台；待虚拟员工、后台长任务或多实例成为真实阶段需求时再抽象 |
| B9 | Agent Unit / Integration Test：E2E 很强，但状态机级测试是否不足 | ✅ | Phase 3.5 补少量高价值、确定性的底层测试，覆盖 Run Lifecycle、Tool Result 幂等、Stop/Steering 竞争、Compaction 中断、旧 Page Instance 与 WRITE 确认等核心不变量；不重建整套测试平台 |
| B10 | AI SQL Migration：阶段 SQL 是否到了需要规范 migration 的时候 | ✅ | 现在建立 fresh-install schema + 有序增量 migration 的文件、命名和执行规则；暂不引入 Flyway/Liquibase，后续只有实际维护成本证明需要时再评估 |

本组主要回答：**哪些是真正关系到以后还能不能继续稳定扩展的地基问题。**

### C. 当前代码结构是否已经出现危险信号

| 编号 | 议题 | 状态 | 最终决策摘要 |
|---|---|---|---|
| C1 | 后端 `AiAgentLoopService` God Object 趋势 | ✅ | Phase 3.5 做有限职责收敛：保留 Agent Turn Orchestrator 门面，只拆以后必然继续膨胀且边界稳定的上下文组装、Tool 协调等职责；不为追求架构图完整把每个概念都拆成 Service |
| C2 | 前端 `AiAssistant` / `QuickSettings` God Component 趋势 | ✅ | 已有体量属于预警但尚未阻塞主线；列为 P1，后续结合真实修改热点拆 Component / Composable，不进行一次性全面前端重构，不改变已验证用户体验 |
| C3 | Page Capability 与业务页面耦合增长趋势 | ✅ | 与 B4 统一处理，不再建立另一套独立重构项目；统一契约定型后，先样板验证，再集中迁移全部现有 Page Capability 页面，从源头消除长期双形态 |

本组与 B 组有交叉，但关注点不同：B 组讨论“缺什么抽象”，C 组讨论“现在是否已经需要拆，以及拆到什么程度”，避免仅因文件变大就为了形式重构。

### D. 是否接受建议的长期目标架构

#### D1 前端目标结构

    AiAssistant Shell
    ├─ Composer
    ├─ MessageList
    ├─ RunStatus
    ├─ Confirmation
    ├─ HistoryPanel
    └─ UserSettings
          ↓
       Global AI Store
          ↓
    Page Capability Adapter
          ↓
    Page Capability Registry

状态：✅ 已定。该结构作为长期职责边界，而不是要求 Phase 3.5 机械创建每个方框对应的组件、文件或抽象；实际拆分仍以真实职责和维护压力为准。

#### D2 后端目标结构

    AgentTurnOrchestrator
    ├─ ConversationService
    ├─ RunLifecycleService
    ├─ PromptContextAssembler
    ├─ Context / Checkpoint Service
    ├─ ToolCallCoordinator
    ├─ CapabilityPolicyRegistry
    └─ AgentRuntime
         └─ SpringAiAgentRuntime

状态：✅ 已定。认可这些模块边界作为长期方向，但它是职责地图而不是强制类图；Phase 3.5 只落地当前真实膨胀点，不为了形式完整一次性创建所有 Service。

本组最终结论：**认可前后端目标职责边界作为长期演进方向，但只按真实需求逐步收敛，不把目标架构图本身变成实施清单。**

### E. Phase 3.5 到底做多大

| 编号 | 议题 | 状态 | 最终决策摘要 |
|---|---|---|---|
| E1 | 哪些属于 P0：第四阶段前必须加固 | ✅ | B1/B2/B3 已完成；剩余 P0 聚焦 B4 基座+代表页、B5、B6、C1 有限收敛、B9 核心测试和 B10 migration 规则 |
| E2 | 哪些属于 P1：以后逐步改善 | ✅ | B4 全量现有页面迁移、C2 前端职责拆分，以及 A1/A2/A4 等已确认低优先级收尾项；其中 B4 全量迁移明确属于 Phase 3.5，不长期遗留双形态 |
| E3 | 哪些属于 P2：有真实需求再做 | ✅ | B7 精确 TokenEstimator、B8 RunExecutionManager，以及 Streaming、Server Tools、Harness、多实例执行等未来能力 |
| E4 | Phase 3.5 是否坚持“不增加新的大用户功能” | ✅ | 坚持。Phase 3.5 是架构加固和现有能力收敛阶段；允许为既有能力完成必要迁移、协议和测试，但不借机进入第四阶段功能扩张 |
| E5 | 是否形成首批架构加固实施计划 | ✅ | 已形成 15 号落地方案、16 号实施计划，并由 17 号记录完成实施与真实验收；B1 / B2 / B3 已进入 AI 主线 |

### F. 最终总体架构结论

| 编号 | 最终问题 | 状态 | 最终结论 |
|---|---|---|---|
| F1 | 三阶段之后 AI 助手底座整体是否健康 | ✅ | 整体健康，核心方向正确，不需要推倒重建；Phase 3.5 的任务是收紧少数已开始膨胀的扩展边界 |
| F2 | 哪些问题必须现在处理，否则第四阶段会积累明显技术债 | ✅ | 在已完成 B1/B2/B3 基础上，优先完成 B4 基座、B5 Tool Policy、B6 Protocol、C1 有限拆分、B9 核心测试和 B10 migration 规则；B4 全量页面迁移在后段一次收口 |
| F3 | 哪些地方虽然不完美，但现在动反而属于过度设计 | ✅ | 精确 TokenEstimator、RunExecutionManager/分布式执行、一次性全面前端重构、复杂规则 DSL、Flyway/Liquibase、多 Runtime 平台、Harness 等当前没有真实驱动力的能力继续延后 |

最终输出应形成一份清晰的：**保留什么、收缩什么、重构什么、延后什么** 的整体结论。

### 讨论顺序

默认顺序：

`A → B → C → D → E → F`

每完成一项，更新本节状态和最终决策摘要，避免深入某个局部问题后失去全局视角。

## 3. 当前代码出现的结构信号

2026-09-19 检查当前开发分支时，几个关键文件已经比较集中：

- `AiAgentLoopService`：约 679 行，约 30 KB。
- `AiFrontendToolPolicy`：约 400 行，约 21 KB。
- `AiConfigService`：约 437 行。
- `AiAssistant/index.vue`：约 1247 行，约 43 KB。
- `QuickSettings.vue`：约 612 行，约 24 KB。

文件大小本身不是问题，但这些文件恰好位于未来扩展最频繁的位置。

同时，用户、角色、部门、菜单、定时任务等页面已经把较大段 Page Capability 定义直接放入原业务 `.vue` 文件。

这说明：

- 总体方向是连续的。
- 现有抽象已经开始承担真实业务。
- 现在正好到了做职责收敛的时点。

## 4. 第三阶段功能重量判断

| 需求 | 判断 | 后续建议 |
|---|---|---|
| R3-01 聊天字体 | 略偏产品细节 | 已实现保留，不继续增加复杂显示策略 |
| R3-02 Stop | 核心且恰到好处 | 保留，加强 Run 状态机 |
| R3-03 双 ESC | 略重 | 已实现保留，作为高级快捷方式即可 |
| R3-04 Steering | 核心且恰到好处 | 保留，加强竞争条件和幂等测试 |
| R3-05 Prompt Cache | 合理 | 保持 Runtime 内部能力 |
| R3-06 Reasoning | 合理 | 继续按模型实际 capability 管理 |
| R3-07 Compaction | 核心 | 保留，改进 TokenEstimator |
| R3-08 每模型 Context 高级设置 | 略重 | 保留高级入口，不继续扩普通 UI |
| R3-09 UI 精修 | 正常产品工作 | 与 RuoYi 视觉保持一致即可 |
| R3-10 完整语义页面能力 | 核心方向正确，但覆盖目标偏重 | 框架优先，页面分级接入 |
| R3-11 Prompt 管理 | 合理但底层偏轻 | 补真实 Prompt Version History |
| R3-12 所有业务页面全面接入 | 阶段目标偏重 | 改为按价值和 Level 1/2/3 渐进覆盖 |
| R3-13 跨页 Agent | 核心且值得保留 | 正式化 Capability Protocol |
| R3-14 应用级状态持久化 | 核心，设计很好 | 明确状态 ownership |
| R3-15 历史会话 | 核心部分合理 | 不继续扩成复杂聊天知识管理产品 |
| R3-16 恢复 / 偏好 / 撤销 | 部分偏重 | 已有功能保留，高级体验按真实需求增加 |
| R3-17 AI 管理 | 长期合理 | 不演化成重型 AI 运营平台 |
| R3-18 会话审计 | 企业场景合理 | 保持只读、独立权限 |

## 5. 目前偏重的部分

### 5.1 R3-03 双 ESC 的交互细节

双 ESC 的价值是给熟练用户一个快速 Stop 手段。

目前已经考虑：

- AI 焦点范围。
- 短时间双击窗口。
- Element Plus 内部 Overlay。
- 第一次 ESC 关闭控件时不计入中断序列。

这已经足够。

后续不要继续发展更多复杂手势。Stop 按钮才是核心，快捷键只是辅助。

同时，这里仍然需要做一次**低优先级的结构收敛**：当前双 ESC 不能长期作为一个孤立的特殊键盘逻辑留在大型 AI Assistant 组件中，否则后续开发者或 AI 很容易沿现有模式继续复制 `keydown`、焦点判断、时间窗口和状态变量，最终形成越来越多彼此独立的快捷键状态机。

建议在主要 P0 架构加固完成后的收尾阶段，建立一个足够薄的统一 AI 快捷键入口，例如 `useAiKeyboardShortcuts` 或等价模块：

- 快捷键定义集中在一个位置。
- 每个快捷键明确自己的生效上下文 / guard。
- 最终行为调用现有业务动作，例如双 ESC 只调用统一 `stopCurrentRun()`。
- 不建设全局通用 Keyboard Framework，也不提前设计复杂 DSL、优先级系统或可配置快捷键平台。
- 后续新增 AI 快捷键优先接入该机制，不再在 `AiAssistant` 内复制一套新的事件监听与状态逻辑。

该项应放在 Phase 3.5 的后段 / 收尾位置。即使因为时间或其他更重要问题暂时搁置，也不会影响 Agent 主链、权限、安全或后续大方向。

### 5.2 R3-08 每模型上下文高级设置

本项已完成讨论，最终决定是：**底层保留，配置界面轻量收敛；高级设置面向普通 RuoYi 管理员，而不是默认假设用户理解 Agent Runtime 内部机制。**

底层继续保留每模型：

- Context Budget。
- Auto Compaction。
- Threshold。
- Reasoning。

这些参数挂在模型级别是合理的，不改成单一全局值，也不因为降低 UI 暴露度而删除数据模型。

产品层调整原则：

- 默认 Reasoning 继续作为明确的模型配置保留。
- “工作上下文窗口”改名为更准确的“Agent 工作上下文预算”，避免被误解为 Provider 模型物理最大上下文。
- 默认使用系统推荐值，例如当前 64K / 自动压缩开启 / 75%。
- 上下文管理优先展示“自动（推荐）”；只有用户主动选择自定义时，才展开自动压缩开关和压缩阈值等 Runtime 参数。
- 增加“恢复推荐设置”，让管理员可以从不合理的手工组合安全回到系统推荐值。
- 不继续扩张普通模型管理表主界面的技术参数。
- 不为此重构数据库，不强行引入 NULL/override 三态模型。
- 不建设复杂 Model Profile、Provider Capability Database 或自动识别平台；以后只有 Provider 能可靠提供元数据或出现真实需求时再评估。

优先级暂定为 **P1，中低优先级**：它会影响管理员是否容易把 Runtime 配坏，因此值得处理；但它不属于第四阶段前必须先解决的核心地基问题。

### A3 核心诉求与不可偏离的设计目标

A3 后续所有方案、计划和实现必须先围绕以下核心诉求展开；这些内容是判断设计是否正确的第一标准，不能被具体技术方案反向替代。

**核心诉求：**

1. **页面天然 AI-ready，而不是后期 AI 改造。**  
   标准 RuoYi 页面在正常开发或由代码生成器生成完成后，应天然具备 AI 可以发现、理解和调用的基础结构与能力，不应再要求开发人员另外维护一套 AI 专用页面清单、字段清单或 Tool 清单。

2. **单一事实来源，不维护平行定义。**  
   页面、字段、按钮、权限、Action、接口等信息应尽可能来源于业务系统本来就存在的定义。新增一个标准字段、按钮或功能时，不应要求开发人员再“记得去 AI 模块登记一次”。

3. **自动发现应基于明确契约，而不是黑盒猜测。**  
   目标是通过 RuoYi 已有菜单/权限、标准页面结构、统一组件/Action 契约、代码生成模板和运行时注册机制，自动派生 AI 能力；不是通过扫描 DOM、猜按钮语义或反射任意 Vue 实现来碰运气。

4. **代码生成器应成为 AI-ready 页面生产入口。**  
   未来由 RuoYi 代码生成器生成的标准 CRUD 页面，应该同时具备 AI 所需的标准扩展点/契约。标准页面的 AI 基础接入成本应尽量接近零，只有特殊业务动作需要少量显式扩展。

5. **AI 页面管理负责“策略”，不负责重新登记系统事实。**  
   系统真实存在的页面、路由、按钮和权限应自动发现；AI 页面能力管理只保存和展示 AI 特有策略，例如是否允许 AI 接入、风险控制或以后确有需要的 AI 专属配置。不能把 `ai_page_config` 或前端 Catalog 变成另一份系统页面目录。

6. **特殊能力可以扩展，但不能破坏标准机制。**  
   角色数据权限、立即执行任务、代码生成等特殊动作允许显式扩展，但扩展机制应建立在统一底座之上，不能让每个特殊页面重新发明一套 AI 接入方式。

**目标体验：**

> 开发人员正常开发 RuoYi 页面；新增标准字段、按钮、权限或 Action 后，AI 基座能够基于统一机制自然发现并获得对应基础语义能力。开发人员不需要维护第二份 AI 清单。只有真正特殊、无法由标准契约表达的业务能力，才需要额外补充少量语义信息。

**防偏航规则：**

后续每一轮设计都必须回答：

- 这项设计是否减少了页面接入 AI 时的额外开发工作？
- 是否引入了新的重复事实来源或人工登记清单？
- 新增标准页面/按钮后，是否仍需要“记得再去 AI 那边配置一次”？
- 代码生成器生成的新页面是否天然具备基础 AI 接入能力？
- AI 页面管理是否仍只是策略层，而没有重新变成页面目录的第二份副本？
- 特殊页面扩展是否仍复用统一底座？

如果答案开始偏离上述目标，应优先调整方案，而不是继续沿已有技术设计向下推演。

### A3 最终收口结论

A3 已完成讨论，以下三条作为后续设计、计划和实现的长期约束：

1. **自动发现边界**  
   标准 RuoYi 页面、字段、按钮、权限和 Action 只要遵循统一页面/Action 契约，就应由系统自动发现并派生基础 AI 能力。特殊业务能力允许少量显式扩展。禁止为了追求“零配置”而走 DOM 扫描、函数猜测、任意反射等黑盒识别路线。

2. **单一事实来源**  
   页面、路由、字段、按钮、权限、Action 等业务事实不得为了 AI 再人工维护第二份定义。AI 可以补充 Risk、AI Description、确认要求等 AI 独有语义，但不能复制业务系统已经存在的事实。单一事实来源不等于所有信息必须物理写在一个对象里，而是同一个事实只能有一个权威来源。

3. **AI 页面管理定位**  
   AI 页面管理不是“页面/按钮登记中心”，而是“已发现能力的 AI 治理/策略中心”。系统真实存在的页面、按钮和权限来自 RuoYi 本身及统一契约；AI 管理只负责是否允许 AI 使用、风险/确认等 AI 专属策略，以及展示当前发现与接入状态。

**A3 北极星：**

> RuoYi 页面和业务 Action 应基于统一契约天然可被系统发现；业务事实只定义一次，AI 复用这些事实并只补充 AI 专属语义；AI 页面管理只负责治理已发现能力，而不承担重新登记页面和功能。

**代码生成器要求：**

RuoYi 代码生成器未来生成的标准页面，应天然符合上述契约，使“正常生成页面”与“获得基础 AI 接入能力”尽可能成为同一件事，而不是两个独立开发步骤。

### 5.3 R3-10 / R3-12 页面全面 AI 化

Page Capability 是正确方向，但“所有页面所有按钮一次全部接入”会形成持续高维护成本。

建议未来将页面能力分级：

1. Level 1 可理解：页面字段、状态、结果。
2. Level 2 可交互：查询、筛选、打开、填写。
3. Level 3 可执行：保存、删除、状态变更和高影响动作。

框架应该允许渐进接入，而不是把 100% 页面覆盖作为每个阶段的前置条件。

### 5.4 R3-15 / R3-16 会话连续性与用户偏好

本项已完成讨论，最终决定是：**现有设计总体合理，不做结构性调整，也不因为架构复盘而重做已经能够解决真实用户诉求的功能。**

保留现状：

- 历史会话及历史入口显示开关。
- 继续旧会话 / 会话切换。
- 新建会话。
- F5 / 刷新后的会话恢复。
- 账号级会话历史。
- 登录后自动恢复上次会话的可选偏好。
- 恢复后的撤销机制。
- 用户默认模型、Reasoning、字体、发送快捷键、助手打开方式等现有偏好。

唯一确认的轻微调整：

- 用户侧“恢复上次会话”统一改成更自然的“继续上次会话”。
- 对应设置文案可统一成“登录后自动继续上次会话”。
- 只调整用户心智和显示文案，不改变现有恢复、撤销和状态语义。

优先级：**低优先级 / Phase 3.5 后段或收尾项**。即使后续因更重要事项暂时搁置，也不影响 Agent 主链和整体架构。

后续冻结范围：没有真实用户需求时，不继续扩展收藏、Pin、文件夹、会话分支、复杂工作区组织等完整聊天产品能力。

### 5.5 R3-17 / R3-18 企业管理能力

本项已完成讨论，最终决定是：**当前 AI 管理和只读会话审计没有做重，现有能力保留。**

现阶段重点继续放在：

- 系统级 AI 配置。
- 页面/能力治理。
- 会话审计。
- 必要的运行诊断。

但这些是**当前阶段的重点，不是永久边界**。本项目的定位是在把 RuoYi 演进为 AI 时代的软件开发框架，因此不能因为当前暂时不需要，就在架构上提前封死运营分析、复杂策略、审批、成本管理、AI 运维等未来能力。

原则是：

> 当前不主动建设，不等于未来禁止建设；是否扩展由后续真实产品需求决定。

其中“页面能力”管理继续服从 A3 已确认的原则：系统能力自动发现，AI 管理负责治理和策略，而不是维护第二份页面登记清单。

## 6. 目前偏轻或需要补强的底层

### 6.1 代理运行时（Agent Runtime）抽象没有真正落地

**B1 最终决定：**

现在建立一层很薄的代理运行时接口（Agent Runtime SPI），目的只是隔离具体模型框架和模型调用方式，不把业务代理逻辑一起下沉进去。

职责边界：

- 代理编排层负责会话（Conversation）、运行（Run）、停止（Stop）、纠偏（Steering）、检查点（Checkpoint）、工具调用流程和业务能力调度。
- 代理运行时负责向模型发送请求、接收响应，并统一返回文本、工具调用、用量等标准结果。
- Spring AI 代理运行时负责封装 Spring AI 自己的消息、提示词（Prompt）、工具回调（ToolCallback）、模型响应（ChatResponse）和 OpenAI 聊天模型（OpenAiChatModel）等实现细节。
- 页面、后端接口、数据库、Harness 等“AI 能做什么”的能力体系，以及权限、风险和治理规则，不属于代理运行时职责。

当前只需要一个 Spring AI 代理运行时实现，不建设多运行时管理后台、动态切换平台或复杂插件体系。

优先级：**P0，属于进入下一大阶段前应完成的底座加固。**


第一阶段设计已经提出 `AgentRuntime` 边界。

但当前实际模型调用仍由 `AiAgentModelFactory` 直接创建 Spring AI `OpenAiChatModel`，Agent 编排对 Spring AI 细节仍有直接依赖。

当前没有功能问题。

长期风险是未来出现：

- 新 Provider 协议。
- Responses API / 不同 Tool Protocol。
- SDK 内嵌 Harness。
- 不同 Streaming / Runtime 行为。

时，`AiAgentLoopService` 会继续被具体 Runtime 细节污染。

建议补一个很薄的 SPI：

    AgentRuntime
      execute(AgentRuntimeRequest)

当前只有：

    SpringAiAgentRuntime

不要现在造复杂 Harness 平台。

### 6.2 运行（Run）状态存在，但状态机不够显式

**B2 最终决定：**

建立统一的运行生命周期服务（Run Lifecycle Service），由它集中定义运行状态和合法转换关系。

目标：

- 停止、纠偏、等待工具、工具结果返回、上下文压缩、完成和失败都通过统一生命周期入口改变状态。
- 明确每个状态允许进入哪些后续状态，以及转换需要满足的前置条件。
- 避免状态转换继续分散在多个服务和数据库更新语句中。
- 通过单元/集成测试验证非法转换、重复结果和竞争条件。
- 不引入通用工作流引擎，也不为了状态机建立复杂配置系统。

优先级：**P0。**


现在 Run 状态和转换散在：

- `AiRunService`。
- `AiRunMapper`。
- `AiMessageService`。
- `AiAgentLoopService`。

同时仍大量使用字符串状态。

当前可工作，但未来如果增加：

- WAITING_APPROVAL。
- WAITING_EXTERNAL。
- PAUSED。
- RETRYING。

状态转换会越来越难验证。

建议：

- `RunStatus enum`。
- `RunLifecycleService` / `RunStateMachine`。
- 明确允许的 transition。
- 将幂等和状态前置条件集中测试。

### 6.3 提示词（Prompt）有版本号，但没有真正版本历史

**B3 最终决定：**

建立真正的提示词版本历史，但不按会话或运行重复保存完整提示词。

存储原则：

- 每次提示词正文发生变化时，生成新的版本记录并保存该版本的完整正文。
- 同一个提示词版本无论被多少会话或运行使用，正文都只保存一次。
- 会话 / 运行只保存对应提示词版本记录的引用。
- 审计时通过版本引用还原当时真实使用的提示词正文。
- 对动态变量不默认保存每次完整渲染结果；仅在确有审计需要时保存必要参数、上下文摘要或校验信息。

当前不建设复杂的提示词审批、发布环境、版本工作流或完整提示词运营平台。

优先级：**P0。**


当前 `ai_prompt` 更新内容时只递增 `version_no`。

Run 会记录：

- system prompt version。
- compaction prompt version。

但 Prompt 更新后旧正文会被覆盖。

因此未来看到“Run 使用 v3”时，无法还原 v3 具体内容。

建议：

- `ai_prompt` 保留当前激活版本。
- 增加 `ai_prompt_version` 保存每个历史版本正文。

也可以考虑 snapshot/hash，但独立历史版本表更清晰。

### 6.4 页面能力（Page Capability）与业务 Vue 页面耦合开始变重

**B4 最终决定：**

A3 已确认的长期原则保持不变：**页面天然 AI-ready、业务事实只定义一次、AI 自动复用、特殊能力显式扩展。**

B4 进一步确定 Phase 3.5 的实际落地方式：

1. **先建设统一 Page / Action 契约和最小适配底座。**
   - 菜单、路由、页面、权限优先复用 RuoYi 已有事实。
   - 标准 CRUD 字段、查询条件、表单属性、字典和校验尽可能复用业务页面 / 代码生成器已有元数据。
   - 标准业务动作使用统一 Action 契约，页面按钮和 AI Capability 复用同一业务动作事实；AI 只补充 Risk、AI Description、确认要求等 AI 专属语义。
   - 不扫描 DOM、不猜函数语义、不通过任意反射制造“自动发现”。

2. **先选择一个代表性页面完整改造并真实验证。**
   - 这个页面承担“参考实现”的作用，用来验证契约、字段复用、Action、权限、风险、Context、Tool Result 与现有页面 handler / API 的协作是否正确。
   - 在代表页验证前，不启动大规模机械迁移，避免把未成熟模式复制到全部页面。

3. **底座与代表页验证完成后，Phase 3.5 后段集中迁移全部现有已接入 Page Capability 的页面。**
   - 该任务优先级定位为中低，但属于本次 Phase 3.5 明确收尾任务，不无限期拖到未来阶段。
   - 目标是完成后主线只保留统一实现形态，避免“新页面一种写法、旧页面另一种写法”长期共存，减少后续开发人员和 AI 判断实现方式时的干扰。
   - 全量迁移可以作为一个独立的大任务统一执行和验收，不需要与最核心 P0 加固同时展开。

4. **“全部迁移”不等于“强制所有 RuoYi 页面一次达到完整 Level 3”。**
   - 本次全量迁移指**当前已经接入 Page Capability 的页面全部迁移到统一机制**。
   - 尚未接入 AI 的页面仍按业务价值和 Level 1 / 2 / 3 策略逐步覆盖。
   - 代码生成器未来生成的标准页面应天然符合统一契约，使后续新增标准页面不再重新产生旧式 AI Adapter。

因此 B4 不采用“只做一个样板然后长期逐页自然迁移”的方式，也不采用“一开始就同时重写全部页面”的方式，而采用：

    统一机制与基座
        ↓
    代表页面完整验证
        ↓
    继续完成其他高优先级 Phase 3.5 加固
        ↓
    集中迁移全部现有 Page Capability 页面
        ↓
    主线只保留统一形态

这既控制实施顺序，又避免把两套页面能力形态长期留给后续阶段。

### 6.5 服务端工具策略（Tool Policy）单体开始膨胀

**B5 最终决定：** Phase 3.5 进行轻量拆分，形成 `ToolPolicyRegistry` + Generic CRUD Provider + 少量领域 Provider。仍然使用明确、可审计的服务端代码规则，不建设数据库动态规则引擎、可视化 DSL 或插件式策略平台。

`AiFrontendToolPolicy` 当前同时负责：

- 特殊 Tool allowlist。
- Permission。
- Risk。
- JSON Schema。
- Generic CRUD 名称推导。

建议保持“代码规则”而不是做数据库动态规则引擎，但拆成：

    ToolPolicyRegistry
    ├─ GenericCrudPolicyProvider
    ├─ SystemCapabilityPolicyProvider
    ├─ MonitorCapabilityPolicyProvider
    └─ GeneratorCapabilityPolicyProvider

这样仍然明确、可控、可审计。

### 6.6 能力协议（Capability Protocol）还不够正式

**B6 最终决定：** Phase 3.5 正式化并冻结最小 `ruoyi-semantic-page-v1` 契约，明确核心必需字段、调用结果和错误边界；当前只解决契约清晰、可测试、可演进，不建设复杂版本协商、多协议路由或兼容平台。

前端已经标记：

`ruoyi-semantic-page-v1`

但目前它主要还是一个标识。

建议明确 v1 契约：

- protocolVersion。
- pageId。
- route。
- pageInstanceId。
- pageVersion。
- capability definitions。
- pageContext。
- invocation result / error。

以后 v2 才有可控升级路径。

### 6.7 上下文（Context）上层定义很细，令牌估算器（TokenEstimator）偏粗

**B7 最终决定：延后。** 当前字符启发式估算已经支撑现有 Compaction 和真实 Provider 验收，没有足够产品证据证明必须现在提高精度。Phase 3.5 保留当前实现；以后出现真实误差问题，或 Provider / tokenizer 能可靠提供计数能力时，再引入正式 `TokenEstimator`，启发式估算继续作为 fallback。

Checkpoint Prompt 已经要求保留很多结构化信息。

但当前 Token 估算主要仍是字符数启发式。

建议：

    TokenEstimator
    ├─ Provider / tokenizer estimator
    └─ Heuristic fallback

同时 Checkpoint 固定结构可以逐步收敛到更稳定的核心栏目：

- 目标与当前状态。
- 用户最新决策。
- 已完成事实和副作用。
- Pending 状态。
- 下一步。
- 关键引用。

### 6.8 运行执行取消目前主要是单 JVM 实现

**B8 最终决定：延后。** 当前产品仍是单实例 RuoYi 内的交互式 Agent，现有 Executor / Future 足够。现在提前抽完整 `RunExecutionManager` 对产品价值有限；等虚拟员工后台长任务、多实例部署或跨进程执行成为真实阶段需求时，再基于当时运行模型设计，不提前为假设场景建设执行平台。

当前 in-flight Future 保存在当前 JVM 内存。

对目前单实例 RuoYi 是合理的，不应该现在引入 MQ / 分布式执行平台。

但建议先抽：

`RunExecutionManager`

当前实现仍然使用本地 Executor / Future。

以后真的出现多实例部署需求时，再替换实现。

### 6.9 测试结构需要补第一层

**B9 最终决定：** Phase 3.5 补一层小而关键的 Unit / Integration Test，不重建完整测试平台。重点保护以后最容易因 Agent 演进而被破坏的运行不变量，包括 Run Lifecycle、重复 Tool Result、Stop / Steering 竞争、Compaction 中断、旧 Page Instance / Page Version、clientRunKey 重试和 WRITE 确认前后 Supersede。

当前 Fullstack E2E 和 Real GPT 验收非常强。

但 Run 状态越复杂，越需要快速、确定性的 Unit / Integration Test 覆盖：

- WAITING_TOOL + Stop。
- WAITING_TOOL + Steering。
- 重复 Tool Result。
- 旧 PageVersion。
- clientRunKey 重试。
- Compaction 中 Stop。
- Compaction 中 Steering。
- WRITE confirm 前后 Supersede。

长期建议形成：

    Unit / Integration
      ↓
    Agent state correctness

    Fullstack E2E
      ↓
    Real system closure

    Real Provider
      ↓
    Provider-specific behavior

### 6.10 数据库迁移（SQL Migration）需要开始规范

**B10 最终决定：** 现在开始规范 AI SQL，但先解决规则而不是引入迁移框架。保持一份可直接初始化当前完整 AI 数据结构的 fresh-install schema，同时新增有序、可判断执行顺序的增量 migration；统一命名、依赖和使用说明。当前不引入 Flyway / Liquibase，后续只有真实维护成本证明需要时再评估。

当前已有：

- AI 基础 SQL。
- model selection。
- Phase 2。
- Phase 3。

继续 Phase 4 / 5 后，如果仍以阶段临时脚本增长，会越来越难判断新环境和升级环境该执行哪些文件。

建议整理为：

- AI fresh install schema。
- 有序 migrations。

不一定现在引入 Flyway/Liquibase；先建立清晰规则即可。

## 7. 当前最大代码级预警：God Object 趋势

**C 组最终决定：**

- **C1：有限拆后端，不做形式化大重构。** `AiAgentLoopService` 继续作为 Agent Turn Orchestrator 门面；Phase 3.5 只拆以后必然继续增长、且边界已经稳定的职责，例如 Prompt / Runtime Context 组装、Tool Call 协调等。B1 Runtime、B2 Lifecycle 等已经形成的独立边界继续复用，不为目标架构图中的每个方框机械创建 Service。
- **C2：前端拆分列为 P1。** `AiAssistant` / `QuickSettings` 已经出现体量预警，但目前功能稳定且没有阻塞下一阶段。后续结合实际修改热点抽 Component / Composable，避免一次性全面重写，也不改变已验证交互。
- **C3：并入 B4。** 页面 AI 适配耦合不单独再开一套重构路线；统一契约、代表页验证和后续全量迁移就是解决该问题的正式路径。

### 后端 `AiAgentLoopService`

目前同时承担或协调：

- Conversation。
- Model selection。
- Run。
- Prompt。
- Context。
- Compaction。
- Tool policy。
- Model invocation。
- Tool Call。
- Tool Result。
- Page Runtime。
- Persistence。

建议未来保留一个薄 Orchestrator 门面，把稳定职责拆给明确 Service。

### 前端 `AiAssistant/index.vue`

目前同时负责：

- 浮窗 / Dock。
- 聊天消息。
- Composer。
- History。
- Restore。
- Model。
- Reasoning。
- Run。
- Stop。
- Steering。
- WRITE Confirm。
- ESC。
- Tool。
- Resize。

后续应按职责拆 Component / Composable，但不改变当前用户体验。

## 8. 长期目标结构与使用方式

**D 组最终决定：** 接受下面的前后端结构作为长期**职责边界地图**，但不把它当成 Phase 3.5 必须逐项实现的类图或目录结构。

判断原则：

- 某项职责已经真实膨胀、形成稳定边界时，再拆到独立模块。
- 如果现有实现仍清晰、稳定，就允许多个职责暂时共处，不为架构对称性制造抽象。
- 目标结构用来防止未来职责重新混回 God Object，而不是要求现在一次性达到“理想形态”。

### 前端

    AiAssistant Shell
    ├─ Composer
    ├─ MessageList
    ├─ RunStatus
    ├─ Confirmation
    ├─ HistoryPanel
    └─ UserSettings
          ↓
       Global AI Store
          ↓
    Page Capability Adapter
          ↓
    Page Capability Registry

### 后端

    AgentTurnOrchestrator
      ├─ ConversationService
      ├─ RunLifecycleService
      ├─ PromptContextAssembler
      ├─ Context / Checkpoint Service
      ├─ ToolCallCoordinator
      ├─ CapabilityPolicyRegistry
      └─ AgentRuntime
           └─ SpringAiAgentRuntime

这不是第二套架构，而是把当前已经存在的职责重新放到更稳定的边界中。

## 9. Phase 3.5 最终范围与优先级

Phase 3.5 坚持一个原则：**这是架构加固和既有能力收敛阶段，不是第四阶段功能开发。**

### P0：进入下一大阶段前完成

已完成：

1. B1 AgentRuntime SPI。
2. B2 Run Lifecycle / State Machine。
3. B3 Prompt Version History。

后续 P0：

4. B4 建立统一 Page / Action 契约与适配底座，并完成一个代表页面完整迁移和真实验证。
5. B5 Tool Policy 轻量拆分。
6. B6 Capability Protocol v1 正式化。
7. C1 对 `AiAgentLoopService` 做有限职责收敛，保留薄 Orchestrator。
8. B9 补 Agent 核心 Unit / Integration Test。
9. B10 建立 AI fresh-install schema + 有序 migration 规则。

### P1：Phase 3.5 后段 / 收尾

- B4 / C3：在统一机制和代表页验证稳定后，集中迁移**全部现有已接入 Page Capability 的页面**，主线不长期保留新旧两种 AI 页面实现形态。
- C2：按真实修改热点拆 `AiAssistant` / `QuickSettings` 的 Component / Composable。
- A1：统一 AI 快捷键入口。
- A2：模型高级设置 UI 轻量收敛。
- A4：将“恢复上次会话”等用户文案统一为“继续上次会话”。

其中 B4 全量页面迁移虽然优先级属于中低，但属于 Phase 3.5 明确的收尾任务，而不是“以后有空再迁”。

### P2：有真实产品需求再做

- B7 更精确的 TokenEstimator。
- B8 RunExecutionManager / 多实例执行。
- SSE / Streaming Event。
- Server Tools。
- SDK 内嵌式 Harness。
- 多实例 Agent execution。
- 更复杂页面操作策略。
- 复杂 Prompt / Tool Policy 运营平台。

## 10. 当前不建议做的事情

Phase 3.5 不应该变成“第四阶段功能开发”。

暂时不建议：

- 在没有业务价值分级的情况下强制所有未来 RuoYi 页面一次达到完整 Level 3。这里不包括 B4 已确认的“把所有现有已接入 Page Capability 的页面统一迁移到新机制”；后者属于消除双形态的架构收尾。
- 更多历史会话高级功能。
- 更多 UI 个性化。
- Tool Policy 可视化 DSL。
- Prompt 审批发布平台。
- 多 Agent。
- RAG。
- 分布式任务系统。
- 独立 Harness 部署。

## 11. 后续讨论方式

### 11.1 架构讨论分支策略

为了兼顾“每确认一个关键点就及时保存”和“主开发分支提交历史保持清晰”这两个目标，后续架构讨论不再直接长期堆积在 `chatgpt/ai-agent-assistant` 主开发分支上。

正式采用：

    chatgpt/ai-agent-assistant
        ↓
    chatgpt/ai-agent-architecture-discussion

作为后续 B4～F 等架构讨论的工作分支。

讨论分支继续保持当前已经验证有效的节奏：

    确认一个关键决策
        ↓
    立即更新讨论文档
        ↓
    立即提交

这样做的目的不是追求“每个提交都适合长期保留”，而是：

- 防止长时间讨论或会话中断导致结论丢失。
- 让后续新会话可以直接通过 Git 恢复已经确认的讨论状态。
- 保留关键设计决策形成过程的时间线。

当完成一个自然里程碑，例如整个 B 组、C 组或一个完整架构主题后，再统一整理讨论分支历史：

- 使用交互式变基（interactive rebase）或等价方式整理提交。
- 将细粒度提交按实际语义进行合并（squash / fixup）和重命名。
- 最终保留少量能够表达完整里程碑的有意义提交。
- 再将成熟结果合入 `chatgpt/ai-agent-assistant`。

在里程碑完成前，不频繁重写已经推送到 GitHub 的讨论分支历史，避免其他会话基于旧提交继续工作时产生混乱。

### 11.2 讨论分支与实施分支分离

架构讨论和专项开发使用不同分支。

首批 B1 / B2 / B3 已在独立实施分支 `chatgpt/ai-agent-hardening-b1-b3` 完成开发和验收，并于 2026-09-19 合入 `chatgpt/ai-agent-assistant`；该历史实施分支已经删除，不再作为后续开发入口。

后续 B4～F 继续在：

    chatgpt/ai-agent-architecture-discussion

中讨论和更新文档。

当前保留主开发线与架构讨论线；后续新的专项实施仍应在事项确认后创建独立实施分支，并保持职责隔离：

- 讨论分支负责“为什么这样设计、边界是什么、哪些事项已经确认”。
- 实施分支负责“按已经确认的方案形成实施计划、开发、测试和验收”。
- 实施分支不得自行替未完成的讨论作出长期架构决策。
- 讨论分支也不在讨论过程中顺手实施尚未进入正式落地范围的代码改造。

完成各自里程碑后，再分别整理提交并合入主开发分支。

### 11.3 跨会话恢复原则

跨 ChatGPT 会话时，可靠的共同事实来源是已经提交到 GitHub 的代码和文档，而不是某个会话里的临时上下文。

因此：

- 讨论中已经确认的关键点，应及时提交到架构讨论分支。
- 尚未确认的想法可以继续停留在讨论中，不写成正式决策。
- 新会话继续讨论前，应先读取当前讨论分支中的 14 号文档和相关总体设计文档。
- 不假设新会话能够自动继承上一会话尚未写入 Git 的临时结论。

这套讨论流程本身属于本次 3.5 架构复盘已经确认的工作约定，后续新会话应继续遵循，除非用户明确重新调整。


本文是一份问题清单和建议池。

建议后续逐项讨论：

1. 这项问题是否真实存在。
2. 是否需要现在处理。
3. 最小处理范围是什么。
4. 是否会破坏当前已经通过的 Agent / E2E。
5. 最终确认后再进入正式 `15-AI助手3.5阶段架构加固实施计划.md`。

没有经过讨论确认的建议，不应因为写在本文中就自动视为必须实施。

## 12. 最终判断

### F1：底座整体健康

前三阶段没有把底座做坏。Conversation、Run、Tool Call、Page Capability、权限边界、Checkpoint、全局 AI Store 等主干方向正确，B1 / B2 / B3 的真实实施与验收进一步证明现有架构可以沿原路线持续加固，不需要推倒重建。

### F2：第四阶段前必须处理的技术债

Phase 3.5 剩余真正需要优先处理的是：

- B4 统一 Page / Action 契约与代表页验证。
- B5 Tool Policy 职责收敛。
- B6 Capability Protocol v1 正式化。
- C1 Agent Loop 有限职责拆分。
- B9 核心状态与竞争条件测试。
- B10 SQL Migration 基本规则。

这些事项共同解决的是“继续扩展时会越来越乱”的问题，而不是增加新的产品功能。

随后在 Phase 3.5 后段集中完成 B4 / C3 的全部现有 Page Capability 页面迁移，避免把两种页面 AI 接入形态带进第四阶段。

### F3：当前不值得提前做的事情

以下事项虽然长期可能有价值，但当前没有足够真实产品驱动力，提前实现会把步子迈得过大：

- 精确 TokenEstimator。
- RunExecutionManager / 分布式执行。
- 一次性全面前端重构。
- 动态 Tool Policy DSL / 规则平台。
- Flyway / Liquibase 等完整 migration 框架。
- 多 Runtime 管理平台。
- 独立 Harness、后台虚拟员工执行平台。
- 其他尚未进入真实阶段的复杂自治能力。

### 最终收口

Phase 3.5 的总体策略是：

> **保留已验证的产品能力和核心架构；把已经开始膨胀、会影响下一阶段的边界收紧；对页面能力做到机制先行、样板验证、后段全量统一；对尚无真实需求的未来平台能力只留接口方向，不提前建设。**

这与三阶段产品愿景保持一致：第一阶段先把“AI 理解业务并可靠帮助用户完成业务”做稳，再为虚拟员工和受治理的自我改进系统逐步增加真正需要的底层能力。