# 成熟执行框架实施与验收工作笔记

> 第三阶段及最终组合验收的动态恢复点；总约束 29，方案 37，计划 38。2026-09-21 建立。

## 当前恢复点

- 三阶段技术验收全部通过。最终后端提交 `acde4a22e625d7655493274f8538c66cafba330d`、前端提交 `7aca19e2ce95c9fde3f9f449c9c4201e59b7f75a`，均在 `codex/ai-server-capability-evolution-impl`；产品工作树已提交，主目录仍只维护文档。
- 前两阶段配对版本和干净清单见 38。第三阶段审核依据为 37 R1 / 38 R1，文档提交 `c0248ae185fac8f4e8b62f34c723865dcef2939a`；38 R2 只追加有依据的环境时长预算。提交前全部代码、测试与文档差异哈希清单为 `.validation/stage3-versions-20260921-082007.json`，52 个后端文件、5 个前端文件，未把未验收代码带入文档分支。
- 前期 Pi 0.85.1 开发库最小原型已证明两次模拟模型调用、自定义工具等待和取消；证据 `.validation/harness-prototype/probe-run/evidence.json`，原型当时无收费调用。后续真实闭环与累计消耗见本页最终记录。
- 已核查当前 `AgentRuntime`、工具协调器、运行取消、上下文压缩、来源鉴权与结果认领。旧适配为独立 Spring AI 单轮请求，本阶段由 Pi 接管连续模型/工具执行，保留若依持久事实。
- 真实三阶段组合、成果浏览器和所有关键反例通过，交付与清理见本页末尾最终记录。工作空间使用宿主回环 28097，当前已停止。专用虚拟机于 08:26:54 正常关机，本轮自约 02:05 连续运行约六小时二十二分钟，未超过 38 R2 的八小时上限。
- 第三阶段唯一账本 `model-budget-stage3.json`：14 请求、57,116 输入、2,176 输出及推理、13 工具轮次、1 次任务尝试、0 重试，上游累计 115.735 秒；所有模型请求都有可信用量。第二阶段 19 请求、38,990 输入、1,279 输出及推理、17 工具轮次；第一阶段 28 请求、44,266 输入、1,723 输出及推理、23 工具轮次。三阶段合计 61 请求、140,372 输入、5,178 输出及推理，账本及失败记录均保留。

## 承接表

| 诉求 / 决策 | 方案机制 | 计划动作 | 实际落点 | 证据 |
|---|---|---|---|---|
| H-01、D-017/018 | Pi 开发库循环、若依异步授权回调与统一历史 | R1 | `runtime/pi`、`PiAgentRuntime`、原协调器 | 六项真实库测试、接口/数据闭环、完整浏览器回归 |
| H-02/03、D-023 | 独立容器、内核资源限制、用户和全局预留 | R2 | `runtime/workspace`、固定镜像/服务 | 隔离 065407、存储 072004、实际处理器/临时目录补测 |
| H-04、D-007/008 | 进程管理、续租回收、崩溃不重放写入 | R1/R2 | `AiRuntimeRecovery`、Pi 回收、服务租约 | 主动停止、真实杀进程、同包重启未知写入拒绝 |
| H-05、D-003 | 治理权限、来源校验、成果限额和读取租约 | R3 | `AiWorkspace*`、04 迁移、前端治理/成果 | 全栈 073739、故障存储、慢下载、治理和真实成果浏览器均通过 |
| H-06、D-020 | 配对回归与组合业务任务 | R4/R5/R6 | 模块、迁移、生产包及浏览器，真实组合脚本 | 41 常规模块、复用仍适用的 4 项 JDBC、完整浏览器、真实组合及配对提交通过，清理见最终记录 |

## 首轮实现与证据

Pi 实际开发库四项测试通过：异步回调阻止提前模型续跑、动态加载真实参数定义、错误参数回执可供模型修正、取消/输入管道关闭/伪造回执均不继续请求模型。首次安装后冷启动超过测试原设 15 秒，且测试清理未等待子进程真正退出而掩盖原超时；测试改为与启动预算一致的 30 秒，并等待退出后回收，复测全部通过。后端模块测试通过，原 Spring AI 聊天适配已由 `PiAgentRuntime` 替换；模型配置检测和分词仍复用原库，不构成第二条聊天循环。

真实后端 28092 于 06:39 启动，PID 121908，包 SHA256 `b092aaf117ee4dd3ad32490b55448c30cecbb7a4c1092b786fe68b610356004d`。Pi 接管后的接口完整验收 `.validation/api-smoke-20260921-064114.json` 通过：193 个目录、59 个可支持契约、读取会话 176、确认写入会话 177、9 次模拟请求，浏览器伪造回执仍拒绝。数据库五种原范围、独立统计、隐藏字段、归属转移、未知范围、旧令牌撤权的真实全栈复验输出 `AI_CONTROLLED_DATA_ACCEPTANCE_OK`；尚未做完整前端与组合验收。

可信隔离服务已在任务虚拟机启动，内核实际支持控制组第二版、内存/交换/处理器/进程限制和默认系统调用过滤。Python 基础镜像锁定摘要 `392307d22300de8b5986851a12d9176dfc0fc073e65bf6523ebd7dcbeb23564e`；文档生成依赖及传递版本已形成 `requirements.lock`，Node 依赖由 `package-lock.json` 锁定，Pi MIT 许可已随包保留。服务使用 512 MiB / 1 处理器 / 96 进程上限，不将容器管理凭证交给任务。首次实际隔离检查中，基础镜像的公开 `GPG_KEY` 名称触发宽泛凭据断言；同时收紧模型命令为明确的最小环境，避免继承基础镜像的无关环境变量，继续实测。

实际隔离验收 `.validation/workspace-isolation-20260921-065407.json` 通过：UID 1000、网络/宿主凭据/管理套接字不可达；内核限制为 256 MiB、32 进程、0.5 处理器；单文件写入报 27、空间写满报 28、第 510 个普通文件触发节点上限；第 28 个子进程创建报 11；400 MiB 分配被内核终止，退出 137 且 `oom_kill=1`。跨任务文件隔离、每用户 2 / 全局 4 任务、成果链接拒绝、实际外部停止、超时整容器删除及 15 秒失租回收均通过，测试产物已清理。

真实存储验收 `.validation/workspace-storage-20260921-072004.json` 通过：实际发布到任务/用户/全局 8/32/128 文件以及 16/64/256 MiB 后，下一件被拒绝；到期时拒绝新读取，已有读取租约释放后真实文件才被清理；磁盘可用约 19.65 GB 低于测试配置水位时无法创建任务，清理仍能执行。回收并发中出现一次 Docker 删除竞争，资源仍被保留计费直至成功删除，测试结束文件和容器已清理；此反例没有通过忽略清理失败来释放配额。

治理与成果适配过程中，调度配置类与实例同名导致启动失败，已分开命名并加入禁止覆盖情况下的真实 Spring 上下文回归测试。工作空间客户端使用独立连接/读取时限；单独四线程安排进程回收和租约，不让文件服务超时阻塞模型取消；撤权任务即使停止请求失败也不再续租。失败的工作空间命令可能产生敏感输出，因此服务端回执的来源复核扩展至失败状态，不能只检查成功状态。

组合验收先证实默认关闭、治理权不等于执行权；随后发现治理返回内部 Jackson 树被当前 MVC 序列化成节点属性，已转普通结构后复验通过。测试原以为同部门仅有读者一人，实际同时创建了同部门治理账号，现按原业务接口的两名可见用户独立核对。第三轮命令被误判超时：数据库时钟为 UTC 的无时区日期，应用运行于上海时区；现用 `timestampdiff` 在数据库内计算运行年龄，成果到期新字段使用 UTC 毫秒。新成果表当时真实记录数为零，已在任务库核对后调整列类型；04 迁移和新装入口同时更新。仍须重新做新装/升级与组合闭环，不能复用调整前的日期列证据。

Pi 新增仅在提供方明确拒绝 `prompt_cache_key` 时的一次兼容请求，普通错误仍无自动重试。第五项实际开发库测试通过，证明兼容请求仍由 Pi 发起，不重放工具，保留原有缓存兼容行为。当前第三阶段尚无收费请求。

日期修正后的 `.validation/workspace-fullstack-20260921-072608/evidence.json` 通过；随后对异步有界下载和失败输出来源复核后的包 `606a543212d6c3dc8c00bf16496278abf0d627f482c6b8a087418f89e672d9d8` 再验，`.validation/workspace-fullstack-20260921-073739/evidence.json` 通过，会话 215。真实 Pi 将本人部门两名可见用户导入无网络容器，生成 XLSX/DOCX/PDF，核对文件结构、实际用户名、计数、字节和 SHA256，跨用户读取拒绝；撤销原用户列表权限后旧令牌无法下载、读历史或发起模型续跑。测试用户、角色、成果及临时容器已清理，三份验证文件留在证据目录。

成果预留改为复制前持久登记，注入 `fsync` 与删除同时失败后，真实残留 8 字节仍占完整配额且环境报告清理异常；解除故障后实际回收，证据 `.validation/workspace-storage-failure-evidence.json`。异步下载以不阻塞写入和容器 60 秒请求时限保护慢客户端，分段重新核对当前授权。应用/数据库时区修正后的新装、历史升级、重复迁移再次输出 `AI_SCHEMA_ACCEPTANCE_OK`。

独立 `ruoyi_ai_regression_20260921_073000` 库、28093/5185/18095 和同包 `606a...` 完成完整浏览器回归，输出 `AI_AGENT_MODEL_SELECTION_E2E_OK`。覆盖原页面写入、敏感动作、模型/推理配置、同会话切换、停止、纠偏、连续纠偏、压缩及压缩中停止/刷新/纠偏、确认后只写一次、跨页角色分配、历史和多标签、设置权限、提示版本、审计及普通业务生命周期。Pi 六项开发库测试通过，包括未声明宿主工具在执行前拒绝；先前框架吞并异常分类已用专门安全分类保留原前端错误语义。预算代理新增第三阶段原生流式协议、完整有界响应和累计结算，九项确定性预算测试通过；尚未收费调用。

07:47 资源复核与延长依据见 38 R2，介质路径仍全部在规定目录，未新增快照/备份/导出。任务镜像不可变标识为 `sha256:a475636e2d1be012246c2c740eb2647f4ced66e937b10acf713947275633c1c3`。此刻正运行真实进程异常验收：先杀死等待确认的 Pi，确认原业务零写入；再在真实业务提交后受控模拟回执丢失，强制终止后端并核对短租约、重启收束及未知写入保护。最终结果尚待该脚本完成。部署和未来客户端边界见 40，当前私有进程凭据协议不会开放给远程客户端。

07:53～07:57 异常流程完成。首轮验收脚本在主动停止后端后遗漏启动函数的必要参数，产品恢复逻辑没有失败；修正为 `start("backend", False)` 并让每项事实即时落盘。恢复核对原写入只出现一次、调用转为 `UNKNOWN`、三个旧运行均失败，证据 `lifecycle-interrupted-recovered.json`，原夹具已清理。完整重跑 `ai_harness_lifecycle_acceptance.py` 输出 `AI_HARNESS_LIFECYCLE_OK`：杀死确认等待中的 Pi 后待办取消且零写入；杀死后端后两个子进程退出、失租容器回收；同包重启后未知写入不能再确认或启动重试，原业务操作日志仍只有一次，孤儿框架目录回收。当前包 SHA256 `664ab2a7659c1cf25f30460d9f9f6a38892449e37605b00e370bb798f806db98`，含目录回收修正；后端 PID 58788，记录 `runtime-evolution/backend-20260921-075527.json`。

新增治理浏览器通过，截图 `stage3/browser/workspace-governance.png`，界面实际关闭再开放、保存状态及容量说明均正确。首次测试点击隐藏的原生开关输入导致等待超时，改点可见开关外壳后通过，未改产品交互。补充 `ai_workspace_limits_finish.py` 实际处理器计算三秒消耗约 1.50 秒、发生 30 次节流；`/tmp` 写满 8 MiB 报 28、第 64 个文件节点被拒绝。四个不读取响应体的下载占满名额，第五个被拒绝；60 秒时限后原客户端尚未关闭，新下载仍成功，实际确认回收；输出 `AI_WORKSPACE_FINAL_LIMITS_OK`。

真实组合验收的首次本地夹具因用户名超过原登录长度限制失败，发生在预算登记和模型请求之前，零收费，夹具已清理；改用短唯一前缀。PDF 独立验收器采用官方 PyPI `pypdf` 6.19.0 纯 Python 包，下载 SHA256 核对后只装入任务虚拟环境，包和来源证据在 `.validation/wheels/`；不改变产品镜像依赖或宿主默认环境。真实任务同时覆盖原接口人数核对、受控三字段查询、导入、隔离生成和三类发布；完成后仍须核对实际文件并通过前端下载。

## 真实组合任务与最后反例

`.validation/harness-real-model-20260921-080540/evidence.json` 通过，会话 223。真实 `gpt-5.6-luna` 先搜索并加载原用户列表接口、调用并按需读结果，再搜索/加载受控数据查询，核对本部门两名用户（166 / 167）；另一部门 168 不可见。随后导入结果，在真实无网络容器执行三次命令、发布三份成果。15 个实际服务端调用全部成功，模型生成工具的轮次为 13（同一响应中多个调用由 Pi 串行处理），没有绕过确认的业务写入。

独立解压 Office 文件并解析 XML，使用独立 PDF 阅读库抽取文字，核对两名用户的完整用户名、编号和真实部门；三份文件均不含外部门用户，PDF 含 `Count: 2`。成果字节、SHA256 和私有下载响应一致。前端 `tests/ai-workspace-e2e.mjs` 以本人账号恢复会话，分别下载三份文件、再次比对哈希，刷新后列表仍正确；输出 `AI_WORKSPACE_ARTIFACT_BROWSER_OK`，截图 `stage3/browser/workspace-artifacts.png` 已查看。脚本最初错误假设新用户默认展示历史入口，现按既有“继续上次会话”入口复验，并正常处理原系统首次密码提示，没有改变产品偏好。

实际模型输入 `model-inputs-stage3.jsonl`：请求 1/2 只有八个发现、结果和工作空间入口，工具定义共 3,135 字符；请求 3 才增加所选接口，定义 4,607 字符；请求 7 才增加 `data_users`，定义 7,573 字符。模型仍完成完整业务目标，证明 Pi 接入后渐进式披露继续生效。128K 为该模型工作预算，保留输出及推理空间；没有用扩大窗口代替披露或进行收费填满试验。

`workspace-cancel-revoke-20260921-081455.json` 对应最后一次活动命令检查，脚本 `ai_workspace_cancellation_acceptance.py` 输出 `AI_WORKSPACE_CANCEL_REVOKE_OK`：若依停止运行 226，状态为 `CANCELLED`，约 2.53 秒回收容器；执行中移除工作空间使用权限，旧令牌所在运行 227 变为 `FAILED`，约 2.94 秒回收。两项都只有初始一次模型请求，命令未成功完成，之后没有模型续跑。具体时间记录在证据中，不能将它们外推为所有部署环境的固定响应上限。

最后慢下载和补充资源证据为 `workspace-final-limits-20260921-080257.json`；生命周期证据为 `harness-lifecycle-20260921-075446.json`。无新增收费请求。最终前端两项契约与 2,593 模块生产构建再次通过；后端最终包保持 `664ab2...`，与已验收运行包完全相同，后续只增加验收脚本和持续集成分支配对配置，没有改变已验收产品代码。工作流内嵌 Python 已逐段语法校验；没有将本地验证称作远端持续集成已运行。

## 反向验收、交付范围与限制

H-01～06、D-002/003/007/008/017/018/020/023 的关键机制均有实际落点和反例：框架负责真实循环，若依统一授权与状态；业务读写走原权限和数据范围；三阶段组合未出现文件、历史或结果越权通道；停止、撤权、崩溃与未知写入闭环成立；空间、文件数、处理器、内存、进程、并发、时限及低水位在实际运行中触发拒绝与回收。原业务、页面工具、确认、纠偏、上下文压缩、刷新、设置和审计仍可用。各阶段设计和计划已按其最初诉求完成，不以模型文字或成功响应代替业务和资源事实。

明确边界：当前是单后端实例、MySQL 8 的已核查映射和 Linux 容器隔离；不开放任意数据库连接、原始 SQL 或宿主命令，不把接口复杂类型保守关闭当作缺陷。客户端只交付同款框架的身份/任务授权/确认/回执/撤销/成果边界，没有实现客户端安装器或通用多框架平台。24 小时保留配置以同一真实清理路径缩短时限验证，不声称持续等待了整天；没有验证自定义模型网关的 256K 长输入上限、集群调度或生产部署，这些不属于本轮完成条件。

## 最终版本与环境交付

后端 `acde4a22e625d7655493274f8538c66cafba330d`、前端 `7aca19e2ce95c9fde3f9f449c9c4201e59b7f75a` 按明确文件白名单提交。提交前编码、定向差异、已授权凭据实际值扫描通过，无凭据和任务数据进入版本库。权威文档只在主目录文档分支提交，产品代码未合并到该分支或稳定主线。文档最终提交与三个干净工作区的配对清单由 `.validation/overall-versions-*.json` 记录；完整交付证据索引为 `.validation/overall-acceptance-evidence.json`。

最终环境证据 `.validation/stage3-environment-final.json`、`vm-info-final.txt`：虚拟机 UUID `4fafc882-7e0c-4363-bdeb-d558763af567`，所有介质仍在 `E:/VMDisk/RuoYi-AI-Evolution/`，关机后物理占用 5,291,974,498 字节；宿主 E 盘余量 131,386,740,736 字节，客体关机前可用 19,624,558,592 字节。镜像总量 1.484 GB，构建缓存为零，可信服务目录 12,288 字节，服务内存约 44.7 MiB。没有创建快照、备份或导出，也未改动其他虚拟机。

临时账号/角色/岗位已通过原接口清理，活动账号只余 `admin` / `ry`、角色只余 `admin` / `common`、岗位只余原始四条。活动运行、任务容器、成果元数据和可信服务实际成果文件均为零；测试成果副本留在证据目录。先停止后端 58788、前端 68516 和客体服务，再于 08:26:54 通过正常电源事件关机。关机过渡中一次 VirtualBox 状态查询返回 1，后续只读复查确认 `poweroff`，未采用强制断电。模型连接已恢复到授权环境变量指向的真实服务，凭据仍加密保存，没有额外模型调用；恢复命令见 40。

自动审批审查拒绝删除 22 个中间构建包，工具仅给出 `blocked by policy`，未提供具体原因；没有尝试绕过，所有中间包和日志原样保留，约 3.3 GB 可回收量仍在宿主余量之内。两个早期框架测试临时目录也保留为故障证据，其中仅观察到 2 字节的空 `auth.json`，无真实凭据；产品任务临时目录已由生命周期用例验证回收。此保留不影响任务容器和成果的强制限额、清理或可复验交付。
