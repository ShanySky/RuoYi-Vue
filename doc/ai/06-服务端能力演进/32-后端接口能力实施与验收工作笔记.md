# 后端接口能力实施与验收工作笔记

> 本阶段动态状态唯一入口。总约束见 29 号，方案见 30 号，实施动作与预算见 31 号。建立：2026-09-21。

## 当前恢复点

- 当前目标：接口阶段 API-01～08 已通过，继续受控数据库阶段；总目标保持活动，第三阶段尚未实施。
- 文档：主目录 `codex/ai-server-capability-evolution`，方案与计划 R1 已提交为 `99c47c24689f57da11f7244912b515992cca45a2`；本笔记后续状态更新尚未提交。
- 代码：后端、前端配对工作树均为 `codex/ai-server-capability-evolution-impl`；接口阶段提交分别为 `be2f4d036c9e1b697b72aa8b52c2afb420b138cd`、`e11d2b6665a2df2158173564973e23b7f267369d`。主目录未改产品代码，文档与实施分支保持分离。
- 已确认：真实 Spring 请求映射可作为发现源；现有登录权限为缓存快照；现有运行只接前端工具；原确认、会话、运行和审计底座继续复用。
- 已完成：接手四目录版本核实；读取 29 号及长期架构依据；补充官方披露和过滤器边界资料；形成方案/计划并自审；Java 17 下后端模块测试通过；Node 20.20.2 两项前端契约通过。
- 已验收：新装与升级；多用户权限/数据范围；停止、真实超时未知、重复确认；结果配额；新增界面；真实模型查询与确认后新增/核对岗位；新版完整既有浏览器回归。后端模块测试最终 30 项、预算代理 6 项、前端两项契约和前后端生产构建通过。
- 下一步：以本阶段配对提交为基线形成数据库阶段诉求、方案、计划及自审。最终生产包 SHA-256 为 `c2a3e159485e32e61d17ba22c6407445bffabc0b3713d1a1b23b919c0ae2d2da`；仅后端 28092、前端 5184 和专用验证机继续供下一阶段使用。基线和回归服务已停止。接口阶段真实验收累计重试 2，不能重启整批收费验收或重置账本。
- 禁止误解：方案通过和 21 项旧测试通过都不表示新阶段已实现；不在主目录改产品代码，不在工作树文档维护平行动态状态。

## 诉求到证据

| 诉求 / 决策 | 方案机制 | 计划 | 实际落点 | 证据与结论 |
|---|---|---|---|---|
| API-01，事实单一来源 | 30 §3 | P2 | `server/AiApiCatalog`、`ApiContractSchema`、治理控制器/策略表、前端 `views/ai/apis` | 新增映射自动发现、未授权及参数模式测试通过；真实目录与浏览器逐接口启停通过 |
| API-02/03，D-002/003 | 30 §4.1 | P3 | `AiFreshIdentity`、`AiIdentityFilter`、`AiNativeRequestGate`、`AiNativeApiInvoker` | 原部门/本人范围、业务唯一性校验、操作日志及旧令牌撤权真实通过 |
| API-04，D-006/007/008 | 30 §4.2 | P3/P4 | `AiServerToolService`、调用记录、原协调器/循环必要分支、前端确认适配 | 确认前零写入、并发单次写入、停止后拒绝、真实超时未知及禁止继续写入均通过 |
| API-05，结果防越权 | 30 §4.1/5 | P4 | `AiBusinessHistoryGuard`、来源记录、结果配额事务锁及过期清理 | 跨用户/运行句柄、撤权历史、审计保护、用户及全局配额拒绝真实通过；清理仍需最终复核 |
| API-06/07，按需披露与成本 | 30 §5 | P4/P5 | 动态工具定义、`AiTokenBudget`、原上下文装配器 | 确定性模型实际输入证明按需加载；中文分词、工具模式和预留空间测试通过；收费模型仍未调用 |
| API-08，D-020 | 30 §6 | P1/P6 | 模块测试、前端契约、生产构建和完整浏览器用例 | 原基线与新版完整闭环均通过；新版截图 `.validation/stage1-final/browser` |

## 基线、问题与取舍

2026-09-21 接手核实四目录均干净且提交与交接一致。后端 `pom.xml` 为 Java 17、Spring Boot 4.1.0、Spring AI 2.0.1；前端流程要求 Node 20。本机有 Java 17.0.11，已通过任务进程选择；另安装 Node 20.20.2 并以进程路径调用，没有修改默认版本。

后端命令 `mvn -B -Pci -pl ruoyi-ai -am test`：21 项、失败 0、错误 0、跳过 0。原始双流日志位于 `E:/Programs/project/ruoyi-ai/ruoyi-github/worktrees/ai-server-evolution/.validation/stage1-baseline/maven-20260921-012459.out.log` 与同名 `.err.log`；Java 子进程固定 UTF-8，严格解码通过且无替换字符。该证据仅对应产品代码未改动的起点。

前端命令 `node tests/ai-capability-contract.test.mjs`、`node tests/ai-settings-boundary.test.mjs`：均成功；`npm install --no-audit --no-fund` 成功且未改锁文件；`npm run build:prod` 成功（2,582 个模块），仅有既有依赖弃用/产物体积提示。浏览器验收未运行。

当前已发现的设计风险：直接使用登录缓存无法满足撤权；只过滤发现列表无法约束伪造工具调用；反射调用可能跳过完整入口；浏览器结果不能作为服务端执行事实；结果进入历史和检查点后可能绕过撤权。30 号 R1 已分别给出机制，后续必须测试机制真正生效。

后端基线生产打包 `mvn -B -Pprod -DskipTests package` 通过。原包已保留在 `.validation/stage1-baseline/ruoyi-admin-8c820632.jar`，SHA-256 为 `51948701f9048b65b12afaf3aa682eeb59d983ccd46cc66aef3ef91053569286`；日志 `package-20260921-013615.out.log`。在产品代码继续修改后仍可用该原包核验初始行为。当前新实现执行 `mvn -q -Pci -pl ruoyi-ai -am test` 通过；新增 `AiApiCatalogTest` 3 项，加原 21 项共 24 项。当前前端构建包含 2,585 个模块，尚未视作界面验收。

环境问题与修正：初始镜像下载速度低且发生超时，保留已下载字节并用同一文件断点续传，最终用 Ubuntu 官方 `SHA256SUMS` 核对通过；未绕过完整性检查。隔离 Python 环境中 `pip` 安装长时间停滞，终止本任务对应进程后改用现有 `uv pip --python <任务环境>`，依赖安装成功，未修改宿主默认 Python。

02:30 后补充事实：新后端生产包已构建并启动。`ai-schema-acceptance.sh` 在专用机容器中验证新装、历史升级及全部有序迁移重复两次，输出 `AI_SCHEMA_ACCEPTANCE_OK`，测试数据库已按脚本清理。MySQL 为 8.0.46，Redis 为 7.4。Docker Hub 在客体连接被拒，改用 Docker 官方公开在 ECR 的镜像；未使用来源不明的替代包。

既有浏览器验收输出 `AI_AGENT_MODEL_SELECTION_E2E_OK`，涵盖页面工具、普通业务、确认、停止、纠偏、压缩、刷新、多标签页、设置和审计。后端对应冻结的 `8c820632` 包；前端为实施工作树（已含本轮少量确认适配），因此不冒称“两个原始提交的纯基线”。使用 Node 20.20.2、Chrome 153.0.8010.48；截图冻结到 `.validation/stage1-baseline/browser`。前端测试增加可选 `PLAYWRIGHT_EXECUTABLE_PATH`，不改变默认浏览器。

02:43 新接口确定性闭环通过，证据 `.validation/api-smoke-20260921-024306.json` 和 `api-inputs-20260921-024306.jsonl`：190 条真实映射，95 条可自动支持；9 次本地模型请求，查询会话 3、写入会话 4。首轮只有发现工具，详情加载后才出现具体接口；返回岗位明细与原 `/system/post/list` 一致；未确认零写入、重复确认单次副作用、浏览器伪造结果拒绝，清理通过原删除接口完成。早一轮验收失败源于测试模型错误依赖工具结果可选 `name`，修正为按 `tool_call_id` 关联，且加强业务数据断言；产品披露机制未因此改弱。

上下文新增两项测试，共 26 项模块测试通过：中文计数使用分词器与原保守下限取较大值，真实工具模式计入预算，64K/128K/256K 均保留输出与推理空间。当前默认仍为 64K；并不据此推断真实模型容量。

02:58 治理复验对应运行包 SHA-256 `c2b8bef06e47e3913a843687ead0b2180c346b7e89df91ae78f8fcbc83f5b151`，证据 `api-governance-20260921-025840.json`。实际创建两名不同部门、不同权限用户，核对本人数据范围与旧会话保护；数据库锁造成回环超时后，解除锁确实观察到一条已提交岗位，AI 调用为 `UNKNOWN` 且拒绝继续写入。所有临时用户、角色和岗位通过原业务接口清理。

03:10 结果限制用例对应包 `d9d606eb9f4d5fe563d63b00a20867e04821f116226f9d55b7666a1054ba6c28`，证据 `api-result-limits-20260921-031014.json`：12 条真实长备注岗位使一次结果披露超过 8 KiB，读取被拒绝，缩到一条后可读；使用精确标记的配额占位记录触及用户 64 / 全局 1024 上限后，真实查询不能保存新结果，占位与岗位均已清理。配额用例验证数据库事务内强制拒绝，不靠模型自律。

03:16 新增界面输出 `AI_SERVER_API_BROWSER_ACCEPTANCE_OK`，证据 `.validation/server-api-browser/{governance,confirmation,completed}.png`。真实点击治理开关并核对后端状态，确认卡展示具体服务端参数；确认前零岗位，确认后单条真实岗位，最后通过原删除接口清理。早一轮界面用例错误等待组件内部隐藏输入，改为点击可见开关；未改产品行为掩盖问题。

真实模型首轮发现整句搜索未命中、提供方忽略关闭并行工具后返回多个动作。修正短关键词说明、真实中文名称片段匹配，以及每轮仅认领首个服务端动作的最小兼容；增加测试证明其余写入没有排队或执行。之后查询任务已成功，但写入任务仅返回文字确认，因此补充“调用写工具先创建系统确认卡，确认后才写入”的明确契约，不能把模型文字当作确认或执行事实。

新版完整浏览器回归曾在 8K 压缩用例首轮被预算拒绝：旧测试未计完整页面工具和输出预留。压缩用例窗口改为 16K，保留第二轮必须产生真实检查点和停止/纠偏断言；8K 超限拒绝仍由预算测试覆盖。重跑又暴露了模型设置、快捷键和原岗位/用户夹具的前轮遗留，不把这些当产品回归，也不将失败算作通过；下一次使用全新专用回归库重做完整流程。构建时 Windows 占用运行中的目标 jar 导致过一次重打包失败，启动脚本现复制不可变运行包并记录哈希，修复后打包通过。

03:28 真实模型接口验收成功，证据 `.validation/api-real-model-20260921-032621.json`，运行包 `c65c1210f81ba21de3d70afd78e8eae0bdd151a410c14d54399818bbab9910dd`。会话 36 查询岗位，会话 37 通过服务端确认新增后再次查询实际岗位编号，原接口核对恰好一条，最后清理。实际请求始终只带常驻发现工具和已加载定义；工作预算 128K，容量来源与网关长输入验证边界见 30 §5。最终包仅追加了异常单权限声明的保守拒绝，真实任务所用岗位契约与执行行为未变化，可复用该收费证据。

03:29 在全新回归库中已通过压缩继续和停止用例；刷新用例失败时，查询运行事实为 `COMPLETED` 且已有检查点，证明原模型桩 1.2 秒延迟不足以让浏览器刷新后发出取消。将持续集成模型桩的压缩延迟改为 6 秒，保留必须观察 `CANCELLED/CLIENT_RELOAD` 和零孤立检查点的断言；该调整只制造明确在途条件，不修改产品停止机制。旧 18092 桩继续保留原提交行为，新 18095 从当前工作树工作流加载。

03:41 新版完整流程输出 `AI_AGENT_MODEL_SELECTION_E2E_OK`。后端使用最终包，前端对应最终产品源代码；覆盖压缩继续/停止/刷新/纠偏、普通停止、连续纠偏、写入确认与已完成写入不重试、跨页面角色分配、历史、多标签页、退出登录、模型回退、提示词版本、页面能力开关、会话策略、审计和普通业务完整生命周期。截图已冻结到 `.validation/stage1-final/browser`。新增默认关闭、禁用及契约变化后拒绝发现/详情/旧治理提交两项测试通过，模块总数 30。

03:44 补充服务端 PUT 修改真实验收，证据 `api-update-20260921-034418.json`：确认前原岗位不变，确认后岗位名称、排序和状态同时正确更新，原业务操作日志记录 `SysPostController.edit`，临时岗位清理。最终目录 190 条真实映射、59 条可自动支持业务接口；45 条运维/工具入口不能被治理开关越过。自然过期后的结果内容与已完成运行的加载记录均为零残留（宽限 2 分钟查询核对）。

## 阶段反向验收与边界

接口阶段通过。代码与证据逐项支撑 API-01～08：目录来自真实契约并默认关闭；披露与执行权限不高于当前用户；原业务入口和数据范围有效；确认、停止、重复请求与未知写入有强制约束；句柄、历史及审计没有成为绕过撤权的通道；按需加载已由真实模型输入和业务结果证明，扩大工作窗口仍受预算预留与分页约束。前后端生产构建、数据库新装/升级和原应保留功能均已验证。

本阶段明确边界：只开放能可靠理解的常规 JSON 业务契约，复杂/流式/文件接口及运维、凭据缓存、代码执行入口保守关闭；授权变化使相关会话历史整体失效，而不尝试猜测哪些文字可以继续披露；已进入原业务事务的写入不能假装被停止回滚，未知结果须核对。128K 是本模型工作预算，未用高成本长输入证明自定义网关上限。数据库直查、文件和命令执行仍未开放，不能据接口阶段通过宣称后两阶段完成。

## 消耗、环境与清理

- 真实模型截至 03:28：累计上游请求 28（含目录查询）、输入 44,266 词元、输出及推理 1,723 词元、工具轮次 23、验收重试 2；两个业务闭环通过。唯一累计账本 `.validation/model-budget-stage1.json`，实际输入与后续输出分别为 `model-inputs-stage1.jsonl`、`model-outputs-stage1.jsonl`；不得重置。预算代理通过 6 项确定性测试，进程重启保留预扣和失败消耗，整体验收重试也持久计入。上游地址及密钥均未输出或提交，应用只存任务代理凭证。
- 主机只读核查：E 盘约 132 GiB 可用，物理内存约 64 GiB；3306、6379 已监听，仅作为存在性事实。现有虚拟机未改动。
- 阶段收口资源：E 盘剩余约 126.26 GiB；客体根盘 23 GiB、已用 3.7 GiB、可用 19 GiB。MySQL 约 484 MiB / 49 进程，Redis 约 3.46 MiB / 6 进程，均在既定上限内。未创建快照、备份或导出。临时业务/配额夹具已清理；保留任务数据库和运行包作为证据与下一阶段基线。基线后端/前端/模型桩、回归后端/前端/模型桩及临时接口模型已停止。
- 新环境：Node 20.20.2、配对工作树构建产物、`.validation/venv`。专用机 `RuoYi-AI-Evolution`（`4fafc882-7e0c-4363-bdeb-d558763af567`）已启动：4 核、6 GiB、24 GiB 动态盘；创建后目录约 2.35 GiB，系统内根盘约 21 GiB 可用。系统初始化完成，内核 `6.8.0-139-generic`。SSH 仅转发宿主 `127.0.0.1:22292`，数据库/Redis 预留转发 `13392` / `16392`。
- 介质 SHA-256：`c1655a37ff4141e4f16effb7364a640188cc17ec7dfc869a1283aa724dcd6bc1`。配置、盘、种子介质和快照目录都核对在指定独立目录内，证据 `.validation/vm-info.txt`；参考虚拟机未修改。任务密钥和测试服务凭据位于 `.validation/`，限定当前账号读取，不进入 Git。
- 可复验脚本：后端工作树 `scripts/local/ai_evolution_vm.py`、`ai_evolution_stack.py`、`ai_baseline_mock.py`、`ai_evolution_run.py`、`ai_server_api_acceptance.py`（未提交）。虚拟机约 02:05（本地时间）开始运行，须在单轮 6 小时预算内复核/正常关机。MySQL 容器上限 1.5 核/1536 MiB/256 进程，Redis 为 0.5 核/256 MiB/64 进程；日志各 10 MiB × 2。基线端口 28091/5183，新版 28092/5184，固定测试模型 18092；每个进程的标识与日志位于 `.validation/runtime-*/`。Python 执行显式设 `PYTHONUTF8=1`，早期客体安装控制台出现过编码替换，不能把那段当作完整原始日志。

## 最小验收清单

- [x] 方案承接诉求，计划承接方案，完成实施前自审。
- [x] 真实全栈基线、产品实现与有判别力的回归测试。
- [x] 新装 / 升级与逐接口治理、撤权、停止、未知结果、历史保护。
- [x] 少量真实模型业务闭环及实际输入披露证据。
- [x] 配对版本构建、保留功能回归、阶段反向验收、阶段临时资源清理与代码版本交付。
