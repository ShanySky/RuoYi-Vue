# 受控数据库能力实施计划

> 版本：2026-09-21 / R1；依据 34 号 R1 与 29 号共同约束；状态自审通过，尚未实施。动态恢复点与证据唯一入口为 36 号。

## 1. 配对基线与影响

后端 `5074e89ab4ff2969c68b519af7988cad3f29ad76`，前端 `357918cc6ddb89a56f146836d13110729bf9fa24`；文档补强验收 `46d6d5ea12da63b95912d9b1593eca3ae9879ea7`。三工作区干净清单为 `.validation/stage1-versions-20260921-045140.json`。继续使用既定主目录文档分支和配对实施工作树。

基线已有 32 项后端模块测试、两项前端契约、前后端生产构建、新装/升级、接口真实模型及完整浏览器回归。详见 32 号，不无故重复。新增影响：数据范围算法入口、动态工具、来源鉴权、停止执行、治理导航；这些入口须补测试。普通业务算法不变，禁止把未验收代码混入主目录文档分支。

## 2. 动作与验证

| 动作 | 具体工作 | 验证及承接 |
|---|---|---|
| Q1 授权与目录 | 小范围抽取原数据范围入口；增加三种原查询绑定及零行元数据；拒绝未知范围/来源/表 | 不同用户行集与原业务对照；原字段投影；字段来源真实核验。DB-02/03 |
| Q2 治理 | 库表字段操作策略、变更指纹、治理控制器与前端树；新装和升级脚本 | 默认关闭；旧配置失效；管理员配置权不等于业务读取权。DB-01/03 |
| Q3 受限执行 | 参数校验/语句编译、只读事务、字节与行数限制、并发和时间限制、停止取消 | 注入与隐藏字段、真实慢查询、只读拒绝、并发、停止后无迟到披露。DB-04 |
| Q4 运行接入 | 搜索/详情/动态定义；共用加载、结果、配额、来源授权和历史保护 | 跨用户/运行、撤权/转部门/字段收紧；实际输入逐步披露。DB-01/05/06 |
| Q5 联合验收 | 模块与生产构建、迁移、真实账号查询聚合、浏览器治理及受影响原链路、少量收费模型分析 | DB-01～06 反向审核；保留接口写入和页面工具；模型任务对照真实统计 |
| Q6 配对交付 | 代码审查、敏感信息检查、代码与文档版本清单、临时数据/进程清理 | 证据充分才关闭阶段；随后接续成熟执行框架阶段 |

实施自审：每项需求都有机制、计划落点与失效观察。Q1～Q4 可按依赖交叉，但 Q5 不以页面提示或接口成功代替实际数据、资源和权限结果。新事实推翻假设时同步 34 / 35 / 36，无需用户另设批准点。

## 3. 第二阶段真实模型累计预算

使用独立的第二阶段持久累计账本，保留第一阶段原账本及消耗；不得借分批或重启重置任一阶段。先完成确定性校验，再做一项用户按部门统计分析，必要时补一个与接口组合的任务。

| 项目 | 上限 |
|---|---|
| 输入 / 输出及推理 | 180,000 / 24,000 词元；单请求输出及推理最多 4,096 |
| 上游请求 / 工具轮次 | 32 / 24；单任务最多 10 轮 |
| 重试 | 全阶段最多 2 次，失败与重试全部计入 |
| 时间 | 单请求 120 秒；上游累计 20 分钟；实际验收墙钟 60 分钟 |

沿用 `gpt-5.6-luna` 128K 工作预算及预留。当前自定义提供方没有可靠价格依据，不虚构金额预算；凭据仅由原本地代理读取用户授权的环境变量。代理先预扣请求/词元预算，失败无用量按预留上限计，不向测试应用或日志暴露上游凭据。

## 4. 环境预算与清理

继续使用 31 号的专用虚拟机和资源上限，数据库只操作本任务命名库。记录每轮真实数据夹具、模型桩、执行查询与连接回收；临时用户/角色/岗位经原业务接口清理。通过后停止本阶段临时回归服务，保留必要证据及下一阶段环境。虚拟机本轮约 02:05 启动，08:05 前必须复核并执行正常关机/后续运行安排，不让容器限制掩盖虚拟机无预算运行。
