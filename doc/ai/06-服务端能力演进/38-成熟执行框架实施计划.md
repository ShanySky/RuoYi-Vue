# 成熟执行框架实施计划

> 2026-09-21 / R2，依据 37 R1，方案承接自审通过。R2 仅据资源复核延长本轮虚拟机连续运行预算；动态恢复点 39。

## 基线与动作

已验收配对代码：后端 `1930753cf7bf774390687286feadfea4542c7797`、前端 `3631b9da17924a58d7695f63c5884a7845b0eeef`；第二阶段文档提交 `13d9f8f57a9aef2346db372143e1a1bb3279f761`；三工作区干净清单 `.validation/stage2-versions-20260921-061912.json`。保留 32、36 的已验收事实，新改动涉及模型循环、停止、工具风险、上下文、治理、文件和部署，必须复验对应链路。

| 动作 | 实施内容 | 关键验证 |
|---|---|---|
| R1 框架接入 | 锁定 Pi 依赖、受控 Node 子进程、异步回调与上下文刷新、替换原模型适配 | 实际库循环、动态工具、暂停确认、停止、无工具压缩、模型用量、异常退出 |
| R2 隔离服务 | Linux 固定容器管理、命令及文件工具、租约、关闭及残留清理、内核资源边界 | 越界/网络/凭据、写满与文件数、内存/处理器/进程、超时、全局和用户并发 |
| R3 治理与成果 | 开关/权限/迁移、导入原结果、成果元数据/限额/下载/清理及前端入口 | 默认关闭、权限收窄、源码来源撤销、跨用户、链接、到期/低水位/清理竞争 |
| R4 保留链路 | 更新确定性模型服务支持框架协议；模块、新装/升级、生产构建、真实浏览器回归 | 原业务、页面工具、接口确认、数据分析、停止/纠偏/压缩/刷新恢复/设置/审计 |
| R5 真实与组合验收 | 少量真实模型任务，隔离生成表格、文档、PDF 并检查文件内容；反向审核 H-01～06 | 真实输入、本人业务结果与文件、停止和权限持续生效，不能只核对文件扩展名 |
| R6 交付 | 最终审查、配对提交、运行指引、许可证和锁定文件、证据清单、环境清理/保留 | 代码/文档版本一致，可按固定命令复验；整体目标条件全部成立才关闭 |

R1、R2 原型可独立推进，产品集成按依赖顺序；本任务主代理实施，不另起平行代码线。默认先确定性验证，再收费调用，失败按实际原因修正，不以测试数量代替完成标准。

## 第三阶段及组合任务累计模型预算

使用唯一 `model-budget-stage3.json`，前两阶段账本永久保留。上限：64 请求，400,000 输入、60,000 输出及推理词元，单请求输出及推理 4,096；48 工具轮次，单任务最多 16 轮；全阶段 3 次重试；单请求 120 秒，上游累计 40 分钟，真实验收墙钟 120 分钟。组合验收也计入本阶段，不另开账本。自动重试默认关闭，失败无可靠用量按预留计。真实模型沿用已授权的 `gpt-5.6-luna` 和 128K 工作预算，不做付费填满窗口试验。

## 环境与资源安排

沿用专用虚拟机 4 处理器 / 6 GiB / 24 GiB 动态磁盘，全部介质、快照、备份和导出受 `E:/VMDisk/RuoYi-AI-Evolution/` 32 GiB 总目录预算约束，宿主 E 盘低于 40 GiB 停止新增资源。仅增加锁定的沙盒镜像、可信工作空间服务及任务成果根目录，镜像/构建缓存预算 4 GiB，服务数据含成果/日志/元数据预算 512 MiB；日志有界轮转。现有其他虚拟机不动。

本轮连续运行始于约 02:05，08:05 前必须实际复核资源、记录必要的后续预算并正常关机或作出有依据的后续运行安排；不能静默重启以掩盖累计时长。Node 22.22.2 仅供框架工作进程，前端 Node 20 和 Java 17 不变。结束时停止本任务临时测试服务与代理，清理测试用户、任务容器、过期临时文件；保留必要的可复验环境、有效成果与证据并记录落点。

R2 资源自审（07:47）：完整浏览器回归已通过，异常退出和最终真实模型验收仍在进行，继续使用现有环境可避免打断在途恢复验证。实测虚拟机目录 5,280,791,475 字节（约 4.92 GiB）、客体根分区约 19 GiB 可用、宿主 E 盘 131,729,911,808 字节可用；仅 MySQL/Redis 两个常驻容器，Docker 镜像总量约 1.48 GB、构建缓存为零，均在既有容量边界内。自主追加最多两小时，连续运行截止改为 10:05，累计上限八小时；不通过重启清零，不增加处理器、内存或磁盘容量。提前完成即正常结束测试并管理关机，10:05 前再次实际处置。
