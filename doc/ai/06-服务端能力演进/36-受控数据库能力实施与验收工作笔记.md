# 受控数据库能力实施与验收工作笔记

> 数据库阶段动态恢复点与证据唯一入口。总约束 29，方案 34，计划 35。建立于 2026-09-21。

## 当前恢复点

- 接口阶段及 P8 已完成，真实补强与基线见 32。第二阶段方案 34 R1 / 计划 35 R1 已自审，尚未实现数据库能力。
- 代码起点：后端 `5074e89ab4ff2969c68b519af7988cad3f29ad76`；前端 `357918cc6ddb89a56f146836d13110729bf9fa24`。文档起点 `46d6d5ea12da63b95912d9b1593eca3ae9879ea7`。分支和目录继续遵守 29。
- 下一步：Q1 原查询授权绑定、数据范围复用和数据库列来源验证，然后 Q2～Q4。数据库能力尚未开放。
- 第二阶段收费请求和消耗均为 0；第一阶段累计记录保持不变。
- 保留环境：实施后端 28092（包 `e35533d3…`）、前端 5184、专用 Linux 虚拟机内 MySQL/Redis；临时回归服务已停止。虚拟机本轮连续运行约自 02:05 起，须在 08:05 前管理运行预算。

## 诉求到证据

| 诉求 / 决策 | 方案机制 | 计划动作 | 实际落点 | 证据与状态 |
|---|---|---|---|---|
| DB-01，权限只收窄 | 库表字段操作治理、逐层交集 | Q2/Q4 | 待实现 | 未验收 |
| DB-02/03，事实来源唯一 | 原列表映射、真实投影、共用范围算法 | Q1 | 待实现 | 已核实当前三类原查询及范围注解；待运行证明 |
| DB-04，资源与只读 | 参数白名单、只读事务、超时/并发/结果限制 | Q3 | 待实现 | 官方机制已核对；待真实强制拒绝 |
| DB-05，来源保护 | 复用接口阶段归属与结果授权 | Q4 | 待实现 | P8 是基线，不等于数据工具已受保护 |
| DB-06，任务与保留链路 | 渐进定义、真实分析、配对回归 | Q5/Q6 | 待实现 | 未验收 |

## 基线与审核依据

已核查 `SysUserMapper.selectUserList` 明确投影不含密码，包含部门名称/负责人关联；部门与用户服务具有 `@DataScope`，岗位列表没有范围注解，原语义为有列表权限后全局读取。原范围切面已有全部、自定义部门、本部门、含子部门、仅本人五种算法。复用同一算法，不能另写近似 SQL。

数据库基线曾发现“记录自身转部门但读取者不变”的缓存缺口，已先完成 P8，不将问题留到新能力上线后。第二阶段还必须证明字段治理和聚合未形成新绕行。当前方案将无法确认的列来源、范围注解和业务表关闭，先以实际驱动元数据验证这一前提。
