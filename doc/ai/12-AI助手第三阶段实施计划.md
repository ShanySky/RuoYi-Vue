# RuoYi AI 助手第三阶段实施计划

> 日期：2026-09-18  
> 状态：进行中（A/C/D 基础闭环已验证，B/E 留最终场景，F 正在持续实施）  
> 需求与验收来源：`11-AI助手第三阶段推进目标与验收方案.md`  
> 实施记录：`13-AI助手第三阶段实施与验收记录.md`

## 1. 计划使用规则

本文件是第三阶段开发的主跟踪文档。

每个任务必须带有对应需求 ID。实施过程中：

- 不允许出现“做了一个技术改造，但不知道对应哪个用户诉求”的任务。
- 不允许为了方便开发把某条用户诉求悄悄缩小。
- 如果实际代码发现方案必须改变，要先回到 `11` 说明变化是否仍满足原诉求，再调整本计划。
- 每个阶段结束时先做“需求回看”，确认没有越做越偏，再进入下一阶段。
- 状态统一使用：`未开始 / 进行中 / 已实现 / 已验证 / 阻塞`。
- “已实现”不等于“已验证”；只有通过对应用户场景验收后才能标记“已验证”。

## 2. 需求追踪总表

| ID | 需求 | 主要实施阶段 | 当前状态 | 最终验收回指 |
|---|---|---|---|---|
| R3-01 | 聊天字体可调整 | B/G | Dock/刷新已验证，重登待 G | 用户调整字体、刷新/重登保持 |
| R3-02 | 停止当前执行 | C | 已验证 | Stop 后旧 Run 不再继续 |
| R3-03 | AI 内双 ESC 安全中断 | B/C | 已验证 | 焦点范围和误触场景 |
| R3-04 | 运行中继续补充 / Steering | C | 主链与 pending WRITE Steering 已验证 | 最新指令接管、无并行写 |
| R3-05 | GPT Prompt Cache | E/I | 实现与 mock usage 已验证，真实 GPT 待 I | 实际 cache read 证据 |
| R3-06 | Reasoning 中文与真实档位 | B/D | 已实现，待全栈验证 | 中文 UI + 正确 API 值 |
| R3-07 | 64K/75% 自动压缩并续跑 | D/I | Runtime 闭环已验证，语义质量待真实模型 | 压缩后仍记得任务并继续 |
| R3-08 | 每模型上下文高级设置 | D | 已验证 | 参数保存并进入 Runtime |
| R3-09 | 模型选择器与聊天 UI 精修 | B | 已实现，待专项/人工验收 | 视觉人工验收 |
| R3-10 | 完整语义化页面能力 | F | 进行中：F1 完成，用户/岗位/参数已接入 | 页面覆盖与权限验收 |
| R3-11 | Prompt 数据库化与管理员可配置 | H | 进行中 | Prompt 保存/版本/恢复默认/运行时生效 |
| R3-12 | 其他业务页面全面接入 AI | F | 进行中：覆盖矩阵已建立 | 页面覆盖矩阵与真实操作 |
| R3-13 | 跨页面导航与连续业务操作 | F | 基础协议已实现，真实跨页闭环待验 | AI 跨页面完成完整业务目标 |
| R3-14 | Conversation/Run/UI 状态刷新恢复 | G | 进行中 | F5/跨页后无感恢复 |
| R3-15 | 用户历史会话与长期恢复 | G | 进行中 | 重新登录后历史可找可继续 |
| R3-16 | 用户偏好、历史入口、自动恢复与撤销 | G | 进行中 | 新手/高级用户分层体验 |
| R3-17 | 系统级与用户级 AI 设置分层 | H | 进行中 | AI 管理模块、模型优先级与权限 |
| R3-18 | 管理员只读会话审计 | H | 未开始 | 全员只读审计且不能接管 |

## 2.1 当前执行快照（2026-09-18 续跑校正）

> 本节必须在每个阶段或小阶段完成后更新，避免代码进度与计划状态脱节。状态以当前两个 GitHub 开发分支真实代码和 CI 为准。

当前仓库：

- 后端分支：`chatgpt/ai-agent-assistant`；当前 HEAD `d1779b54420a167a10c0335739e6e3efa5abdab2`，相对 master `ahead=179 / behind=0`；最新 Backend CI run `35328821905` 成功。
- 前端分支：`chatgpt/ai-agent-assistant`；当前 HEAD `5ec89e7888f4382b52fc555d96bb0bdedd7bfe43`，相对 master `ahead=99 / behind=0`；最新 Frontend CI run `35328874566` 成功。
- 两个仓库相对 master 均为纯 ahead、behind=0，master 未修改。

已经实际落入代码、但部分仍未完成验证的第三阶段内容：

- 后端已加入 phase3 数据库迁移、Run / Checkpoint / Prompt / UserPreference 等领域与服务骨架。
- 后端已加入 Run-aware Agent Loop、Stop/Steering 基础逻辑、Checkpoint compaction、模型运行参数和 Prompt Cache key 支持。
- 后端已加入 Conversation/Run 恢复、Prompt/UserPreference API 的基础实现。
- 前端已加入全局 AI Store、历史/偏好 API 基础、reasoning 中文化和模型选择器 Chevron 精修。
- 前端最新 CI 已通过。

当前验证状态：

- 后端接口漂移编译问题已修复；Backend CI run `35322805306` 已完整通过 Maven Build、Spring Boot 真启动和 Agent API 验证。
- Tool Result 续跑时的消息顺序问题已修复，避免为缓存重排历史时破坏 USER → ASSISTANT(tool) → TOOL 语义。
- 前端 reasoning 中文化相关 CI `35322877317` / `35322885363` 已通过。
- B1 聊天字体账号偏好已经实现；前端 CI `35323184459` / `35323214700` 已通过。
- Fullstack E2E 已恢复绿色：run `35323880260`（backend `e239803b14`，运行时 checkout 当前前端开发分支），第二阶段 Provider/模型/Tool Loop/WRITE/Dock 等浏览器闭环重新通过。
- Fullstack E2E run `35325844042` 已通过第三阶段专项场景：字体切换/Dock/reload、Stop、双 ESC 内部菜单防误触、Steering；数据库同时验证 USER_STOP/DOUBLE_ESC 的 CANCELLED、STEERING 的 SUPERSEDED，以及旧 Run 迟到 Assistant 消息未落库。
- D/E Backend CI run `35326094428` 已通过：临时 8K/50% 预算触发自动压缩，生成 Checkpoint 后同一 Run 自动继续；Run usage 记录 input/cache-read/total，mock 请求日志确认 conversation 与 compaction 两类 prompt_cache_key。该证据只证明 Runtime/协议链路，不冒充真实 GPT 缓存命中。
- D5 高级设置已在完整 AI 配置和 Quick Settings 共用同一 Dialog；Frontend CI `35326421533` 及 Fullstack rerun `35325844042` attempt 2 已通过 64K/75% → 72K/70% → 恢复默认的浏览器/API 持久化验证。

下一步：

1. B/C 主闭环已通过；继续补 B4 的业务页面/表单焦点防误停，以及 C3 的 pending WRITE Steering 边界。
2. D8 模型高级设置 UI 已实现，正在跑 Frontend CI；D7 自动压缩 + E cache usage 已加入 Backend CI 专项场景。
3. D/E 验证通过后立即更新本文件和 `13`。
4. 之后继续 F → G → H → I，每完成一个小阶段立即同步文档。

## 3. 实施顺序总览

为减少返工，本阶段按以下依赖顺序推进：

```text
A. 基线与数据模型
        ↓
B. UI/交互基础与 Reasoning 展示
        ↓
C. Run 生命周期：Stop + ESC + Steering
        ↓
D. Context Budget + Checkpoint + Auto Compaction
        ↓
E. Prompt Cache + Usage 可观测
        ↓
F. 完整语义化页面能力 + 跨页面导航
        ↓
G. Conversation 持久化、历史会话与用户偏好
        ↓
H. AI 管理模块、Prompt 管理与会话审计
        ↓
I. 全量回归、真实 GPT 验证、Public Preview、用户场景验收
```

每个阶段都采用：

`实现 → 自动验证 → 用户诉求回看 → 修复 → 再验证 → 进入下一阶段`

## 4. 阶段 A：当前基线复核与第三阶段数据模型

### A1. 重新检查仓库实际状态（已验证）

对应：全部需求的实施前置。

任务：

- [x] 重新检查后端 `ShanySky/RuoYi-Vue` 当前开发分支 HEAD、master 差异、Java/Spring Boot/Spring AI 实际版本。
- [x] 重新检查前端 `ShanySky/RuoYi-Vue3` 当前开发分支 HEAD、Vue/Vite/Element Plus 实际版本。
- [x] 重新跑当前 Backend CI / Frontend CI / Fullstack E2E，确认第三阶段开始前基线绿色。
- [x] 检查第二阶段 Provider、模型选择、能力检测、Dock、Tool Loop、WRITE 确认功能未发生仓库漂移。

完成标准：

- 明确第三阶段真实基线 SHA。
- 记录到 `13`。
- 不基于静态文档假设版本。

### A2. Run 生命周期设计（已实现，待专项验证）

对应：R3-02、R3-04。

任务：

- [x] 审核现有 Conversation / Message / Pending Tool Call 表结构。
- [x] 已采用独立 `ai_run` 表，并在 Message / Pending Tool 上关联 `run_id`。
- [x] 每个 Run 已追踪：runId、clientRunKey、conversationId、userId、modelId/modelCode、reasoning、status、start/end、supersededBy、cancel reason、Prompt 版本与 Usage。
- [x] 已通过 Conversation 行锁 + 条件 UPDATE 定义活动 Run 的状态转换与并发保护。
- [x] 新 Run 创建时会在同一事务内 supersede 旧活动 Run，并取消旧 pending Tool / in-flight 模型调用。
- [x] 已定义请求取消与迟到模型响应丢弃规则；前后端均有 generation/run 状态二次检查。
- [x] 已在模型返回、Tool Call/Result 恢复、前端 WRITE 确认后执行前进行 Run/generation 状态检查。

验收重点：

- 数据模型不是为了“状态丰富”，而是必须真实支撑 Stop 与 Steering 不乱执行。

### A3. Context / Checkpoint 数据模型（已实现，待专项验证）

对应：R3-07、R3-08。

任务：

- [x] 已加入模型级 `context_window_tokens`、`auto_compaction`、`compaction_threshold_percent` 字段。
- [x] 第一版默认：65536 tokens / 开启 / 75%。
- [x] 已增加 `ai_checkpoint` 与对应 Domain/Mapper/Service。
- [x] Checkpoint 已记录 `covered_sequence_no`、Run、模型、估算 token 与状态。
- [x] 当前 compaction 只新增 Checkpoint，不删除原始消息。
- [x] 已定义取消时回退既有 Checkpoint、其他压缩失败安全报错且不删除原消息；仍待专项故障场景验证。

### A4. Cache Usage 数据模型/观测点（基础实现已完成，待 Provider 证据）

对应：R3-05。

任务：

- [ ] 核对当前 Spring AI ChatResponse Usage 实际可获得字段。
- [x] 已在 `ai_run` 记录 input/cache-read/cache-write/total token usage。
- [x] Usage 只记录计数，不把 Prompt/Token 明文写入普通日志。
- [x] 当前按 Run 记录 usage，便于与模型、reasoning、Prompt 版本关联。
- [x] `AiRun`/`AiRunMapper` 已预留 cache read/write 字段；真实 GPT 是否返回这些字段留到 E 阶段验证。

## 5. 阶段 B：聊天 UI、设置与 Reasoning 修正

### B1. 聊天字体大小（Dock/刷新已验证，重登恢复待 G）

对应：R3-01。

任务：

- [x] AI 设置新增“小 / 标准 / 大 / 特大”。
- [x] 只作用于消息正文、输入区、Tool 状态等聊天内容。
- [x] 小窗和 Dock 共用 CSS 变量。
- [x] 字体偏好最终写入用户级 AI 偏好；localStorage 仅作为前端缓存。
- [ ] 页面刷新、重新登录和换设备后按账号偏好恢复。
- [x] 不影响 RuoYi 业务页面字号。

自动验收：

- [x] 修改字体后读取 computed style。
- [x] 小窗 → Dock 后值保持。
- [x] reload 后值保持。
- [ ] 重新登录后仍按用户账号偏好恢复。

用户场景验收：

- [x] 字体变化已通过浏览器 computed style + Dock/reload 场景验证；标题/按钮未使用正文 CSS 变量。

### B2. 模型选择器视觉精修（已实现，待视觉验收）

对应：R3-09。

任务：

- [x] 已删除主触发器文本字符假箭头。
- [x] 主触发器已使用 Element Plus `ArrowDown`。
- [x] 已调整 trigger padding、图标尺寸与基线。
- [x] 模型名已使用 ellipsis。
- [x] Hover / Selected / Disabled 已沿用 Element Plus / RuoYi 主题变量。
- [x] Reasoning 继续使用“档位”二级入口。
- [ ] 小窗和 Dock 仍需浏览器视觉验收。

自动/视觉验收：

- [ ] production build。
- [ ] Chromium 截图。
- [ ] 人工确认与 RuoYi 风格一致。

### B3. Reasoning 档位修正和中文显示（已实现，待全栈场景验收）

对应：R3-06。

任务：

- [x] 后端能力枚举重新核对当前 GPT 实际支持档位。
- [x] GPT-5.6 探测覆盖 none/low/medium/high/xhigh/max。
- [x] 不再把 minimal 当成 none。
- [x] 前端显示：无/低/中/高/极高/最高。
- [x] API 与 DB 保存原始英文值。
- [x] 非 GPT Provider 仍以实际探测结果展示。
- [x] 聊天模型选择器和系统模型高级设置统一映射。

验收：

- [x] UI 不再出现英文档位（Frontend CI 已通过，仍需浏览器 E2E 复核）。
- [ ] 选择“无”时实际发送 `none`（Provider 需要该参数时）。
- [ ] 选择“最高”时实际发送 `max`。
- [ ] 不支持档位不展示。

### B4. 双 ESC 前端交互（AI 内场景已验证，业务焦点防误停待补）

对应：R3-03。

任务：

- [x] 仅 AI Panel / AI 自身浮层焦点参与双 ESC。
- [x] 时间窗为约 700ms。
- [x] 第一次 ESC 显示“再按一次 Esc 停止当前执行”。
- [x] 约 720ms 自动清除。
- [x] AI 模型 Popover 与 Element Plus AI 内浮层会优先消费/清空 ESC 序列。
- [x] Quick Settings 已提供账号级“双击 ESC 中断”开关。

自动验收：

- [x] AI input 聚焦时双 ESC 命中。
- [ ] 业务 input 聚焦时双 ESC 不命中。
- [ ] RuoYi 编辑弹框内 ESC 不停止 AI。
- [x] AI 模型菜单打开时第一次 ESC 只关菜单。

## 6. 阶段 C：Run 控制、Stop 与 Steering

### C1. 后端 Run 状态机（已验证）

对应：R3-02、R3-04。

任务：

- [x] 创建 Run。
- [x] 新用户消息绑定 Run。
- [x] 模型调用前检查状态。
- [x] 模型响应返回后再次检查状态。
- [x] Tool 返回前检查状态。
- [x] Tool Result 恢复前检查状态。
- [x] WRITE 用户确认前/后检查状态。
- [x] 为同一 Conversation 建立活动 Run 互斥或原子状态更新。

### C2. Stop API 与真正中断（已验证）

对应：R3-02。

任务：

- [x] 前端运行时“发送”按钮切换为“停止”。
- [x] Stop 发起后立即更新 UI。
- [x] 后端 cancel endpoint / run cancel 逻辑。
- [x] 尽可能取消 Spring AI / HTTP 上游请求。
- [x] 无法取消的迟到响应必须丢弃，不继续 Tool。
- [x] 已产生但未执行 Tool 取消。
- [x] pending WRITE 取消。
- [x] 已提交完成 WRITE 只记录真实状态，不伪造回滚。

自动验收场景：

- [x] 模型慢响应时点击 Stop。
- [x] 后端模拟迟到响应。
- [x] 迟到响应没有产生 Assistant 后续动作和 Tool。
- [x] Stop 后可以立即发送下一条消息。

### C3. Steering / Supersede（主链与 pending WRITE Steering 已验证）

对应：R3-04。

任务：

- [x] AI running 时输入框保持可输入。
- [x] 用户补充消息立即持久化。
- [x] 当前 Run 原子标记 SUPERSEDED。
- [x] 新 Run 基于完整最新 Conversation 创建。
- [ ] 多次快速补充全部按顺序保留，不丢消息。
- [ ] 新规划基于旧 Run 已经真实完成的 Tool 结果。
- [x] 旧 Run 尚未执行的 Tool 不再继续。
- [ ] pending WRITE 确认被新补充覆盖时取消旧确认链。

核心 E2E：

```text
用户：把 ry 昵称改成 A
AI：开始查询
用户：等等，改成 B，而且先不要保存
→ 旧 Run 停止
→ 不提交 A
→ 新 Run 知道用户最终要求是 B + 不保存
```

验收不能只看状态表，必须检查真实页面/数据库没有执行旧要求。

### C4. Stop 与 ESC 联动（已验证）

对应：R3-02、R3-03。

任务：

- [x] Stop 按钮和双 ESC 进入同一后端取消逻辑。
- [x] 不维护两套取消语义。
- [x] ESC 只是 Stop 的快捷触发方式。
- [ ] UI 提示统一。

## 7. 阶段 D：Context Budget、Checkpoint 与自动压缩

### D1. Token 预算计算（已实现，真实模型 token 精度待 I）

对应：R3-07、R3-08。

任务：

- [x] 明确 Runtime 可获得的实际 token usage。
- [x] 无本轮 usage 时建立合理估算方式。
- [x] 计算稳定 System Prompt、Tool schema、Checkpoint、近期消息和动态 Context 占用。
- [x] 以模型配置 64K 为第一版工作预算。
- [x] 75% 到达时触发 compaction。
- [x] 预留输出、Tool 和控制信息空间，避免顶到硬上限。

### D2. Checkpoint Prompt（结构化实现与传输已自动验证）

对应：R3-07。

任务：

- [x] 编写专用压缩提示词。
- [x] 明确要求保留 `11` 中列出的 16 类关键信息。
- [x] 输出采用固定 8 组结构化栏目，Backend CI `35328061798` 已确认实际发送给 Compaction 模型。
- [x] 要求区分事实 / 暂定方案。
- [x] 要求说明“当前正在做什么”和“下一步”。
- [x] 要求记录已完成不可逆操作。
- [x] 要求记录 pending Tool / WRITE。
- [x] 要求保留最新 Steering 指令。
- [x] 明确禁止重复已完成任务。

### D3. 安全压缩点

对应：R3-07。

任务：

- [x] cutoff 保留最新用户回合，已配对 Tool Call/Result 同段进入压缩；未配对旧 Tool 显式标记为已取消/未完成。
- [x] 当前 pending Tool 所属最新用户回合不进入旧历史压缩段。
- [x] Compaction 只发生在模型调用前；WAITING Tool/WRITE 不在压缩过程中被折叠，旧未完成 WRITE 会被标明不得视为成功。
- [x] 压缩 payload 已携带关联 Run 的 status / cancel reason。
- [x] 当前 USER 保持原始消息；Backend CI `35328061798` 断言 `LATEST_USER_RAW=1`。

### D4. 压缩后自动续跑（Runtime 闭环已验证，真实模型语义待 I）

对应：R3-07。

任务：

- [x] 触发阈值后生成 Checkpoint。
- [x] 重建 Runtime context。
- [x] 不要求用户重新发送。
- [x] 自动继续当前 Run。
- [ ] UI 只显示轻量“正在整理较早上下文”，不弹模态确认。
- [x] 压缩失败时明确恢复路径。

核心验收：

- [ ] CI 已用 8K/50% 确定性触发链路；默认 64K/75% 的等比例真实长会话留到 I 做最终压力验收。
- [x] CI 使用 LONG_CONTEXT_A → LONG_CONTEXT_B 明确连续任务触发压缩。
- [ ] mock 已证明结构化 Checkpoint 被重新注入并继续最新指令；真实模型对主题/进度/下一步的语义回答留到 I。
- [x] 自动继续原任务；Backend CI `35328061798` 要求主模型只有读到 Checkpoint 且保留原始 LONG_CONTEXT_B 才返回 `CHECKPOINT_CONTINUED_OK`。
- [ ] 不重复已经完成的 WRITE。
- [x] 原始 DB 消息仍完整存在；Backend CI `35328061798` 分别断言 LONG_CONTEXT_A/B 原始 USER 消息仍各存在 1 条。

### D5. 模型高级设置（已验证）

对应：R3-08。

任务：

- [x] 系统模型新增“高级设置”入口。
- [x] 64K / 自动压缩 / 75% 参数。
- [x] 参数后端持久化。
- [x] Quick Settings 与完整 AI Config 共用数据。
- [x] 不把参数塞回主表格造成视觉拥挤。

## 8. 阶段 E：Prompt Cache 与真实 GPT 验证

### E1. 再查官方当前实现（已完成）

对应：R3-05。

任务：

- [x] 查当前 OpenAI Prompt Caching 官方文档。
- [x] 查当前 GPT-5.6 具体缓存行为。
- [x] 查当前 Spring AI 版本对 prompt cache key / extra body / usage metadata 的实际支持。
- [x] 以仓库实际依赖版本为准，不使用过时 API。

### E2. Prompt 稳定前缀重构（已实现，真实命中待 I）

对应：R3-05。

任务：

- [x] 将长期系统规则与动态 Page Context 拆开。
- [x] Tool schema 稳定排序；前端 `5ec89e7` 已在 `toolRegistry`/CRUD capability 输出侧固定声明顺序，Frontend CI `35328874566` 通过。
- [x] 不因 route/Page Context 每轮变化破坏最前面的固定前缀。
- [x] Checkpoint 进入合适位置。
- [x] 保持多轮消息追加顺序。
- [ ] 对实际序列化 HTTP payload 做验证。

### E3. Cache Key / Provider 适配（基础完成，真实 Provider 兼容待 I）

对应：R3-05。

任务：

- [x] GPT/OpenAI 路径使用当前推荐方式。
- [x] cache key 粒度评估：用户/Conversation/稳定业务前缀。
- [x] 避免不同用户敏感上下文意外共享业务语义。
- [ ] 兼容不支持缓存参数的 OpenAI-compatible Provider。
- [ ] 不因缓存配置导致非 GPT Provider 请求失败。

### E4. 缓存可观测性（后端记录完成，管理展示待 H）

对应：R3-05。

任务：

- [x] 记录每 Run 的 input tokens。
- [x] Provider 提供时记录 cache read / cache write。
- [ ] 提供后端可查看的调试/统计信息。
- [x] 不把 Prompt/Token 明文写普通日志。
- [ ] 能解释缓存未命中的主要原因。

### E5. 真实 GPT 验收

对应：R3-05。

必须尽量使用当前用户实际测试 GPT Provider 做：

- [ ] 同一 Conversation 首次请求。
- [ ] 保持大段稳定前缀继续第二/第三轮。
- [ ] 检查 Provider usage。
- [ ] 证明后续请求出现 cache read。
- [ ] 对比重复输入成本。
- [ ] 记录模型、请求结构、cache usage 证据到 `13`。

如果某中转 Provider 不返回 cache usage，不得直接写“缓存已验证”；需要明确记录证据缺口，并使用能验证的链路补证据。

## 9. 阶段 F：完整语义化页面能力层

### F1. 页面与动作盘点（已完成）

对应：R3-10。

任务：

- [x] 从当前前端源码建立 19 个 system/monitor/tool 主要页面清单，并在 `13` 维护覆盖矩阵。
- [x] 已逐页盘点查询字段、表单字段、权限按钮和主要业务动作；特殊页面单独标记。
- [x] 已在 `13` R3-10 节建立 19 页页面能力覆盖矩阵。
- [x] 已区分已接入、待接入、只读优先、特殊页面/不适合普通 CRUD 自动化。
- [x] 矩阵依据当前页面源码与真实动作建立，不以通用 factory 代替页面盘点。

### F2. 通用 Page Capability 协议（主体已实现，schema 声明与稳定排序已完成，字典/复杂校验继续补强）

对应：R3-10。

任务：

- [x] 已定义 pageId/pageName/route/pageInstanceId/pageVersion。
- [x] 已定义 queryFields/formFields 与 JSON inputSchema；前端 `d9150c7` 强制按页面显式声明字段 schema，`5ec89e7` 固定 Tool/schema 序列化顺序；字段字典/复杂校验继续补强。
- [x] 已支持 query/pagination/rows/selectedIds 等页面上下文。
- [ ] 定义字典/选项。
- [ ] 定义校验规则。
- [x] CRUD factory 与页面 actions 均生成语义动作及参数 schema。
- [x] 前端按当前用户 permission 过滤暴露；后端再次按 ToolPolicy 权威校验 permission/risk/schema。
- [x] Tool handler 返回结构化执行结果并回传 Agent Loop。
- [x] 已兼容现有 Pending Tool / Tool Result / Run 状态机。

### F3. 权限与风险边界（基础链已实现，高影响动作继续细分）

对应：R3-10。

原则：

- [ ] 不人为维持 Demo 白名单。
- [x] 前端不暴露无权限 Tool；后端 ToolPolicy 和原业务 API 双重权限校验。
- [x] 页面 Tool 调用现有业务 API/页面方法，不直接绕过后端数据范围。
- [x] 前端语义 Tool 不直接更新数据库。
- [x] WRITE / DANGEROUS_WRITE 继续走内联用户确认与 Run 安全检查。
- [ ] 导出/重置密码/删除/批量操作等按真实业务风险处理。
- [x] Page Capability 只描述当前用户可见能力，不赋予额外业务权限。

### F4. 用户管理完整覆盖（代码已覆盖，专项全动作验收进行中）

对应：R3-10。

用户管理作为第一个完整覆盖页面：

- [ ] 所有查询条件。
- [ ] 表格上下文/选择。
- [ ] 新增用户。
- [ ] 编辑当前业务允许的全部字段。
- [ ] 启用/停用。
- [ ] 删除。
- [ ] 重置密码。
- [ ] 分配角色/岗位。
- [ ] 导入/导出。
- [ ] 批量操作。
- [ ] 其他当前页面实际可用动作。
- [ ] userName / nickName 既有语义边界继续正确。

### F5. 其他业务页面全面接入（进行中）

对应：R3-10、R3-12。

任务：

- [ ] 使用同一 Page Capability 协议扩展当前 RuoYi 其他业务页面。
- [x] 已从当前真实源码建立 19 个主要页面覆盖矩阵。
- [x] 已先接用户、岗位、参数设置；其余系统管理及高频页面继续推进。
- [ ] 每个页面逐项回填查询字段、表格上下文、表单字段、Dialog/Drawer、业务动作、权限状态。
- [ ] 管理员可在 AI 管理中控制“哪些页面允许接入 AI”。
- [ ] 无法覆盖的页面或动作必须在 `13` 记录技术原因、用户影响和后续方向，不能静默忽略。

完成判断：

> 用户说“这个页面上我能正常做的事，为什么 AI 看不到/做不了”的情况应显著减少，并有完整覆盖矩阵说明真实边界。

### F6. 跨页面导航与连续业务操作（基础协议已实现，代表性闭环待验）

对应：R3-13。

任务：

- [x] Page Context 已提供 availablePages，`app_navigate` 提供语义化导航。
- [x] `app_navigate` 仅通过 Vue Router；生产能力未引入坐标/任意 DOM 控制。
- [ ] 导航前确认当前用户有目标页面访问权限。
- [x] 导航后等待目标 route 的新 pageInstanceId 注册完成再返回 Tool Result。
- [x] 每轮请求与 Pending Tool 已绑定 route/pageInstanceId/pageVersion。
- [x] 前端 Tool 真正执行前和后端 Tool Result 恢复均校验页面实例快照。
- [ ] Agent 在一个业务目标中可以连续跨多个页面，不要求用户重新发送指令。
- [ ] 跨页面期间 Conversation、Run、模型、reasoning、Checkpoint、Steering 状态保持。

核心 E2E：

```text
用户：检查 ry 的角色；如果 test-role 不存在就创建，再给 ry 分配
→ 用户管理定位 ry
→ 角色管理检查/创建角色
→ 用户授权页面完成分配
→ Conversation 不变，整个过程连续完成
```

验收不能只验证 Router 跳转成功，还必须验证跨页后的业务结果正确、旧页面 Tool 未被继续调用。

## 10. 阶段 G：Conversation 持久化、历史会话与用户偏好

### G1. 后端 Conversation / Run 恢复 API

对应：R3-14、R3-15。

任务：

- [ ] 提供当前用户自己的会话列表、会话详情、消息历史、Checkpoint、当前 Run / Pending 状态读取 API。
- [ ] 会话列表按最后更新时间排序，并支持标题搜索和时间分组所需字段。
- [ ] 会话详情只允许会话所属用户读取；管理员审计走独立只读接口和独立权限。
- [ ] F5 恢复时能够返回当前 Conversation、模型/reasoning、消息、当前 Run 和 pending 状态。
- [ ] 明确刷新后 RUNNING / WAITING_TOOL / WAITING_WRITE 等状态应该恢复、取消还是重新确认。
- [ ] 退出登录时安全取消当前用户活动 Run，pending Tool / WRITE 不得在下次登录时自动继续危险动作。

### G2. 前端全局 AI Store 与 Tab 级恢复入口

对应：R3-14。

任务：

- [ ] 将 conversationId、messages、activeRun、model/reasoning、Draft 等关键状态从 AiAssistant 局部 ref 抽到应用级 AI Store。
- [ ] Layout、AI Assistant、Page Capability Registry、跨页面导航共用该 Store。
- [ ] sessionStorage 保存当前 Tab 的 activeConversationId 和未发送 Draft 等“恢复入口”。
- [ ] localStorage / 本机存储只保存设备相关 UI 状态，例如 Dock 宽度。
- [ ] 页面正常路由切换不重建 Conversation。
- [ ] F5 后读取 sessionStorage，再以服务器数据重建 Store，服务器始终是 Source of Truth。
- [ ] 不同 Tab 的 activeConversationId 互相隔离。
- [ ] 同一 Conversation 即使被多个 Tab 打开，后端仍只允许一个活动 Run。

核心验收：

- [ ] 用户在会话中按 F5，聊天记录和当前会话自动恢复。
- [ ] AI 正在 Run 时刷新，能恢复真实 Run 状态，而不是回到“新会话”。
- [ ] 刷新前 Page Capability Instance 失效，刷新后新实例重新注册。
- [ ] 两个浏览器 Tab 可以分别打开不同 Conversation。

### G3. 用户历史会话管理

对应：R3-15。

任务：

- [ ] 提供“我的会话”列表。
- [ ] 支持最近会话、时间分组、标题搜索。
- [ ] 支持新建、切换/恢复、重命名、归档/删除（受系统策略约束）。
- [ ] 第一条消息先生成临时标题，首轮完成后可生成更易读标题；后续不频繁自动改名。
- [ ] 重新打开历史会话后加载 Message、Checkpoint、模型/reasoning 等继续工作所需状态。
- [ ] 关闭浏览器、退出登录后重新进入，历史 Conversation 仍存在。
- [ ] 重新打开曾因退出登录中断的会话时，明确显示上次执行已中断，不自动续跑危险操作。

### G4. 用户 AI 偏好

对应：R3-01、R3-03、R3-16、R3-17。

任务：

- [ ] 建立用户级 AI 偏好后端存储，localStorage 只作为前端缓存或设备级覆盖。
- [ ] 保存“我的默认模型”与“我的默认 reasoning”。
- [ ] 保存聊天字体、发送快捷键、双 ESC 开关。
- [ ] 保存是否显示会话历史入口。
- [ ] 保存是否登录后自动恢复上次会话，默认关闭。
- [ ] 保存 AI 助手默认打开方式：默认小窗、默认 Dock，或恢复上次使用模式。
- [ ] 系统默认模型和用户默认模型严格分层：用户默认有效时优先；被管理员禁用/下线后自动回退系统默认并提示。
- [ ] Dock 宽度等明显受设备尺寸影响的 UI 偏好采用本机优先，避免跨设备污染。

### G5. 恢复上次会话与撤销恢复

对应：R3-16。

任务：

- [ ] 默认提供显式“恢复上次会话”操作。
- [ ] 高级用户开启“登录后自动恢复上次会话”后，登录时自动切回上一次 Conversation。
- [ ] 手动或自动恢复后提供短时“撤销恢复”提示。
- [ ] 恢复前暂存 previousConversationId、Draft、模型/reasoning 等必要 UI 状态。
- [ ] 撤销后恢复到恢复前状态。
- [ ] 一旦用户在恢复后的会话发送新消息、创建新 Run 或执行新的 Agent 行为，撤销立即失效。
- [ ] 撤销只表示“切回恢复前会话状态”，绝不暗示回滚已经发生的业务操作。

用户场景验收：

- [ ] 用户误点“恢复上次会话”，在未产生新行为前可以一键撤销。
- [ ] 用户恢复后发出新消息，撤销入口消失，但仍可通过历史列表切换会话。
- [ ] 默认新手用户不显示历史入口；开启偏好后才出现低干扰历史按钮。

## 11. 阶段 H：AI 管理模块、Prompt 管理与会话审计

### H1. 一级“AI 管理”模块与权限边界

对应：R3-17。

任务：

- [ ] 新增一级“AI 管理”模块，仅向有对应管理权限的用户显示。
- [ ] 至少组织为：AI 服务、模型管理、提示词管理、页面能力、会话审计。
- [ ] “运行与用量”作为可扩展入口，可承载 Token / cache usage 等信息，但第三阶段不为了形式完整做复杂运营平台。
- [ ] 管理员系统配置与普通用户“我的 AI 设置”使用不同菜单/入口和权限。
- [ ] Provider/模型管理权限与会话审计权限分离。
- [ ] 系统配置修改进入 RuoYi 操作日志。

### H2. 系统级 AI 配置与用户级设置分层

对应：R3-17。

系统级由管理员控制：

- [ ] Provider Base URL / Token / 是否启用。
- [ ] 远端模型加载、哪些模型加入系统、哪些模型允许用户使用。
- [ ] 系统默认模型。
- [ ] 模型上下文窗口、自动压缩默认参数、能力检测。
- [ ] System Prompt / Compaction Prompt。
- [ ] 哪些页面允许接入 AI。
- [ ] 会话保留天数、是否允许用户归档/删除自己的会话等会话管理策略。
- [ ] 明确历史清理/保留策略的实际执行方式，避免“界面有配置但从不生效”。
- [ ] 会话清理不得破坏仍需恢复的有效 Conversation；归档、删除和系统清理语义分别定义清楚。

用户级由本人控制：

- [ ] 我的默认模型。
- [ ] 我的默认 reasoning。
- [ ] 字体、发送快捷键、双 ESC。
- [ ] 历史入口、自动恢复。
- [ ] 其他不改变系统安全边界的个人体验设置。

配置优先级验收：

- [ ] 用户默认模型有效且系统仍允许时使用用户默认。
- [ ] 用户默认被管理员禁用/下线后回退系统默认并给予合理提示。
- [ ] 用户不能通过个人设置选到系统未开放模型。
- [ ] reasoning 只能从当前模型实际支持且系统允许的档位中选择。

### H3. System Prompt / Compaction Prompt 数据库化

对应：R3-11。

任务：

- [ ] 建立系统 Prompt 配置表或等价持久化模型。
- [ ] 至少支持 SYSTEM_PROMPT 与 COMPACTION_PROMPT。
- [ ] 保存内容、启用状态、版本号、修改人、修改时间。
- [ ] 提供管理员读取、编辑、保存和恢复系统默认。
- [ ] 支持必要模板变量，并在保存前校验关键变量/模板结构。
- [ ] Conversation / Run 记录实际使用的 Prompt 版本，便于审计。
- [ ] Runtime 从数据库加载 Prompt，不再把当前业务 System Prompt / Compaction Prompt 长期硬编码在 Java。
- [ ] 后端权限、数据范围、WRITE 确认、Tool/Capability 校验不能因 Prompt 可配置而弱化。

验收：

- [ ] 修改 System Prompt 后新 Run 实际使用新版本。
- [ ] 修改 Compaction Prompt 后新 Checkpoint 使用新版本。
- [ ] “恢复默认”后内容和版本正确。
- [ ] 管理员写入恶意“绕过权限”Prompt 时，服务端权限仍生效。

### H4. Prompt Cache 的管理员边界

对应：R3-05、R3-17。

任务：

- [ ] Prompt Cache 默认作为 Runtime 应利用的能力，不做“开启/关闭缓存”管理员开关。
- [ ] 当前阶段不暴露 Cache Key 策略配置 UI。
- [ ] 后端按当前 Provider/模型能力自动选择可用 cache key / cache 参数。
- [ ] 管理界面只在“运行与用量”或诊断信息中展示 cache usage 证据，不要求管理员理解内部策略。
- [ ] 未来只有出现真实多租户、复杂路由或特殊 Provider 场景时，再评估策略配置 UI。

### H5. 页面接入管理与安全策略边界

对应：R3-12、R3-17。

本阶段管理员可配置：

- [ ] 哪些业务页面允许接入 AI。
- [ ] 查看页面能力覆盖/注册状态。

本阶段固定实现但不做复杂策略编辑器：

- [ ] RuoYi 原权限与数据范围。
- [ ] WRITE / 高风险操作确认的可靠服务端规则。
- [ ] Tool / Capability 服务端校验。
- [ ] 页面实例/version 校验。

暂不要求复杂 UI：

- [ ] 按动作类别自定义确认策略。
- [ ] 全局 Tool/Capability 规则编辑器。
- [ ] 大量策略表达式或企业级策略 DSL。

只有出现明确业务需求时再在后续阶段开放，避免当前阶段过度设计。

### H6. 管理员只读 AI 会话审计

对应：R3-18。

任务：

- [ ] 增加独立审计权限，例如 `ai:conversation:audit`，具体命名实施时按 RuoYi 现有权限规范确定。
- [ ] Provider/模型管理权限不自动授予查看所有用户对话的能力。
- [ ] 审计列表可按用户、Conversation ID、时间、模型、状态、标题等筛选。
- [ ] 可标识是否发生 Tool / WRITE / Stop / Steering / Compaction。
- [ ] 会话详情只读展示 Message、Tool Call/Result、WRITE 确认、Checkpoint、Run 状态等。
- [ ] 审计页不提供输入框、继续、重试、接管、Tool 执行或 WRITE 操作。
- [ ] 管理员查看其他用户对话本身进入访问/操作日志。

权限验收：

- [ ] 普通用户只能看自己的会话。
- [ ] 只有 Provider 管理权限、没有审计权限的管理员无法读取其他用户会话。
- [ ] 有审计权限的管理员可以查看所有用户会话，但无法创建/继续他人 Conversation 的 Run。
- [ ] 对审计详情的访问可追溯。

## 12. 阶段 I：全栈回归和最终用户场景验收

### I1. 基础工程验证

- [ ] Backend Maven package。
- [ ] Backend 真正启动。
- [ ] MySQL 真连接。
- [ ] Redis 真连接。
- [ ] Frontend production build。
- [ ] Vite 真启动。
- [ ] 接口真实调用。
- [ ] Chromium 浏览器 E2E。

### I2. 第二阶段回归

必须重新验证：

- [ ] Provider 保存 / Token 加密。
- [ ] 测试模型加载。
- [ ] 远端模型选择。
- [ ] 自动能力检测。
- [ ] 默认模型星标。
- [ ] reasoning 切换。
- [ ] 小窗 / Dock。
- [ ] Dock 拖动。
- [ ] Enter / Shift+Enter / Ctrl+Enter。
- [ ] Tool Loop。
- [ ] WRITE 确认。
- [ ] userName / nickName。
- [ ] pending Tool Result 使用原模型/档位。

### I3. 第三阶段用户场景验收

必须按 R3-01 ~ R3-18 一项不漏逐条验收，至少完成以下真实闭环：

1. 调字体后刷新和切 Dock，偏好仍保持。
2. 长响应中点击停止，旧 Run 不再继续 Tool/WRITE。
3. 业务弹框按 ESC 不误停；AI 内双 ESC 正确停止。
4. AI 工作中连续补充指令，最终按最新要求执行。
5. Steering 期间不重复 WRITE，也不让旧 Run 死灰复燃。
6. 64K/75% 达阈值自动压缩，压缩后自动继续。
7. 压缩后仍能说明当前任务、已完成进度、下一步和 pending 状态。
8. GPT 真实请求出现 cache read 证据。
9. reasoning 中文档位与真实 Provider API 值正确映射。
10. 当前页面完整字段/动作可由 AI 使用，权限不足动作仍被拒绝。
11. 管理员修改/恢复 System 与 Compaction Prompt，Runtime 实际使用对应版本。
12. 用户管理之外的其他业务页面进入能力覆盖矩阵，并实际验证代表性操作。
13. AI 为完成同一目标跨多个页面连续操作，Conversation 和 Run 不断。
14. 正常跨页、F5、运行中刷新后 Conversation / Run / pending 状态按真实情况恢复。
15. 用户退出登录再登录后能找到、切换并继续自己的历史会话。
16. 历史入口默认隐藏；开启后可用；手动/自动恢复上次会话支持短时撤销且新 Agent 行为后撤销失效。
17. 系统级 AI 管理与用户“我的 AI 设置”权限和数据边界正确，用户默认模型在系统边界内生效并可安全回退；会话保留/归档/删除策略实际生效，且不会误清理仍需恢复的有效会话。
18. 有审计权限的管理员只读查看所有用户会话，无法接管；无审计权限管理员无法读取他人对话，审计访问有日志。

### I4. Public Preview

- [ ] 创建临时公网预览。
- [ ] 预览包含第三阶段关键功能。
- [ ] 提供按 R3-01 ~ R3-18 编排的人工验收路线。
- [ ] 至少保留足够时间供用户检查。
- [ ] 用户指出的问题回到对应 R3 编号继续修正，不能以 CI 已通过为由结束。

## 13. 实施中禁止的偏航

开发过程中发现以下情况时必须主动纠正：

- 为了“架构漂亮”新增大量与 R3-01 ~ R3-18 无关工作。
- 技术测试全绿，但用户场景仍不好用。
- 为通过 E2E 改测试绕开真实产品问题。
- Stop 只做视觉不做运行时取消。
- Steering 实际只是排队等待。
- Compaction 只做摘要不自动续跑。
- Cache 只设置参数不验证命中。
- Page Capability 只做协议抽象不做页面覆盖。
- 因新增 UI 把第二阶段已有设置功能删掉。

## 14. 每次阶段完成时的回报格式

后续每完成一个阶段，`13` 至少记录：

- 对应需求 ID。
- 实际修改文件/模块。
- 关键决策。
- 自动测试证据。
- 真实运行证据。
- 用户场景是否已通过。
- 已知偏差。
- 下一步。

只有“用户场景已通过”才能把对应需求状态改成“已验证”。
